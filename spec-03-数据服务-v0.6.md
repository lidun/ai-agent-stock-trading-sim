# spec-03 数据服务规格 v0.6（Web 部署版适配：交易时段进程在线保障语义改写）

> 版本：v0.6（2026-09-06，Web 部署版适配轮·随总纲 v0.20 提交，待用户终审：承载形态由桌面壳 + sidecar 重构为**浏览器前端 + Nginx 反代 + core 常驻（systemd）**——本文件仅做在线保障与承载表述改写：§3.5 交易时段在线保障（#56）由"阻止系统睡眠（Windows SetThreadExecutionState / Tauri 电源插件）"改为 **core 进程级交易时段在线保障（systemd 常驻保活，无休眠语义）**，验收口径不变（交易时段闲置 40 分钟不掉采集）；进程守护（#42）由桌面层改为 systemd；采集/拉取/回补/就绪/守护等数据机制与决策不变；v0.5 全部内容保留）
> **状态：⏳ Web 部署版适配稿（随总纲 v0.20 一并以 Web 部署版 P1 基线提交，待用户终审定稿）——定稿后 v0.6 替代 v0.5 成为 Web 部署版开发基线（机制同 v0.5）。**
> 依据：总纲 v0.20（Web 部署版，业务决策承 v0.18）§8 数据服务层（8.1-8.5）、§7.2 回放数据三级精度、§12.1（core 常驻 systemd）、§12.8 决策表；联动 spec-01 v0.7（票级选档、判定序列消费接口、§3.1 官方收盘价独立窗口、§2.8 试运行复位）、spec-02 v0.5（性能记录）、spec-05 v0.6（§6.2 试运行回放数据依赖）
> **受约束决策**：#30（环境标签） #35（数据保鲜） #37（资源闸门·源侧配额声明） #43（数据就绪） #44（复权因子存储） #53（采集与三级精度） #54（数据正确性守恒） #56（交易时段进程在线保障，Web 改写替代防睡眠） #59（收盘价口径）
> **§16 勾销**：#7 #19 #24（数据侧） #25 #27 #29（数据侧） #30（见文末勾销表）
> 与总纲冲突时以总纲为准并同步本文件。

---

## 1. 模块定位与边界

- **职责**：数据源适配（统一接口、能力标签含频率/并发/额度配额声明、优先级链与分层切换）、盘中 3 秒采集器（L0 供给）、分钟/日线拉取与缓存（L1/L2 供给）与**初始历史回补（backfill）**、数据正确性守护（交叉抽检/停牌过滤/单源同日不变式/口径自检/除权跳变兜底/日历校验）、环境标签客观规则、交易日历与参考数据刷新、数据源健康度与故障切换、能力工具（get_market_snapshot/get_quotes_batch 等，供引擎与 Agent 工具层）。
- **不负责**：撮合判定与回放算法（spec-01，本规格只保证"供给的序列正确、就绪、带质量标记"）；记忆向量库（spec-02）；知识库统计（spec-05）；推送（spec-04）；**资源闸门的调度排队（spec-04 #37，本规格提供每源配额声明数据）**。
- **消费方**：引擎结算（spec-01：按票取 L0/L1/L2 序列与官方收盘价）、信号环境标签（spec-01 登记时取 env_bucket）、子 Agent 工具（行情/财务查询走统一工具，经编排层执行器）、spec-04（健康度/配额声明作资源闸门与审批输入）。

## 2. 数据源分层与适配器

### 2.1 统一适配器接口（最小集）与能力标签
```
calendar()                     → 交易日历（含节假日）
daily(symbol, start, end)      → 日线（真实价 OHLCV + 官方收盘价权威列）
minute(symbol, date)           → 当日 1 分钟 OHLCV（L1；历史深度见 §16 #19 验证）
realtime_batch(symbols)        → 批量实时快照（L0 采集源；字段含 价格/涨跌幅/累计成交额，见 §4.1）
snapshot_all()                 → 全市场快照（选股扫描用，§8.4 分级拉取）
financials(symbol)             → 估值/财务（PE/PB/市值等，选股用）
ref_suspend(symbols)           → 停牌状态（前置过滤 #54；含盘中临时停牌中间态）
ref_st_board(symbols)          → ST/退市/板块/涨跌幅限制分类
ref_adjust(symbol)             → 复权因子（存储 spec-01 §2.6 adjust_factors，本规格负责获取与刷新）
index_quotes(index, date)      → 指数（沪深300 等，卖出跟踪基准与环境标签）
```
- **能力标签（v0.4 补配额声明）**：各源实现上述接口并声明——有/无盘口、分钟历史深度、快照频率容忍、**频率配额（次/分钟）、并发上限、每日额度**；配额声明供 spec-04 资源闸门（#37 同源互斥排队、并发排队）消费，数值经 §11 实测固化。
- **预留扩展接口（v0.5 注，对齐总纲 §8.1 数据分类）**：龙虎榜/北向资金/融资融券/资金流向/公告与舆情——适配器插槽预留（P2+ 按"能力申请"接入，走同一适配器规范与配额声明；不阻塞 P1/P2 主链路，避免总纲数据分类与本接口集的口径缺口）。

### 2.2 优先级链与故障切换（v0.4 分层修订）
- **默认链**：`ftai.chat（FTShare，候选第一，P1 验证通过后置顶）→ akshare → 东财直连 → 新浪/腾讯直连兜底`；收费源插槽（tushare pro 等，用户配置启用）；
- **ftai 置顶前提（v0.4 明确）**：验证通过前，**L0 采集默认源与 L1 主源均为 akshare**（§2.3/§2.5）；ftai 经 §11 实测通过后按 §2.3/§2.5 升级；
- **切换规则分层（v0.4 拍板 A2，消除与 §3.3 的矛盾）**：
  - **拉取类**（日线/分钟线/参考数据/交叉抽检）：连续失败/降权触发**当日自动切换**（对新拉取生效，不做拉取中切换）；
  - **采集器**：受 §3.3 单源同日不变式约束——**当日只退避、不切源**，换源次日生效；源级故障的当日断档由完整率自然降级兜底（L1/L2）；
- 源选择记录入数据访问日志（audit 可查：哪个源供了什么）。

### 2.3 L1 分钟源：默认可用 + 升级策略（v0.3 用户二次拍板）
- **默认 L1 主源 = akshare 分钟线**（空窗期即启用，无需等待任何验证）——L1 仅为降级档（分钟级近似+相邻分钟判定，spec-01 §3.3），其精度风险由 quality 标记与 L2 兜底覆盖；akshare 聚合波动由健康度监控与自动切换兜底（§9）；
- **升级路径**：ftai 分时验证通过（§11 清单）→ 升级为主源，akshare 降为兜底；东财直连分钟线验证通过 → 列入备源（故障切换顺序：主源 → 备源 → 兜底）；
- **兜底链（升级后）**：ftai 分时 → 东财直连分钟 → akshare 分钟；akshare 在任意阶段都**保留为最后兜底**（不删除其 L1 角色）；
- **劣化切换**：主源连续失败/降权（§9）→ 自动切换下一候选（拉取类，§2.2 分层规则），健康恢复后冷却回权；
- **长期结论决策点**：若 akshare 分钟在长期运行中健康度持续劣化且免费候选均不达标 → L1 仅收费源（与总纲 §8.2 免费/收费边界一致，P1 末评估）。

### 2.4 免费/收费边界
- EOD/日线级数据免费链恒可用；盘中 L1 分钟数据视免费源验证结果（不达标则仅收费源）；L0 由本地采集器自主供给（不依赖外部实时接口的长期可用性，仅有源频限风险）。

### 2.5 L0 采集源：默认可用 + 升级策略（v0.4 拍板 A1；v0.5 实现载体技术修正）
- **策略不变（免费源默认可用、无验证前置）**：ftai 验证通过前，L0 采集默认走**东财 push2 直连批量行情接口**（多 secid 单请求返回指定清单）——它是 `realtime_batch(symbols)` 语义（按任意清单批量）的天然实现，与 akshare 东财系接口同属一个底层（source_family 同族，§7 抽检校验注意）；
- **akshare 聚合快照（全市场单请求）不用于 3 秒采集（v0.5 技术修正）**：其单请求返回全市场数千标的、重量级且高限流风险，仅用于 `snapshot_all()` 选股扫描与收盘对账——v0.4"默认采集源=akshare 批量快照"的表述在此修正为"默认采集实现=东财直连批量接口（akshare 生态同底层）"：**免费源默认可用策略、升级路径均不变**，仅实现载体按接口语义修正；
- **升级路径**：ftai 批量快照 3 秒频率限流/封禁实测通过（§11）→ 升级为采集源，东财直连批量降为备源；
- 采集源候选集 = 已通过限流实测的源（未实测源不得进入采集轮换——3 秒持续轮询对未验证源是封禁风险）；
- 采集源的切换受 §2.2 分层规则约束（当日不切源）。

## 3. 盘中 3 秒采集器（L0 供给，§16 #25/#30 落地）

### 3.1 采集范围（票级）与清单时点
- 每日开盘前由引擎计算并下发采集清单：`当日有效条件单引用证券 ∪ 当前持仓 ∪ 观察集合`；
- **清单计算时点（v0.4 补）**：默认 9:25 集合竞价结束前完成；启用早盘任务时在 9:15 挂单后重算一次（P1 定终值）；
- **观察集合（v0.2 用户拍板：v0.1 即支持）**：管理 Agent 可配置的常驻观察证券集合——供市场异动探索源（#32）等输入；上限可配（默认 ≤20 只，超出审批）；默认空但配置接口在 v0.1 实现；集合变更次日生效（采集清单每日重算）；
- 清单内证券**停牌即剔除**（§8.5 前置过滤，含当日盘中转停牌的处理见 §3.4），复牌日自动回补。

### 3.2 频率、批量与字段语义
- 交易时段（9:30-11:30、13:00-15:00，北京时间）每 **3 秒**一个采集周期；一次采集 = 对清单内全部证券的**批量快照请求**（单请求尽量覆盖全清单，拆批上限可配默认 1 批）；
- 每个快照字段最小集：`symbol, ts, price(最新价), prev_close, pct_chg, cum_turnover(累计成交额, 必需——spec-01 §4.3 流动性口径依赖), volume_cum(累计量), status(停牌/正常)`；盘口五档为可选扩展字段（当前默认不采，待盘口源验证）；
- **prev_close 语义（v0.4 拍板 B1）**：prev_close = **源的昨收基准价**——除权日为**调整后基准**（与行情软件涨跌幅口径一致），pct_chg 与之同基准；涨跌幅触发单判定、交叉抽检对比、开盘核验（§7）均用此基准；原始昨收盘价另从日线获取（spec-01 #44 复权口径用），两者不得混用；
- **15:00 后延续采样 1-2 分钟**（#59：取最贴近 15:00:00 的有效样本兜底收盘价对齐）；**延续样本落库标记 `is_extended=1`（v0.4 拍板 A3）——不参与回放判定序列，仅作 §4.2 收盘对齐候选**。

### 3.3 节流、退避、单源同日与绑定粒度
- 频率固定 3 秒；失败退避：单次失败跳过该周期（不补拉），连续失败 ≥3 次 → 指数退避（5s/10s/30s…上限 60s），恢复后回到 3 秒；
- **单源同日判定不变式（#54）**：每证券当日绑定采集源并记录（source_binding 表）；换源只对**次日起**生效——当日判定序列同源，杜绝源交界假触达；
- **当日不切源（v0.4 拍板 A2，§2.2 分层规则的采集侧）**：源级失败当日只指数退避等待恢复，**不自动切换源**；当日断档的代价由完整率自然下降 → 按票降级 L1/L2 兜底；
- **绑定粒度与故障代价（v0.4 补）**：批量模式下当日实际绑定 = **采集器当日主源**（全票同源）——源级故障当日 = 全部绑定票 L0 断档降级（非单票影响），日报降级清单须区分原因（源故障 / 覆盖不足）；
- 全源失败 → 当日该票 L0 中断（完整率自然下降 → 自动 L1/L2 降级，spec-01 §3.1）。

### 3.4 午休与停牌（v0.4 补盘中临时停牌）
- 11:30-13:00 不采集；停牌证券不入采集范围（§8.5）；13:00 恢复前重读采集清单（午间子 Agent 可能新增条件单）；
- **盘中临时停牌（v0.4 拍板 A5）**：盘中快照返回 `status=停牌` 的证券——该样本**剔除、不落判定序列**（落库仅标 status=suspended 供审计）；复牌后恢复采集；
- **分母对齐（v0.4 拍板 A5）**：个股盘中停牌时段从该票分母剔除——spec-01 §3.1"当日实际交易秒数"的票级细化 = **该票当日实际可交易秒数**（连续时段折算、午休剔除、15:00 后延续段不计入、**盘中停牌时段剔除**）；剔除秒数落 l0_coverage（suspended_secs）供审计。

### 3.5 交易时段进程在线保障（#56，Web 改写替代"防睡眠"）

- core 为 Linux 常驻服务（systemd，总纲 §12.1），**交易时段内进程级在线保障**——不依赖桌面电源策略，systemd `Restart=always` + 看门狗确保采集器在交易时段不被中断/挂起（服务器无休眠语义，替代原 Windows `SetThreadExecutionState` / Tauri 电源插件防睡眠）；**P1 验收口径不变：交易时段闲置 40 分钟不掉采集**；
- 进程守护（#42）由 **systemd** 负责（采集器崩溃自动拉起）；拉起后按"中途启动"口径补算完整率（见 §4.1）。

### 3.6 落盘与清理
- L0 序列落 SQLite（表见 §5.1），按 (symbol, trade_date) 分区语义组织；**收盘结算后删除当日序列**（可配保留 N 日供审计/复算，默认 0）；
- 保留 N>0 时，l0_ticks/l0_coverage 表随业务库进入每日备份范围（spec-02 §10）。

## 4. 回放数据供给（spec-01 消费接口）

### 4.1 按票就绪判定与取数（票级选档 D4/v0.17.2）
```
is_ready(symbol, trade_date) → {l0_ready, l0_coverage, l1_ready, official_close}
get_replay_series(symbol, trade_date) → {
  level: l0|l1|l2,
  series: [(ts, price[, high, low]) ...],      // 判定用：L0 截止 15:00:00（不含 is_extended）或 L1 分钟 OHLC
  close_candidates: [(ts, price) ...],          // 15:00 后延续样本（v0.4 拍板 A3，仅供收盘对齐）
  official_close, official_close_source, quality, notes
}
```
- L0 覆盖判定（#56 + v0.4 票级细化）：`覆盖率 = 该票实际采集到的有效采样点数 / 当日该票应有采样点数`；**分母 = 该票实际可交易秒数 / 3s**（连续时段折算、午休剔除、15:00 后延续段不计入、**盘中停牌时段剔除**，见 §3.4）；
- **中途启动**：采集器于盘中启动 → 分母按全天应有数、分子按实际采集数（**禁止"启动过就算完整"**）；离线整段按缺口计入分母；
- **L1 就绪含根数核验（v0.4 拍板 A4）**：默认 L1 源（akshare 分钟）当日数据落地**且根数核验通过**才算就绪——当日应有分钟根数按源日历口径（240 根 ±1，9:30 根依源而定）；缺失率 ≤5%（可配，P1 与 L0 90% 门槛校准对齐）→ 就绪但标 degraded；缺失率 >5% → **该票 L1 不就绪 → 降 L2**（防缺段漏触发无标记）；
- **官方收盘价源当日绑定（v0.4 拍板 B5）**：官方收盘价按优先级链选定后**当日绑定**（official_close_source 落记录），换源次日生效——防跨源收盘价基准漂移（与单源同日精神一致）；
- 官方收盘价：来自日线权威源，当日收盘后即可得（**可得时点实测入 §11**；就绪检查并入结算触发，spec-01 §3.1）。**就绪窗口独立于回放序列（v0.5）：重试至当日 20:00（可配），与 spec-01 §3.1 第 4 项一致——宁可晚结、不可错结。**

### 4.2 收盘价对齐与偏差检测（#59）
- 引擎以官方日线源收盘价结算收盘档（spec-01 §3.3）；本规格提供 **close_candidates（延续样本）** 与官方收盘价**偏差检测**：偏差 > **0.3%**（阈值 v0.17.1 固化）→ 该票当日 `quality=degraded`，标记传播（结算/日报/信号注册表，见 §7）；
- 偏差检测取 close_candidates 中最贴近 15:00:00 的有效样本为 L0 收盘候选值。

## 5. L0 序列存储（数据表草案）

```
l0_ticks: id, symbol, trade_date, ts(精确到秒), price TEXT(Decimal),
          prev_close, pct_chg, cum_turnover TEXT, status, is_extended BOOL(默认0),
          source, quality
  -- 索引 (symbol, trade_date, ts)；主键 (symbol, trade_date, ts)
  -- is_extended=1 的样本不进判定序列（§4.1 series 截止 15:00:00），仅 close_candidates
l0_coverage: symbol, trade_date, expected_ticks, actual_ticks, started_ts, ended_ts,
          suspended_secs INT(盘中停牌剔除秒数，v0.4), coverage_rate TEXT(Decimal),
          quality, degraded_reason(source_fault|coverage_gap|cross_check_hit|...), notes
          -- 供日报与降级判定共用
source_binding: symbol, trade_date, source            -- 单源同日判定不变式
official_close_source: trade_date, source             -- 官方收盘价源当日绑定（v0.4）
采集清单: collector_watchlist(symbol, added_ts, reason, active)
```
- 数值以 TEXT 存 Decimal（与 spec-01 §0 数值精度约定一致）；
- L0 保留期：默认收盘结算后删除；保留 N>0 时 N 日自动清理任务（可延迟组）。

## 6. 分钟线与日线拉取（L1/L2 + 参考数据 + 初始回补）

- 分钟线：仅对"需要 L1 的票 + 当日"拉取（按票按需，不预拉全市场）；**默认源=akshare 分钟（v0.3），ftai 分时验证通过后升级为主源**（§2.3）；历史深度按候选源验证结果（补跑依赖）；
- 日线：增量更新（每交易日收盘后拉当日全市场日线或按需）；官方收盘价以"日线权威源"列为准（当日绑定见 §4.1）；
- **初始历史回补 backfill（v0.4 拍板 A6，P1 首启执行）**：
  | 范围 | 内容 | 依赖方 |
  |---|---|---|
  | 指数日线 | 沪深300 ≥250 交易日（环境标签分位基准） | spec-03 §8 |
  | 个股日线历史 | 持仓/候选票回看深度可配（默认 ≥60 交易日），全市场日线按需 | L2 补跑、卖出跟踪 10 日回看 |
| **试运行回放数据（v0.5 补）** | 试运行窗口（默认 5、至多 20 交易日）+ 指标回看期的**全市场日线区间**（一次性分批拉取——历史日选股需全市场可观测信息重构，非仅候选票） | spec-05 §6.2 信息集构造 |
  | 复权因子全历史 | adjust_factors 初始化灌装 | spec-01 §2.6/§4.2 |
  | 参考数据全量 | 交易日历/ST/板块/停牌/退市名单 | 全系统 |
  - 回补分批限速执行（走资源闸门 #37，按 §2.1 配额声明）；耗时与完整性实测入 §11；
- 参考数据（每日刷新）：交易日历、ST/退市名单、板块分类、停牌列表（含盘中中间态）、复权因子（刷新写入 spec-01 adjust_factors/suspensions/delistings 表）；
- 缓存策略：本地 SQLite 缓存 + 更新时间戳；增量更新；拉取记录（源/耗时/成败/数据量）写 performance_records（spec-02 §11）。

## 7. 数据正确性守护（§8.5 落地）

- **多源交叉抽检（v0.2 用户拍板：2-3 只/5 分钟）**：每交易时段每 5 分钟从活跃清单抽 **2-3 只**证券，用第二源对比 price/prev_close/cum_turnover；价差 >0.3% → 该源当日该票 `quality=degraded`、告警管理 Agent、入日报数据段；连续劣化 → 源健康度降权（§9）；
  - **抽检源独立性（v0.4 拍板 B2）**：维护**源→底层归属映射表**（source_family：东财系[akshare 东财接口/东财直连]、新浪系、腾讯系、ftai 独立等）——抽检第二源必须与绑定源**不同 family**（同底层抽检是假跨源）；无可用异族源时该轮抽检跳过并记录（不造假抽检）；
  - **命中后回填语义（v0.4 拍板 B4）**：价差超阈 → **l0_coverage.quality=degraded（票级）+ degraded_reason=cross_check_hit**；后续新样本行级标注，**已落库样本不逐行回填**——判定与日报以票级标记为准（避免大量 UPDATE，语义清晰）；
- **停牌旧值过滤**：拉取与采集前置停牌状态检查（ref_suspend，含盘中中间态 §3.4）——停牌票不入采集范围、其旧价不参与任何判定序列与估值（估值走 spec-01 §6.6 停牌估值规则）；
- **单源同日判定不变式**：实现为 source_binding 表 + 采集器/拉取器强制绑定检查（换源次日生效；采集侧当日不切源，§2.2/§3.3）；
- **口径一致性自检**：随机 N 笔成交（引擎提供）用官方日线 OHLC 核验区间（spec-01 §6.5 执行，数据侧提供权威 OHLC）；
- **除权跳变兜底（v0.4 拍板 C2）**：日线连续性检测——今开相对**原始昨收盘价**的变动幅度超出该板块涨跌停幅度可解释范围 → 中间必有除权或数据错误 → 核查 adjust_factors 是否有对应记录，无 → **疑似除权未入库：告警并阻塞该票当日相关结算**（人工确认补因子后重跑）；防复权因子源滞后导致静默算错；
- **交易日历校验（v0.4 拍板 C3，双向）**：日历为交易日但全源全天无数据 → 告警（疑临时休市、日历滞后）；非日历日出现新增交易数据 → 告警（疑调休补班未入库）；告警转管理 Agent 待办，人工确认后修正日历；
- **quality 语义（v0.4 拍板 B3：字典归口）**：**枚举字典归本 spec 定义基础集**——`ok | degraded(价差超阈/源劣化) | stale(延迟/缺窗口) | stale-price(停牌退市最近价结清) | approx(降级档结算)`；**引擎侧扩展标记**（spec-01 已用 `close_minute_fill`/`circuit_break`/`basis_requires_l0` 等）由 spec-01 登记使用，**全量字典唯一权威清单位置=P1 蓝图统一维护**，传播路径（结算日志、日报数据段、信号注册表、知识库统计）只允许使用字典内标记。

## 8. 环境标签客观规则（§16 #7；信号登记消费）

- **规则计算、非 LLM 判断**，输出 `env_bucket` 字符串供 spec-01 信号登记写入：
  - 维度 1 市场状态：`沪深300 收盘 vs 20 日均线` → up / down；
  - 维度 2 波动率：沪深300 近 20 日年化波动率分位（相对近 250 日，依赖 backfill §6）→ low / mid / high（33%/67% 分位桶）；
  - 桶组合 ≤ 2 维 × 3 桶（#55 约束）：如 `up_low`、`down_high`…；
  - **标签时点（v0.4 拍板 B6 文字修正）**：**当日盘中登记（买入成交即登记）用上一交易日的 env_cache 标签，此后不回填、不追溯改桶（v0.2 拍板）**——防统计不一致；
- 实现：独立计算模块，每日收盘后计算一次缓存（env_cache: trade_date, bucket, 计算输入快照），供次日登记与展示；桶边界/分位算法 P1 用真实数据校准后固化。

## 9. 健康度与故障切换

- 每源指标：成功率、延迟 P50/P95、当日降级票数、连续失败数 → 健康度分（A/B/C）；
- C 级连续 N 分钟 → 自动降权/切换（**按 §2.2 分层规则执行**：拉取类当日切换、采集类次日生效）；恢复探活成功 → 回权（冷却期可配）；
- 健康度是管理 Agent 审批"盘中档申请"的输入（总纲 §8.2），供其评估 L0/L1 可用性风险；同时供 spec-04 资源闸门参考。

## 10. 能力工具（引擎与 Agent 工具层共用）
```
get_market_snapshot()          // 全市场快照（选股扫描，§8.4 一次拉取→本地过滤）
get_quotes_batch(candidates)   // 候选批量明细
get_quotes(symbol, freq)       // 单票（工具聚合先行：返回统计摘要，明细按需——成本原则）
get_financials(symbol)
calc_indicator(...)            // 指标计算（前复权口径，#44）
```
- 工具输出遵循"数据不进 prompt"（§8.4：先统计聚合、结论进上下文、明细二次调用）。

## 11. 验证任务清单（§16 #19/#27，P1/P2 执行并固化回本文件）

| 任务 | 归口期 | 结果固化位置 |
|---|---|---|
| **ftai.chat：注册与额度条款、按次计费日成本测算** | P1 首批 | §2.2/§2.3/§2.5 |
| **ftai 分时 3 秒级/分钟级接口可用性实测（限流/封禁风险）**——通过后升级为 L1 主源与采集源 | P1 首批 | §2.3/§2.5/§3 |
| **东财直连分钟线候选验证**——通过后列入 L1 备源 | P1 首批 | §2.3 |
| **akshare 当日 1 分钟线可得时点与完整根数实测（v0.4 补，A4/A7）**——L1 就绪驱动结算的时点依据 | P1 首批 | §4.1/§6 |
| **官方日线源收盘价可得时点实测（v0.4 补，A7）**——结算最早点（15:35）触发依赖 | P1 首批 | §4.1 |
| ftai 分钟线历史深度（补跑依赖） | P1 | §6 |
| ftai 盘口五档/ST/板块字段存在性 | P1 | §2.1 |
| akshare 最小集逐项可用性 + 直连兜底映射（日线/参考数据角色） | P1-P2 | §2.2 |
| **3 秒频率限流实测（采集批量接口）**——当前默认采集实现=东财直连批量（ulist，v0.5 载体修正）+ ftai（升级前置） | P1 | §3.3/§2.5 |
| 收盘价偏差 0.3% 实际分布实测（校准阈值） | P1 | §4.2 |
| L0 覆盖率口径与中途启动场景验证（含盘中停牌分母剔除） | P1 | §4.1/§3.4 |
| L1 根数核验阈值校准（缺失率 5% 与 L0 90% 门槛对齐） | P1 | §4.1 |
| **backfill 耗时与完整性实测（v0.4 补，A6）** | P1 | §6 |
| 环境标签桶边界校准（分位算法） | P1 | §8 |
| **长期结论决策点**：akshare 分钟长期健康度监控——若持续劣化且免费候选均不达标 → L1 仅收费源 | P1 末 | §2.3/§2.4 |

## 12. 测试计划

- 采集器：节流/退避/**当日不切源（仅退避）**/多源轮换次日生效/午休停牌跳过/**盘中临时停牌剔除与分母扣除**/延续采样/**is_extended 隔离（不进 series）**/进程在线保障验收（交易时段闲置 40 分钟不掉采集）；
- 正确性：交叉抽检触发、**抽检源异族校验**、停牌旧值拦截、单源同日不变式（换源次日生效）、quality 传播路径、**票级 degraded 不回填语义**、**除权跳变检测（今日开 vs 原始昨收超涨跌停幅 → 无因子告警阻塞）**、**日历双向校验**；
- 覆盖率：89.9%/90.0% 边界（Decimal）、中途启动、离线缺口、**盘中停牌剔除后的分母**；
- 就绪判定：L0/L1/L2 按票矩阵与超窗兜底、**L1 根数核验（缺失 5% 边界）**、**官方收盘价源当日绑定**；
- **backfill：初始灌装完整性（指数 250 日/复权因子全历史/参考数据）与分批限速**；
- 参考数据刷新：复权因子/停牌列表/日历增量正确性；
- 环境标签：桶边界用例与分位计算单测、**盘中登记用昨日标签不回填**。

## 13. §16 勾销表（本文件状态）

| # | 事项 | 状态 |
|---|---|---|
| 7 | 环境标签客观规则（大盘 20 日线/波动率分位等，规则计算非 LLM） | 已写入 §8（spec-03/05 中本规格侧；统计消费在 spec-05；依赖 backfill 指数 250 日） |
| 19 | 免费源能力验证清单（批量快照 3 秒频率/分钟线深度/日历/日线/复权/ST 板块 + 直连兜底映射） | 已写入 §2.1/§11（P1-P2 执行；v0.4 补 akshare 分钟可得时点/官方收盘价时点/backfill 实测） |
| 24 | EOD-Replay 回放执行细则数据侧（L0 完整率/三级精度供给/采集范围/偏差阈值） | 已写入 §4/§5（引擎判定侧在 spec-01；v0.4 补延续样本隔离与 close_candidates） |
| 25 | 3 秒采集器细则（节流退避/多源轮换/午休停牌跳过/清理/离线降级联动/共用行情流/延续采样/进程在线保障） | 已写入 §3/§5（v0.4 补采集源默认与升级、当日不切源、盘中临时停牌、绑定粒度与清单时点；v0.6 防睡眠改进程在线保障） |
| 27 | ftai.chat 接入与验证 | 已写入 §2.1/§11（验证后置顶；验证前 L0/L1 默认 akshare） |
| 29 | 收盘价偏差阈值与标记传播（数据侧） | 已写入 §4.2/§7（引擎衔接在 spec-01；v0.4 补 close_candidates 与收盘价源当日绑定） |
| 30 | L0 完整率细则（分母=实际交易秒数、中途启动、离线缺口、落库结构） | 已写入 §4.1/§5（v0.4 细化：分母=该票实际可交易秒数，盘中停牌时段剔除，suspended_secs 落库审计） |

## 14. 评审确认记录

### 第一轮（2026-09-05 用户拍板，v0.2/v0.3 落实）

1. **买一价**：默认不采（确认原稿）——开板/封板事件判定在 L0 下用价格序列判定即可；盘口类条件单待盘口源验证后另行启用（§3.2）；
2. **交叉抽检**：每 5 分钟抽 **2-3 只**（§7）；
3. **L1 分钟源策略（v0.2 拍板门禁 → v0.3 用户二次拍板反转）**：**空窗期以 akshare 分钟为 L1 主源（默认可用、无需等待验证）**；ftai 分时验证通过后升级主源、东财直连分钟通过后列备源；akshare 任意阶段保留为最后兜底；劣化自动切换；长期劣化且免费候选均不达标才 L1 仅收费源（§2.3/§2.4/§4.1/§11）；
4. **环境标签不回填**：当日盘中登记用上一交易日标签，结算后不回填改桶（确认，防统计不一致，§8）；
5. **观察集合**：v0.1 即支持管理 Agent 配置常驻观察集合（默认空、上限可配 ≤20 只、次日生效），服务市场异动探索源 #32 输入（§3.1）。

### 第二轮（2026-09-05 用户拍板，v0.4 落实）

1. **A1 L0 采集源**：验证前默认 **akshare 批量快照**，ftai 限流实测通过后升级（与 L1 对称，§2.5/§11）；
2. **A2 切换规则分层**：拉取类当日自动切换、采集类当日只退避不切源（次日生效）（§2.2/§3.3/§9）；
3. **A3+A4+A5 数据核验三件套**：延续样本 is_extended 标记不进判定序列（close_candidates 单列，§3.2/§4.1/§5.1）；L1 就绪含 240 根核验（缺失 ≤5% 标 degraded、>5% 降 L2，§4.1）；盘中临时停牌剔除+分母=该票实际可交易秒数对齐 spec-01（suspended_secs 落库，§3.4/§4.1）；
4. **A6 backfill**：初始历史灌装范围（指数 250 日/个股日线历史 ≥60 交易日/复权因子全历史/参考数据全量）写入 §6，验证入 §11；
5. **A7 验证清单补**：akshare 当日分钟可得时点与根数、官方收盘价可得时点两项实测入 §11；
6. **B 类口径 6 项**：prev_close=除权调整后昨收基准（§3.2）；抽检源异族校验（source_family 映射，§7）；quality 字典归 spec-03 定义基础集、引擎扩展登记、P1 蓝图统一维护（§7）；抽检命中票级 degraded 不回填（§7）；官方收盘价源当日绑定（§4.1/§5.1）；§8 标签时点文字修正（不回填）；
7. **C 类**：能力标签加频率/并发/每日额度配额声明（§2.1，供 spec-04 资源闸门）；除权跳变兜底（开盘连续性检测→阻塞告警，§7）；交易日历双向校验（§7）；绑定粒度=当日主源全票同源+故障代价写明（§3.3）；采集清单计算时点 9:25 前（§3.1）；测试计划补 5 项（§12）。

### 第三轮（2026-09-06，夜间优化轮·用户追认后定稿，落实于 v0.5）

1. **§2.5 L0 采集实现载体技术修正**：默认采集实现=东财 push2 直连批量接口（realtime_batch(symbols) 语义的天然载体）；akshare 聚合全市场快照接口不适用 3 秒轮询（单请求全市场、重量级、高限流风险），仅用于 snapshot_all 选股扫描——**免费源默认可用策略与升级路径不变**，策略层无反转；
2. **§6 backfill 补试运行回放数据范围**：试运行窗口+指标回看期的全市场日线区间（spec-05 §6.2 信息集重构依赖，此前 backfill 只覆盖持仓/候选票）；
3. **§2.1 预留扩展接口注**（龙虎榜/北向/两融/资金流/舆情）——消除与总纲 §8.1 数据分类的口径缺口；
4. **§4.1/§11**：官方收盘价独立就绪窗口（至 20:00）与 spec-01 §3.1 同步；限流实测对象随载体修正更新；
5. **用户追认（2026-09-06 上午）**：L0 采集载体修正（默认实现=东财直连批量）作为四项拍板之一正式确认——v0.5 定稿。

**剩余待实测项（P1 执行，固化回对应章节）**：§11 验证清单 15 项（ftai/直连分钟/akshare 时点与根数/官方收盘价时点/限流/阈值校准/覆盖率/L1 根数/backfill/环境桶等）；L1 空窗期对 P1 验收的影响：P1 验收以 L0 回放 + L2 降级路径为准，L1 不作为 P1 验收前提。

**Web 部署版适配（v0.6，随总纲 v0.20，待用户终审）**：§3.5/§12/§13 中"防睡眠（Windows SetThreadExecutionState / Tauri 电源插件）"表述已改写为 **core 进程级交易时段在线保障（systemd 常驻保活）**，验收口径不变；采集/拉取/回补/就绪/守护机制与决策无变更。

## 变更记录

| 版本 | 日期 | 状态 | 要点 |
|---|---|---|---|
| v0.1 | 2026-09-05 | 草稿待评审 | 初稿：适配器与优先级链、3 秒采集器细则（按票范围/批量字段含累计成交额/节流退避/单源同日不变式/午休停牌/防睡眠/延续采样）、L0 落盘与按票就绪接口（覆盖口径 #56）、收盘价对齐与 0.3% 偏差、数据正确性守护四机制、环境标签规则（2 维×3 桶）、健康度与切换、能力工具、验证任务清单、勾销 #7/#19/#24(数据侧)/#25/#27/#29(数据侧)/#30；待评审项 5 条 |
| v0.2 | 2026-09-05 | 第一轮评审修订 | ①新增 §2.3 **L1 分钟源验证门禁**：akshare 不作 L1 主源，候选=ftai 分时→东财直连分钟→收费源，空窗期 L0→L2 跳级标注，验证为 P1 首批，全部不达标→L1 仅收费源（§2.3/§4.1/§6/§11）②交叉抽检改 2-3 只/5 分钟（§7）③观察集合 v0.1 即支持（默认空、≤20 只上限、次日生效、#32 输入）（§3.1）④买一价不采确认、环境标签不回填确认（§14）⑤§14 改评审确认记录+验证清单 11 项，注明 L1 非 P1 验收前提 |
| v0.3 | 2026-09-05 | L1 源策略二次拍板（用户反转） | ①§2.3 门禁改"**默认可用+升级策略**"：akshare 分钟=默认 L1 主源（空窗期即启用，无验证前提）；ftai 分时验证通过→升级主源、东财直连验证通过→备源；akshare 任意阶段保留为最后兜底；劣化自动切换；长期劣化才评估 L1 仅收费源②§4.1/§6/§11 同步（验证任务不再卡 L1 可用性，仅"升级替换"触发）③§14 第 3 条记录反转 |
| v0.4 | 2026-09-05 | 第二轮评审修订（ZCode 深度评审） | ①**§2.5 L0 采集源默认可用+升级**（验证前 akshare 批量快照，ftai 限流实测通过后升级，与 L1 对称）②**§2.2 切换分层**：拉取类当日自动切换、采集类当日只退避不切源（消除与 §3.3 矛盾）③延续样本 is_extended 隔离、get_replay_series 增 close_candidates（A3）④L1 就绪含 240 根核验（缺失 ≤5% degraded、>5% 降 L2）（A4）⑤盘中临时停牌剔除+分母=该票实际可交易秒数+suspended_secs 落库（A5）⑥§6 初始历史回补 backfill（指数 250 日/日线历史/复权因子全历史/参考数据）+§11 实测（A6）⑦§11 补 akshare 分钟可得时点/根数与官方收盘价可得时点实测（A7）⑧prev_close=除权调整后昨收基准（B1）⑨抽检源异族校验 source_family（B2）⑩quality 字典归口 spec-03+P1 蓝图统一维护（B3）⑪票级 degraded 不回填（B4）⑫官方收盘价源当日绑定（B5）⑬§8 标签时点文字修正（B6）⑭能力标签加频率/并发/每日额度配额声明（C1，供 #37 资源闸门）⑮除权跳变兜底阻塞告警（C2）⑯交易日历双向校验（C3）⑰绑定粒度=当日主源全票同源+故障代价（C4）⑱采集清单 9:25 前计算（C6）⑲测试计划补 5 项；勾销表 7 项状态注更新 |
| **v0.5** | 2026-09-06 | 夜间优化轮（**用户追认定稿**） | ①§2.5 L0 采集实现载体技术修正（默认=东财直连批量接口；akshare 聚合接口不适用 3 秒轮询——免费源默认策略不变，仅载体修正）②§6 backfill 补**试运行回放全市场日线**数据范围③§2.1 预留扩展接口注（对齐总纲 §8.1）④§4.1 官方收盘价独立就绪窗口（至 20:00）与 spec-01 §3.1 同步⑤§11 限流实测对象更新 |
| **v0.6** | 2026-09-06 | Web 部署版适配（**随总纲 v0.20 提交，待用户终审**；机制同 v0.5，无新增行为决策） | ①§3.5 **交易时段在线保障（#56）改写**：Windows SetThreadExecutionState / Tauri 电源插件防睡眠 → **core 进程级在线保障（systemd 常驻保活，无休眠语义）**，P1 验收口径不变（交易时段闲置 40 分钟不掉采集）②§3.5 进程守护（#42）由桌面层 → **systemd**（Restart=always，拉起后按中途启动口径补完整率）③§12 测试计划/§13 勾销表 #25"防睡眠"改进程在线保障 ④依据与交叉引用版本同步（总纲 v0.20；spec-01 v0.7/spec-02 v0.5/spec-05 v0.6） |
