# spec-05 能力·知识库·策略优化规格 v0.6（Web 部署版适配：仅引用同步）

> 版本：v0.6（2026-09-06，Web 部署版适配轮·随总纲 v0.20 提交，待用户终审：本文件无桌面承载触点，仅做**交叉引用版本同步**——依据与联动引用改总纲 v0.20 / spec-01 v0.7 / spec-02 v0.5 / spec-03 v0.6 / spec-04 v0.7 / spec-06 v0.5；能力/知识库/策略优化/试运行验收等业务机制与决策不变，v0.5 内容全保留）
> **状态：⏳ Web 部署版适配稿（随总纲 v0.20 一并以 Web 部署版 P1 基线提交，待用户终审定稿）——定稿后 v0.6 替代 v0.5 成为 Web 部署版开发基线（业务同 v0.5）。**
> 依据：总纲 v0.20（Web 部署版，业务决策承 v0.19：#62 满仓策略、#63 验证与试运行独立账户）§1.2（知识库/拦截追踪/信号级前瞻追踪定义）、§3.2（管理 Agent 职责·知识库维护）、§4.1-4.3（新建 Agent 状态机/能力评估/能力三来源）、§5（能力配置中心）、§6（大纲）、§7.6（卖出跟踪与信号注册）、§12.8 决策表；联动 **spec-01 v0.7（§2.5/§2.7 版本标记字段链路：strategy_version_no+trial_flag、§8 信号登记字段）**、spec-02 v0.5（§9 策略版本化存储与 checkpoint/activate/rollback API、memory_entries 演进记录、§8 跨 Agent 授权检索、context_templates 参考块）、spec-03 v0.6（§8 env_bucket 环境标签、§3.1 观察集合、backfill 日线历史）、**spec-04 v0.7（§2.1 任务类型与生命周期事件触发、§2.5 可延迟组、§4.1 launch 上线确认单、§5.5 体检报告）**、spec-06 v0.5（知识库页/能力市场/标注渲染 UI）
> **受约束决策**：#10（知识库模块） #14（知识库权限） #21（生命周期·策略双层） #24（概念评审/有效性） #25（EVOQUANT 优化范式） #26（参考非指令/防循环/验证预算） #27（工作流分层） #30（信号级证据流） #31（版本化与归档回流，v0.17.6 记账口径） #32（探索与用户回路） #40（探索配额与受控愚蠢） #55（环境切片约束） #57（试运行精度差） #9/#18（卖出跟踪引擎侧）
> **§16 勾销**：#6 #7 #8 #9（流程侧） #10 #11 #13 #14 #15 #16（spec-05 侧） #18 #21 #28（spec-05 侧） #34（见文末勾销表）
> 与总纲冲突时以总纲为准并同步本文件。

---

## 1. 模块定位与边界

- **职责**：能力配置中心（能力注册/版本/绑定/沙箱上架门槛/申请-下发闭环）、知识库（正向概念库+反向避坑库：条目模型/状态机/客观证据统计/**概念标签治理**/双闸评审/"参考非指令"落地/选考排序/防循环/市场异动探索源）、策略优化（EVOQUANT 范式、分仓验证机制与**记账模型声明**、版本晋升/回滚流程——**流程侧**，存储侧在 spec-02 §9、引擎记账在 spec-01 §2.7）、归档经验提取、试运行验收（三层标准+历史回放信息集构造+精度差标注）、管理大纲存储与版本（charter_versions）、工作流分层（强制骨架/执行层自主）、健康度指标供给（体检报告③）。
- **不负责**：信号注册与 N 日结算执行（spec-01 §8——本规格只做**统计消费**）；策略版本存储 schema 与回滚 API（spec-02 §9——本规格是**流程调用方**）；引擎侧版本记账实现（spec-01 v0.7 §2.7——字段链路保留为记录，本规格声明独立验证账户模型与消费口径）；环境标签计算（spec-03 §8——本规格消费 env_bucket）；审批流机制与待办管理（spec-04 §4——本规格提供能力/任务申请的**业务规则**）；知识库 UI 展示与标注渲染（spec-06）；记忆存储（spec-02）。
- **消费方**：spec-01（信号注册表统计读取、strategy_version_no/trial_flag 分组消费）、spec-02（strategy_versions API 调用、演进记忆写入、知识库证据流读取演进记忆、跨 Agent 授权检索）、spec-03（env_bucket 消费、观察集合配置写入、日线历史消费）、spec-04（能力申请业务规则、体检报告②概念验证进度与③健康度供给、归档事件触发、验证评估/经验提取/kb_stats 刷新/试运行回放任务调度）、spec-06（知识库页/能力市场/"参考 KB-xxx（状态）"标注）。
- **信任边界**：#33——知识库统计聚合为**管理 Agent 授权路径**（spec-02 §8 跨 Agent 查询），子 Agent 只能读下发/可查条目，无统计权限；全部写操作（入库/状态变更/能力绑定/大纲更新）审计留痕。

## 2. 能力配置中心（§5/§4.3 落地）

### 2.1 能力模型与 schema

```
capabilities: id(ULID), name, type(skill|tool|mcp|datasource), version,
  description, maintainer(manager), source_type(opensource|api|selfmade),
  source_ref(开源:仓库+License | API:服务商+费率 | 自研:作者),
  sandbox_status(pending|passed|failed), sandbox_report_ref,
  metadata JSON(输入输出/依赖/权限需求), status(active|deprecated),
  created_ts, updated_ts
capability_bindings: id, capability_id, version, agent_id,
  bound_by(manager), bound_ts, unbound_ts(解绑/回滚留痕), created_ts
```

- 能力四类（§5.1）：Skill（SKILL.md 指令式，可含脚本/模板）/ Tool（函数级）/ MCP 服务器 / 数据源订阅。

### 2.2 三种获取来源与上架四门槛（§4.3）

- 来源：①全网检索开源（GitHub/量化社区→代码评审+沙箱验证→封装 Skill/工具）②第三方 API（评估接入→MCP 服务器或数据源适配器）③自研编写 SKILL.md；
- **上架四门槛（齐备才可下发）**：①元数据完整（名称/描述/版本/维护者/用途）②沙箱测试通过（行为符合预期、无越权与恶意脚本——开源引入必测）③许可与来源留痕 ④绑定记录；
- **沙箱执行（v0.2 确认，分阶段）**：**P1=静态检查**（危险调用扫描：网络访问/文件删除/执行外部命令）**+ 管理 Agent 人工评审**；**P2=自动沙箱**（受限子进程：禁网/文件系统白名单/超时/资源限额）——P2 上线前 P1 流程为上架唯一通道。

### 2.3 能力申请-下发闭环（§5.2）

子 Agent 提交能力申请（用途/期望输入输出/优先级）→ **spec-04 审批流（type=capability）**，业务规则=必要性 / **现有能力覆盖检查**（已具备等价能力→提示指路不重复注册，§5.3）/ 合规风险 → 通过则按 §2.2 获取 → 沙箱验证 → 注册（v1.0）→ 绑定目标子 Agent → 生效通知；驳回附理由可申诉。

### 2.4 版本与兼容（§5.3）

- 每能力独立版本号；更新生成新版本，子 Agent 可保留旧版本引用；
- 绑定清单（能力 ID+版本）可查可回滚（unbound_ts 留痕）；能力废弃标 deprecated 不再下发，存量绑定通知迁移。

## 3. 知识库（Knowledge Base，#10/#14/#24/#26/#30/#55/#32）

### 3.1 条目模型

```
kb_entries: id(KB-0001 递增), type(positive|pitfall), name,
  description(概念/陷阱形态描述), computable_spec JSON(事前可计算规格：
  触发识别条件/计算口径/所需数据——pitfall 必填，positive 选股类必填),
  severity(pitfall 用: high|mid|low), env_scope(env_bucket 或 all),
  status(observing|validating|valid|invalid|sealed),
  invalid_reason, sealed_reason(证据不足封存),
  source(user|retrospective|manager_observation|market_anomaly),
  created_by(manager|user), origin_agent(发现来源 Agent,可空),
  review_gate1_ref(规则校验结果), review_gate2_ref(管理 Agent 评审记录),
  created_ts, updated_ts
kb_stats（统计快照表，按条目×环境桶）: kb_id, env_bucket, sample_n, win_rate,
  avg_win, avg_loss, expectancy(期望值,含费), intercept_n(被拦截), exception_n(破例),
  stale_n(stale-price 样本), dispatch_n(下发/引用计数——UCB n_i，v0.3 补),
  window_days, updated_ts
kb_tag_aliases（标签别名映射，v0.3 新增）: alias(自由标签), canonical_kb_id,
  merged_by(manager), merged_ts, reason   -- 概念归并重映射（§3.11）
```

- 两类条目（§1.2）：**正向概念库**（有效的选股概念/方法论，服务新策略创建与子 Agent 优化）；**反向避坑库**（系统性陷阱与亏损模式，命中候选必须回应）。

### 3.2 状态机与客观证据源（#14/#30；v0.3 补试运行隔离与外推公式）

```
观察 observing → 验证 validating → 有效 valid
                    ├→ 失效 invalid（含 invalid_reason）
                    └→ 封存 sealed（证据不足，#26 到期结论）
失效/封存 → 可经新证据或管理 Agent 复核重新进入 validating
```

- **证据源=信号注册表客观统计（#30 核心切换）**：状态晋升/降级**只由** signal_registry 聚合统计驱动（spec-01 登记与 N 日结算），杜绝"子 Agent 自报"；子 Agent 复盘上报仅触发**入库申请**（观察态），不直接写状态；
- **试运行样本隔离（v0.3 拍板 A3）**：试运行（历史回放）期登记的信号带 `trial_flag=1`（spec-01 v0.7 §2.5）——**默认排除于状态机证据统计**（L2 近似撮合样本不参与晋升判定，可展示参考不作证据），防试运行样本污染；
- 统计为**管理 Agent 授权路径**的跨 Agent 聚合（spec-02 §8），归档 Agent 样本继续参与（防样本沉底）；
- **环境切片（#55）**：按 (concept_tag × env_bucket) **单桶独立统计，不跨桶合并**；晋升状态仅对应环境桶成立（valid 标注适用桶）；换环境=新验证周期、进度不继承；
- **"当前环境桶预计可验证时间"外推公式（v0.3 补，B3）**：`预计交易日 = (30 − 当前桶 n) ÷ max(近 30 日该桶入信号频率, ε)`——freq=0 时展示"暂无入信号，无法外推"；**spec-04 §5.5② 体检报告数据接口**。

### 3.3 有效性统计口径（#24/#34/#6；v0.2 确认门槛包；v0.3 补重算触发）

- 指标：样本数 n、胜率、均盈、均亏、**期望值 = 胜率×均盈 − 败率×均亏（含费）**；数据取 signal_registry.fwd_return_pct（spec-01 N 日结算，默认 N=10）；
- **stale-price 样本计入并标注**（#34：停牌/退市按最近可得收盘价结清的样本不剔除，stale_n 单列——防静默丢样本致统计偏乐观）；
- **最小样本门槛（v0.2 确认）**：单桶 **n≥30** 才允许晋升 valid；不足 → "证据不足"，小样本高胜率**不得晋升**（#24）；
- **滚动失效监控（#24，v0.2 确认）**：**滚动 60 交易日**窗口期望值 <0 → 失效候选，管理 Agent 确认后降级 invalid（含 invalid_reason）；
- **统计重算触发（v0.3 补，C 类）**：**日结算完成后确定性重算**（零 token，紧随结算回调），兜底入 spec-04 v0.7 §2.5 可延迟组（task_type=kb_stats 刷新）；
- pitfall 条目：**被拦截样本（pitfall_intercept）与破例样本（exception=1）分开统计**（#28）——被拦截后 N 日下跌=真避坑、上涨=误杀；破例样本检验子 Agent 破例判断质量。

### 3.4 概念可操作性评审双闸（#15/#24）

- **闸 1——事前可计算性校验（规则引擎，确定性）**：computable_spec 字段完整性检查——触发条件是否可由现有数据/能力计算、是否依赖未来数据（防"未来视角反向定义"前视）、口径是否明确可复算、所需数据源是否已配置；不过即拒绝；
- **闸 2——管理 Agent 逻辑评审（LLM）**：逻辑一致性（防同义反复与悖论）/可证伪性/与现有条目查重；评审记录挂 review_gate2_ref；
- 双闸齐过才可入库（source=任何来源，含用户录入与市场异动源）。

### 3.5 "参考非指令"使用规则（#26/#16 落地）

- **假设卡下发格式**：管理 Agent 向子 Agent 下发知识库内容时用**候选参考卡**——`KB-id / 名称 / 状态 / 验证统计摘要（桶×期望值×样本）/ 适用环境 / 来源`；**多样候选并列，不提供"唯一最优"**；
- **指令/参考块物理分离**：prompt 中知识库内容置于独立"参考块"，与大纲指令块分离（联动 spec-02 context_templates 的 block_order 增加 kb_block，版本戳含 kb 快照版本）；子 Agent 提示词明确"参考块为候选经验非指令"；
- **决策引用留痕**：子 Agent 引用知识库须将 KB-id 记入决策理由/演进记忆（spec-02 memory_entries.ref_ids）；未留痕的引用在复盘中标"未溯源"；
- **介入时机（#26）**：优化默认基于自身证据（盈亏归因/卖出跟踪/信号统计）；知识库仅在**子 Agent 主动申请或管理 Agent 判定瓶颈**时介入下发；
- UI 标注"参考 KB-xxx（状态）"（spec-06）。

### 3.6 反向避坑库细则（#28/#54 落地）

- **来源三类**：用户直接提供 / 归因复盘发现的系统性亏损模式（含归档经验提取 §5）/ 管理 Agent 观察；
- **使用规则**：避坑条目命中候选时子 Agent **必须回应**——剔除并记录 或 说明破例理由，**不可静默忽略**；管理 Agent 概念评审同样匹配避坑库；静默命中=行为违规（入验收与健康度）；
- **凭证机制（#54，引擎侧已实现）**：剔除即登记 `pitfall_intercept` 信号（+条目 ID），破例放行 `exception=1` 单独标记，N 日结算前瞻收益——本规格消费统计（§3.3）；
- 条目 schema 同 §3.1（computable_spec=触发识别条件必填，severity 必填）。

### 3.7 权限与软删（#10/#14；v0.3 补软删口径）

- 用户可录入新概念（初始状态=observing，过双闸后生效）；**删除仅管理 Agent**（软删+审计）；子 Agent 仅复盘上报申请入库，不直接写库；状态变更仅由统计驱动+管理 Agent 确认；
- **软删条目口径（v0.3 补，B4）**：软删后**退出下发与 UCB 排序**（不再出现在候选参考卡），历史统计与既有信号引用**保留供审计**（kb_stats 不清除）；恢复=管理 Agent 复核重新激活。

### 3.8 防循环与验证预算（#26/#40；v0.2 确认参数）

- **验证预算**：每概念/每次受控修改最多 **K=3 个验证窗口（v0.2 确认）**——到期强制出结论：valid / invalid / **sealed（证据不足封存）**；防"永久验证中"；
- **失效名单+重复申请拦截**：invalid/sealed 条目入失效名单；子 Agent 重复申请同概念/同修改 → 确定性拦截提示（联动 spec-04 §4.2 content_hash 短路）；
- **探索额度（#26/#40，v0.2 确认）**：**每 20 交易日**每子 Agent ≥1 个探索概念申请额度；每月每子 Agent 信号中"知识库未覆盖新概念"占比建议 **10-20%（软约束，不达标管理 Agent 引导非强制）**——占比统计口径=自由标签信号占比（§3.11）；
- **受控愚蠢许可（#40）**：沙盒/试运行允许反常识策略，配验证预算、到期自动归档。

### 3.9 选考排序（#13；v0.2 确认；v0.3 补 n_i 载体）

- **UCB1 参考实现（v0.2 确认）**：`score = expectancy + c·√(ln N_total / n_i)`——expectancy=该条目（适用环境桶）期望值（含费），**n_i=kb_stats.dispatch_n（该条目下发/引用计数，v0.3 补载体——每次进入候选参考卡/被查询返回即 +1）**，N_total=全部下发次数，**c 默认 1.0 可配**；
- 排序用于两个场景：管理 Agent 组装候选参考卡（§3.5）时的条目挑选、子 Agent 主动查询时的返回排序；
- 探索压力由额度（§3.8）与异动源（§3.10）承担，**不另设 epsilon 随机项（v0.2 确认）**。

### 3.10 市场异动探索源（#11/#32）

- 输入：spec-03 全市场快照/日线（周涨幅数据）+ 观察集合（spec-03 §3.1）；
- 流程：每周五收盘后可延迟任务（spec-04 §2.5）→ 计算周涨幅异动板块（阈值可配）→ 提取板块共同特征（LLM）→ 生成观察态候选概念 → **双闸评审（§3.4）** → 入库 kb_entries（source=market_anomaly，observing）；
- 与子 Agent 探索额度联动：新候选概念可供子 Agent 申请探索（#32 数据驱动探索）。

### 3.11 概念标签治理（v0.3 新增，拍板 A2——统计根基）

- **风险**：concept_tag 若为自由文本，同义变体（"低吸/低位买入/回调低吸"）使统计碎片化——单桶 n≥30 永远凑不齐，知识库永久停在"证据不足"（#55 维度灾难在标签维度的重演）；
- **治理三件套**：
  1. **受控词表优先**：信号登记时 concept_tag **优先确定性匹配现有 kb_entries.name**（词表校验经执行器，spec-01 §8 登记联动）——命中即用规范名；
  2. **自由标签=探索**：未覆盖新概念允许自由标签（即 #40 探索占比 10-20% 的统计口径，§3.8）；
  3. **月度归并窗口（v0.3 拍板 C 类时点）**：**每月首个周末可延迟组**（与每周五异动源错开）——LLM 提议同义合并（自由标签→规范条目）→ 管理 Agent 确认 → 写 kb_tag_aliases 映射（append-only）；统计按 canonical 归并（**不回写改写历史信号行**，映射表口径统一换算——保 append-only 与可追溯）；
- 归并后 kb_stats 按映射重算；未归并的自由标签独立统计（作探索观察，不入状态机晋升证据）。

## 4. 策略优化（#25/#31/#8/#9 流程侧）

### 4.1 EVOQUANT 范式流程（#25；v0.3 补语义受控执行机制）

```
①诊断：引用盈亏归因/卖出跟踪结果/信号统计证据（先诊断后修改——
        无证据引用的修改提案直接拒绝）
②受控修改：语义受控——核心理念锁定（变更需用户授权，双层结构），
        仅执行层参数可改；禁凭空改写（修改 diff 必须可追溯到诊断证据）
③验证：分仓验证期（§4.2，checkpoint 后小仓位运行）
④晋升/回滚：验证通过晋升全仓 / 失败自动回滚+失败原因入记忆
```

- **语义受控执行机制（v0.3 补，B1）**：**执行层参数字段白名单**——config JSON 允许修改的字段表由大纲执行层节映射固化（白名单外字段出现在 diff 中即拒绝，含核心理念字段）；核心理念文本**不入 config**（存 charter_versions），checkpoint 提交时校验理念哈希未变（变了=需用户授权的另一流程）；
- **评估计入成本（#25）**：优化任务（诊断/验证评估）为 llm-heavy 资源类（spec-04 §7.2 闸门），成本入 performance_records。

### 4.2 独立验证账户机制（v0.5 重写：总纲 #63——原账户内分仓方案 A 废弃）

| 项 | 确认值（v0.2） |
|---|---|
| 验证窗口 | **10 个交易日 或 ≥20 笔新信号**（先到为准） |
| 仓位 | **满仓运行（#62）**：验证账户与主账户同范式——买入即全部可用资金，无仓位上限约束（单票上限参数默认 100%） |
| 通过判定 | 窗口内**零规则级违规** ∧ 验证期期望值（含费）≥ 现役版本同期 或 >0 |
| 失败判定 | 触发风控熔断 ∨ 窗口期望值（含费）< −2% → **自动回滚** |

- **v0.5 机制（独立验证账户）**：验证新版本参数时创建**独立验证账户**（spec-01 v0.7 迁移 005：accounts.role=validation、parent_agent_id=主 Agent、同初始资金 10 万、status=trial、满仓运行 #62）——与主账户**同期、同市场、独立结算**；
  - **参数**：验证账户的大纲按新版本 config 全量执行（checkpoint 的 config 快照）；主账户保持现役版本不动；
  - **通过判定**（参数同 v0.2 包，口径改跨账户）：窗口内零规则级违规 ∧ 验证账户期望值（含费）≥ 主账户同期 或 >0；窗口=10 交易日或 ≥20 笔新信号先到为准；
  - **通过 → 切换**：主账户 activate 新版本（spec-02 §9 API，参数切换）+ 验证账户整体归档留证（trial_archive）；
  - **失败 → 归档**：主账户保持现役 + 验证账户归档 + 失败原因入记忆（防重复 #25）；
  - **满仓语义（#62）**：验证账户同样满仓运行——验证的是"满仓下的择时择股能力"，无仓位分散可依赖；
  - **信号/版本追溯**：strategy_version_no 保留为记录字段（spec-01 v0.7）；trial_flag 隔离保留；
- 回滚调用 spec-02 §9 rollback API（主账户参数回退至最近 validated 版本；验证账户归档）；
- 失败版本标 failure_reason 并**连同拒绝原因入记忆**（防重复，#25）；
- 全部阈值（窗口/判定线）入 config 可调，P1 试运行期按真实数据校准。

### 4.3 版本流程（#9 流程侧，存储侧 spec-02 §9）

- 修改提案确认 → **checkpoint(agent, config, basis)**（config 全量快照+诊断证据引用；语义受控校验 §4.1）→ trial_window 参数写入（仓位上限/窗口长度）→ 验证期运行（新旧版本并行：现役全仓+验证分仓，§4.2 记账）→ 窗口结束判定（§4.2）→ 通过 **activate(version_no)** 晋升全仓 / 失败 **rollback(agent, reason)**；
- 演进记录（memory_entries type=strategy）逐条挂 version_no（spec-02 §9 不变量）；
- checkpoint/activate/rollback 幂等与全审计由 spec-02 API 保证；本规格负责判定与调用时点（**验证窗口到期由调度器"验证评估"任务触发**——checkpoint 时注册，spec-04 v0.7 §2.1 生命周期事件联动）。

### 4.4 演进记录与失败记忆（#25）

- 每次优化记录：修改前/后/依据（证据引用）/预期/结果——全量入 memory_entries（spec-02 §3.1）；
- **失败修改连同拒绝原因入记忆防重复**；重复提案触发 spec-04 §4.2 短路提示"该修改已被验证失败"。

## 5. 归档经验提取（#10/#31）

- **触发**：Agent 进入归档/退休（生命周期终态，总纲 §3.5）——**spec-04 v0.7 §2.1 生命周期事件即时生成"经验提取"任务（可延迟组）**；
- **流程**：
  1. **终局统计（确定性，零 token）**：该 Agent 全部信号按 (concept_tag × env_bucket) 分桶统计（§3.3 口径；按 kb_tag_aliases 归并 §3.11）——哪些概念在该 Agent 证实有效/失效；
  2. **LLM 终局归因**：结合演进史/卖出跟踪/失败修改记录，产出终局归因报告（什么有效、什么无效、为什么）；
  3. **管理 Agent 评审**：归因结论双闸复核（§3.4）；
  4. **回填知识库**：证实有效的概念 → 合并进现有条目统计或新建（observing）；确认无效 → 失效证据追加（invalid_reason 强化）；
  5. 全程审计；
- **防样本沉底与系统遗忘**：归档 Agent 的历史信号样本**继续参与**知识库跨 Agent 统计（授权路径，spec-02 §8）；归档 Agent 记忆转只读命名空间可读（spec-02 §5）。

## 6. 试运行验收（#18/#21/#57；v0.3 拍板 A4：默认历史回放）

### 6.1 三层验收标准（#18）

| 层 | 内容 | 判定方 |
|---|---|---|
| 工程完整性 | 流程跑通（选股→条件单→结算→日报闭环）/ 账户对账平（#49 双守恒） | 引擎数据+脚本（确定性） |
| 行为合规 | 大纲遵守 / 事实红线（#20 数字来自工具）/ 无越权（#33 审计无属主违例） | 审计检查（确定性）+ 管理 Agent 复核 |
| 逻辑自洽 | 选股结果与申报理念一致（抽样评审） | 管理 Agent（LLM） |

- **硬性门槛**：N ≥5 交易日 **且** 含 ≥1 次条件单尝试；**盈亏不设门槛**；**规则级异常（对账不平/越权/红线违规）不允许上线**；
- **上线确认单（v0.3 拍板 B5）**：三层验收通过后，管理 Agent 产出《验收报告》并以 **launch 单**（spec-04 v0.7 §4.1 type=launch）提交**最终用户确认**——总纲 §4.1 [上线运行] 节点的落地机制（decided_by=user，走待办/超时/审计机制）。

### 6.2 历史回放信息集构造（#21，防试运行前视；v0.3 拍板 A4）

- **试运行形态（v0.3 写死）**：**默认 = 最近 N 个交易日的历史回放**（回放窗口可配 **5-20 交易日，默认 5**，与 #18 N≥5 对齐），不依赖实盘跟随；回放任务由 spec-04 v0.7 调度承载（task_type=试运行回放，子 Agent 创建进入试运行时生成）；
- 历史日全市场快照**用日线重构当日可观测信息**（依赖 spec-03 backfill 日线历史）；**T 日选股只用 ≤T-1 收盘数据**（信息集硬规则，与 #57 配套）；
- 重构信息集由本规格产出规格、spec-03 提供数据、spec-01 L2 档执行回放（历史日无 L0/L1）；
- **试运行信号隔离**：历史回放期登记的信号全部标 `trial_flag=1`（spec-01 v0.7 §2.5），知识库统计默认排除（§3.2）；
- **试运行独立 trial 账户（v0.5，总纲 #63）**：试运行回放跑独立 trial 账户（创建 Agent 时随主账户一并生成，status=trial）——launch 通过后 trial 账户归档留证，**主账户天然干净无需复位**（spec-01 §2.8 v0.6；#61 复位机制废弃）；
- **结算落点与上线复位（v0.4，总纲 #61；spec-01 §2.8 / spec-04 §4.1）**：试运行回放结算**落主账户**（status=trial，历史日按可得档直接结算）；launch 单通过（用户确认）→ spec-04 触发**账户复位**（试运行数据快照归档为验收证据 + 现金/份额/净值重置 10 万 + status trial→normal）——**L2 近似业绩不进入正式净值曲线**，正式业绩自上线交易日起算（#48 份额法口径）；
- **试运行日报（v0.4）**：回放日日报正常生成落库（闭环验证需要），narrative 头部标注[试运行回放]；**默认不主动推送**直达（spec-04 §6.2 notify_rules 默认 disabled，防历史日日报轰炸用户会话），随 launch 单以《验收报告》向用户呈交结论；
- **验收报告不含试运行盈亏作为能力证据（v0.4，#57 一致）**：试运行盈亏仅供参考展示，验收判定以三层标准（§6.1）为准。

### 6.3 精度差标注（#57）

- 试运行历史日仅日线可得（L2 近似撮合），上线后为 L0 秒级精度——**"验证通过"=流程跑通与规则合规，不可外推为回放撮合行为无 bug**；
- 差异写入评估结论与交接说明；上线后**前 5 个交易日重点观察**（spec-04 日报联动标注）。

## 7. 大纲存储、版本与工作流分层（#27/§6）

### 7.1 大纲存储

```
charter_versions: id(ULID), agent_id, version_no, parent_version,
  content JSON(结构化大纲，§6.1 十节模板骨架), changed_by(manager|user),
  change_reason, created_ts, active BOOL
  -- UNIQUE(agent_id, version_no)；active 唯一
```

- **动态更新（§6.2）**：管理 Agent 编写/更新 → 版本留痕 → 通知子 Agent；charter_version 递增联动 spec-02 §7.2 静态前缀缓存失效；风控/粒度/任务清单变更**经审批（spec-04 §4）后**写入；
- 初始大纲由管理 Agent 创建子 Agent 时生成（总纲 §4.1）；
- **content 结构化字段表（v0.3 补，B6）**：按 §6.1 十节模板定义 JSON 字段结构——**P1 蓝图定稿**（本规格只锁定十节骨架与 active 版本语义）。

### 7.2 工作流分层（#27）

- **强制骨架（大纲定义，不可自选）**：每日闭环流程 / 事实红线 / 风控 / 汇报格式 / 权限边界；
- **执行层（子 Agent 自主优化）**：选股检索方式 / 复盘侧重 / 技能编排 / 任务与能力申请重塑节律——受上下文预算（spec-02 §7.3）与成本红线约束；
- **管理 Agent 定期效率审查**（每月）：执行层工作流变更的效率评估，结论入体检报告（spec-04 §5.5③健康度）。

### 7.3 健康度指标供给（spec-04 §5.5③ 数据源）

- 指标：违规记录数 / 缺勤天数 / 高耗任务占比 / **卖出质量趋势**（连续"卖飞"→提示该 Agent 复查止盈逻辑，总纲 §7.6）；
- 供给接口：月度聚合查询 → spec-04 体检报告生成消费。

## 8. 测试计划

- **能力**：上架四门槛逐项校验/危险调用静态扫描样例/版本兼容与绑定回滚/等价能力提示/申请-下发闭环流转；
- **知识库状态机**：双闸评审（前视样例被闸 1 拦截、同义反复被闸 2 拦截）/统计驱动状态流转（**单桶门槛 30 边界**）/不跨桶合并断言/stale-price 计入/滚动失效触发（**60 日窗口**）/验证预算到期出结论（**K=3**，valid/invalid/sealed 三分支）/失效名单重复申请拦截/**trial_flag 样本排除（试运行样本不作晋升证据）**/**预计可验证时间外推公式（freq=0 边界）**；
- **概念标签治理（v0.3）**：登记受控词表命中（规范名替换）/自由标签=探索口径/月度归并提议-确认-映射（kb_tag_aliases）/归并后统计按 canonical 重算（历史行不改写断言）；
- **参考非指令**：参考块与指令块物理分离（prompt 结构断言）/候选卡多样并列/决策引用留痕（ref_ids 断言）/静默命中避坑=违规用例/**UCB 排序单测（c=1.0、dispatch_n 计数、无 epsilon 项）**；
- **策略优化**：诊断先行校验（无证据提案拒绝）/**语义受控白名单（越界 diff 拒绝、理念哈希变更拦截）**/验证窗口判定（**10 日或 20 信号边界、先到为准**）/**独立验证账户（v0.5：创建 role=validation/满仓运行/跨账户同期比较/通过切换+归档/失败归档）**/晋升回滚状态机（联动 spec-02 API 幂等、无 validated 目标拒绝）/失败原因入记忆/重复提案短路；
- **归档提取**：终局统计正确性（含标签归并口径）/回填评审/归档 Agent 样本继续参与跨 Agent 统计（授权路径）/**生命周期事件触发经验提取任务**；
- **试运行**：三层验收清单逐项/**默认历史回放窗口（5-20 可配边界）**/历史回放信息集防前视（T 日信息 ≤T-1 断言）/**试运行信号 trial_flag 全量标记**/精度差标注入交接/规则级异常一票否决/**launch 上线确认单流转（spec-04 联动）**/**结算落主账户 trial 态 + launch 后账户复位（归档完整、重置 10 万、信号保留，v0.4 联动 spec-01 §2.8）**/试运行日报落库不推送（notify_rules 默认值）；
- **大纲**：版本链完整/active 唯一/charter_version 联动缓存失效（spec-02）/审批后写入。

## 9. §16 勾销表（本文件状态）

| # | 事项 | 状态 |
|---|---|---|
| 6 | 信号注册表 schema 统计口径（spec-01+spec-05） | 已写入 §3.2/§3.3（spec-01 引擎侧登记与 N 日结算已定稿；本规格定义统计消费口径） |
| 7 | 环境标签客观规则（spec-03/05） | **两侧齐备，关闭**——spec-03 §8 计算规则（env_bucket 生成与不回填）+ 本规格 §3.2/§3.3 统计消费（单桶独立门槛、换环境新周期、预计可验证时间外推公式）（v0.3 补勾销） |
| 8 | 分仓验证机制（仓位上限/窗口/判定/自动回滚） | 已写入 §4.2（v0.2 参数包确认 + v0.3 记账模型方案 A，引擎侧 spec-01 v0.4 §2.7） |
| 9 | 策略版本化存储（spec-02/05） | 流程侧已写入 §4.3（spec-02 §9 存储/API 已定稿；spec-01 v0.4 记账支持）——**两侧齐备，本项关闭** |
| 10 | 归档经验提取流程 | 已写入 §5（v0.3 补 spec-04 生命周期触发联动） |
| 11 | 市场异动探索源 | 已写入 §3.10 |
| 13 | 知识库选考排序（期望值+不确定性） | 已写入 §3.9（UCB1 参考实现，v0.3 补 dispatch_n 载体） |
| 14 | 知识库状态机证据源切换（自报→客观统计） | 已写入 §3.2/§3.3（含最小样本门槛、环境切片口径、试运行隔离） |
| 15 | 概念可操作性评审落地为可执行校验（双闸） | 已写入 §3.4 |
| 16 | 知识库"参考非指令"prompt 落地（spec-05/06） | spec-05 侧已写入 §3.5（假设卡/物理分离/引用留痕）；UI 标注待 spec-06 |
| 18 | 试运行验收三层标准 | 已写入 §6.1（v0.3 补 launch 上线确认单机制） |
| 21 | 试运行历史回放信息集构造（防前视） | 已写入 §6.2（v0.3 写死试运行默认历史回放形态） |
| 28 | 反向避坑库细则（spec-05/06） | spec-05 侧已写入 §3.6/§3.1（schema/来源/状态机/使用规则/凭证统计）；UI 展示与统计待 spec-06 |
| 34 | 停牌/退市前瞻收益兜底（统计侧） | 已写入 §3.3（stale-price 计入并标注；引擎侧执行已在 spec-01 §8） |

## 10. 评审确认记录

### 第一轮（2026-09-05 用户拍板，v0.2 落实）

1. **分仓验证参数包（#8/#31）**：验证窗口=10 交易日 或 ≥20 笔新信号（先到为准）；验证期仓位上限=账户 ≤30%（单票沿用大纲）；通过=窗口内零规则级违规 ∧ 期望值（含费）≥现役同期或 >0；失败=风控熔断 ∨ 期望值 <−2% → 自动回滚（§4.2）；
2. **验证预算 K（#26）**：每概念/修改最多 **K=3 个验证窗口**，到期强制出结论（valid/invalid/sealed）（§3.8）；
3. **统计门槛包（#24/#55）**：单桶最小样本 **n≥30**（不足=证据不足不晋升）；滚动失效窗口 **60 交易日**（期望值 <0 → 失效候选）（§3.3）；
4. **选考排序（#13）**：**UCB1** 参考实现（**c 默认 1.0 可配**），不另设 epsilon 随机项（探索由额度+异动源承担）（§3.9）；
5. **探索额度（#26/#40）**：每 **20 交易日**每子 Agent ≥1 个探索概念申请额度；新概念信号占比 **10-20% 软约束**（不达标管理 Agent 引导非强制）（§3.8）；
6. **能力沙箱（§2.2）**：**P1=静态危险调用扫描+管理 Agent 人工评审；P2=自动沙箱**（受限子进程：禁网/文件白名单/超时/资源限额）。

### 第二轮（2026-09-05 用户拍板，v0.3 落实）

1. **分仓记账模型=方案 A 账户内版本标记（A1）**：引擎 strategy_version_no 全链路（spec-01 v0.4 §2.7 新增），验证子仓按版本分组核算（20 笔计数/现役同期比较的取数键）、风控红线仍账户级、窗口收尾未平仓按官方收盘价并入且版本盈亏定格、回滚不改历史；**总纲 #31 同步 v0.17.6**；
2. **概念标签治理三件套（A2）**：受控词表优先（登记时匹配 KB 条目名）+ 未覆盖自由标签（探索口径）+ **月度归并窗口**（每月首个周末，LLM 提议+管理 Agent 确认+kb_tag_aliases 映射重算，历史行不改写）（§3.11）；
3. **试运行样本统计隔离（A3）**：signal_registry 加 trial_flag（spec-01 v0.4），知识库状态机统计默认排除试运行样本（可展示不作晋升证据）（§3.2）；
4. **试运行默认历史回放（A4）**：写死"默认=最近 N 交易日历史回放（L2）+信息集重构"，回放窗口可配 5-20 默认 5；回放任务挂 spec-04 调度（试运行回放任务类型）（§6.2）；
5. **spec-04 v0.4 联动补丁（A5）**：可延迟组补 kb_stats 刷新/归档经验提取、task_type 枚举补试运行回放/验证评估/经验提取/kb_stats 刷新、生命周期事件触发任务生成、审批 type 补 launch——悬空引用全部落地；
6. **勾销表补 #7（A6）**：两侧齐备（spec-03 计算规则+本规格统计消费），关闭；
7. **B 类**：语义受控执行机制=执行层参数字段白名单+diff 越界拒绝+理念哈希校验（§4.1）；UCB n_i 载体=kb_stats.dispatch_n（§3.9）；预计可验证时间外推公式（§3.2）；软删条目退出下发排序、统计保留（§3.7）；上线确认单 type=launch（§6.1，spec-04 联动）；charter content 字段表 P1 蓝图定（§7.1）；
8. **C 类**：kb_stats 重算触发=日结算完成后确定性重算零 token（§3.3）；归并窗口=每月首个周末（§3.11）；回放窗口可配 5-20 默认 5（§6.2）。

### 第三轮（2026-09-06，夜间优化轮·用户追认后定稿，落实于 v0.4）

1. **试运行结算落点与上线复位**（§6.2，总纲 #61 / spec-01 §2.8 / spec-04 §4.1 三方联动）——修复"试运行历史回放结算落在哪个账户、上线后账户状态如何"未定义缺口：落主账户 trial 态、launch 后复位归档、L2 业绩不入正式净值曲线；
2. **试运行日报默认不推送**（spec-04 §6.2 notify_rules 默认值联动），日报落库可查、结论随《验收报告》呈交；
3. **验收报告不含试运行盈亏作能力证据**（#57 精度差一致性）；
4. **用户追认（2026-09-06 上午）**：#61 试运行复位语义（trial 态结算 + launch 后复位归档 + 试运行日报默认不推送）拍板同意——v0.4 定稿。

### 第三轮（2026-09-06，满仓策略+独立账户，用户决策直接落地，v0.5）

1. **§4.2 重写**：分仓验证（方案 A 账户内版本标记）**废弃** → **独立验证账户**（role=validation、同初始资金、满仓运行 #62、跨账户同期比较、通过切换/失败归档）；
2. **试运行独立 trial 账户**（创建 Agent 时随主账户生成；launch 后归档留证）——#61 复位机制废弃；
3. spec-01 v0.6 §2.7/§2.8 同步（记账字段保留记录、复位删除）。

**剩余待实测项（P1/P3 执行，均为 config 可调参数按真实数据校准）**：单桶门槛 30 与滚动失效 60 日窗口（知识库统计真实分布）；UCB c=1.0（下发命中效果）；分仓验证阈值（10 日/20 信号/−2% 线）；异动板块阈值——结果固化回对应章节。

## 变更记录

| 版本 | 日期 | 状态 | 要点 |
|---|---|---|---|
| v0.1 | 2026-09-05 | 初稿待评审 | 能力配置中心（四类能力/三来源/上架四门槛/申请-下发闭环/版本绑定回滚）、知识库（条目模型 kb_entries+kb_stats/状态机五态/信号注册表客观证据源+单桶统计+stale-price 计入/双闸评审/参考非指令四落地（假设卡/物理分离/引用留痕/介入时机）/反向避坑库（三来源+必须回应+分开统计）/权限/防循环（验证预算 K+失效名单+探索额度）/UCB1 选考排序/市场异动探索源）、策略优化（EVOQUANT 四步/分仓验证参数/版本流程联动 spec-02 API/演进与失败记忆）、归档经验提取（五步+防样本沉底）、试运行验收（三层标准+历史回放信息集防前视+精度差标注）、大纲存储版本+工作流分层+健康度供给；§16 勾销 13 项（#6/#8/#9 流程侧/#10/#11/#13/#14/#15/#16/#18/#21/#28/#34）；待评审项 6 条 |
| v0.2 | 2026-09-05 | 第一轮评审拍板落实 | ①分仓验证参数包确认（窗口 10 交易日或 ≥20 信号先到为准/仓位 ≤30%/通过=零违规∧期望值≥现役或>0/失败=熔断∨期望值<−2% 自动回滚，阈值入 config 可调）②验证预算 K=3 确认③统计门槛确认（单桶 n≥30+滚动 60 日失效）④UCB1 c=1.0 确认、不设 epsilon⑤探索额度确认（20 交易日 ≥1 概念+10-20% 软约束）⑥沙箱分阶段确认（P1 静态扫描+人工评审→P2 自动沙箱）；§10 改评审确认记录+剩余待实测项（三个参数包 P1/P3 真实数据校准） |
| v0.3 | 2026-09-05 | 第二轮评审拍板落实（ZCode 深度评审） | ①**分仓记账=方案 A 账户内版本标记**（A1：引擎 strategy_version_no 全链路——**spec-01 v0.4 §2.7 联动新增**+总纲 #31 同步 v0.17.6；验证子仓分组核算/风控仍账户级/窗口收尾并入/回滚不改历史）②**概念标签治理三件套**（A2：受控词表+自由标签探索口径+月度归并窗口 kb_tag_aliases，§3.11 新增——防标签碎片化致统计永凑不齐）③**试运行样本隔离**（A3：trial_flag 默认排除状态机证据）④**试运行默认历史回放写死**（A4：L2+窗口 5-20 可配默认 5+回放任务挂 spec-04）⑤**spec-04 v0.4 联动补丁**（A5：可延迟组+2 员/task_type+4 类/生命周期事件触发/launch 单）⑥勾销表补 #7 关闭（A6）⑦B 类：语义受控白名单+理念哈希（B1）/dispatch_n 载体（B2）/外推公式（B3）/软删口径（B4）/launch 上线确认单（B5）/charter 字表 P1 定（B6）⑧C 类：kb_stats 日结算后重算/归并窗口每月首个周末/回放窗口可配 5-20；测试计划同步扩充 |
| **v0.4** | 2026-09-06 | 夜间优化轮（用户追认定稿） |
| **v0.5** | 2026-09-06 | 满仓策略+独立账户（用户决策落地） | §4.2 重写为**独立验证账户机制**（分仓方案 A 废弃；满仓运行 #62；通过=主账户切换版本+验证账户归档；失败=归档）；试运行独立 trial 账户（#63，复位废弃）；spec-01 v0.6 §2.7/§2.8 同步 | ①§6.2 **结算落点与上线复位**（总纲 #61 三方联动：spec-01 §2.8 复位机制 / spec-04 §4.1 launch 触发；trial 态结算、复位归档、L2 业绩不入正式净值曲线）②试运行日报默认不推送（notify_rules 默认值）③验收报告不含试运行盈亏作能力证据（#57 一致）④测试计划补复位与推送用例 |
| **v0.6** | 2026-09-06 | Web 部署版适配（仅引用同步，待用户终审） | 随总纲 v0.20 Web 部署版 P1 基线提交：本文件无桌面承载触点，业务机制同 v0.5 全部保留；仅同步头部版本/状态与依据、联动引用为总纲 v0.20 / spec-01 v0.7（§2.5/§2.7/§8 字段链路）/ spec-02 v0.5 / spec-03 v0.6 / spec-04 v0.7 / spec-06 v0.5；§10 v0.3/v0.5 轮评审记录与历史变更行保留原文 | （终审动作为用户拍板：确认后 v0.6 即 Web 部署版基线） |
