# spec-04 调度·日报·审批规格 v0.7（Web 部署版适配：sidecar → core 常驻 + 站内通知 web）

> 版本：v0.7（2026-09-06，Web 部署版适配轮·随总纲 v0.20 提交，待用户终审：承载形态由 Tauri 桌面壳 + Python sidecar 重构为**浏览器前端 + Nginx 反代 + core 常驻（systemd）**——§1 边界/§2.2 tick 归属/§6 通知通道/§9.2 单实例与守护/§11 勾销/§13 P 阶段 承载表述改写（sidecar → core、桌面通知 → 站内通知 web、Tauri 单实例/心跳 → core 进程级单实例/systemd 守护）；调度/任务表/审批/日报/执行预算/时钟自检/launch 复位等业务规则与决策不变；v0.6 全部内容保留）
> **状态：⏳ Web 部署版适配稿（随总纲 v0.20 一并以 Web 部署版 P1 基线提交，待用户终审定稿）——定稿后 v0.7 替代 v0.6 成为 Web 部署版开发基线（业务规则同 v0.6）。**
> 依据：总纲 v0.20（Web 部署版，业务决策承 v0.18 与 #60/#61）§2 架构、§3.3-3.5（角色/消息/生命周期）、§4（管理工作流/审批统一模式/性能与容量）、§10（调度器/时间线/日报模板/汇报链路/分工）、§12.1（部署形态：core 常驻 systemd）、§12.3-12.4（LangGraph/LLM 路由）、§12.6；联动 spec-01 v0.7（§2.8 试运行复位、结算就绪检查）、spec-02 v0.5（messages/audit_logs/performance_records 表、checkpoint 保留策略）、spec-03 v0.6（数据就绪判定 is_ready、源配额声明）、spec-05 v0.6（§6.2 试运行结算落点与复位、上线确认单）、spec-06 v0.5（审批中心/日报中心/直控台 UI 展示）
> **受约束决策**：#6（容量用户决定） #8（推送渠道，v0.17.7 修订为 clawbot+企微；v0.7 Web 改写：站内通知 web 默认） #13（预算仅提示） #19（可靠性三件套） #22（北京时间） #23（引擎≠LLM 分工） #27（工作流分层·任务申请） #28（任务时效分级） #32（月度体检报告） #33（执行器唯一 actor） #34（熔断短路澄清收敛） #36（用户直控台·联动） #37（资源闸门，v0.17.5 修订口径） #39（管理Agent降级） #41（单实例锁，Web 改写：core 进程级） #42（守护/优雅退出，Web 改写：core + systemd） #43（结算数据就绪） #45（结构化输出校验重写） #50（可延迟无时效） #58（interrupt 审批超时）
> **§16 勾销**：#12 #36（见文末勾销表）
> 与总纲冲突时以总纲为准并同步本文件。

---

## 1. 模块定位与边界

- **职责**：调度器与任务表（tick 循环、任务状态机、崩溃恢复、补跑与时效分级、可延迟任务组、启动序列）、LangGraph 执行层运行时（checkpoint 衔接、interrupt 审批节点、执行器封装、结构化输出校验）、审批流（统一模式、确定性短路、澄清收敛）、日报体系（模板、引擎数据段+LLM 叙述段、缺勤日报、月度《策略体检报告》）、消息路由与汇报链路及推送通道（路由表、会话直达、事件映射、限额聚合）、容量评估与资源闸门、LLM 路由与统一适配层、管理 Agent 降级（安全自治模式）、core 进程保障（调度侧：进程级单实例锁/看门狗/退出顺序，§9.2）；**任务表/审批单/日报表/推送配置的 schema 归本规格**（spec-02 §1 已声明"任务表与调度归 spec-04"）。
- **不负责**：引擎业务计算（spec-01——本规格只负责触发与登记，结算/撮合/风控逻辑在引擎）；数据获取与就绪判定（spec-03）；存储层 schema 与备份机制（spec-02——本规格是备份任务的**调度执行方**，spec-02 §10 已定安全点与最高优先级）；能力中心/大纲存储/知识库（spec-05——审批流的**业务规则**如沙箱验证归 spec-05，本规格提供通用审批机制，写大纲配置经 spec-05 API）；UI（spec-06——审批中心/性能监控/日报中心展示，含冷却期人工放行入口）；单实例锁与守护的 **systemd 承载实现**（部署层，总纲 §12——本规格只定义 core 侧配合约定：进程级单实例锁/看门狗/退出顺序，§9.2）。
- **消费方**：引擎（spec-01 被触发结算/撮合）、数据服务（spec-03 被触发拉取、闸门消费其配额声明）、记忆系统（spec-02 摘要谓词执行/备份调度/收尾沉淀调用）、**策略优化与知识库（spec-05：验证评估/经验提取/kb_stats 刷新/试运行回放任务的调度承载）**、管理 Agent 与子 Agent 工作流、spec-06（审批待办/性能/日报展示数据源）。
- **信任边界**：#33——执行器节点为唯一 actor，LLM 输出仅为节点内"建议意图"；agent_id 通道绑定注入、属主校验、审计盖章（audit_logs 统一写入口，spec-02 §5.1）。

## 2. 调度器与任务表（§10.1 落地）

### 2.1 任务表 schema（草案）

```
task_schedule: id(ULID), agent_id, task_type(选股|挂单|结算|日报|总汇报|复盘|摘要补跑|
    备份|L0清理|体检报告|补救启动|健康检查|试运行回放|验证评估|经验提取|kb_stats刷新|...),
    task_slot(HH:MM 或谓词名), trade_date,
    status(pending|running|done|failed|skipped|expired|partial), scheduled_ts,
    started_ts, ended_ts, attempt_count, last_error,
    interrupt_entered_ts(interrupt 挂起时刻，v0.3), approval_id(来源审批单,可空),
    resource_class(scan|llm-heavy|light), is_deferrable BOOL, priority INT,
    dedup_key, payload JSON, created_ts
  -- 幂等键：UNIQUE(agent_id, task_type, trade_date, task_slot, dedup_key)——补跑/重跑不重复生成
  -- v0.6：dedup_key 入键（默认空串）——事件触发任务（验证评估/经验提取等）以业务子键
  --（checkpoint_id/归档事件 id）区分同日多事件，防同键碰撞吞任务
```

- **任务生成**：每日 00:05（北京时间）调度器按各 Agent 大纲定时任务清单（大纲 §6.1 第 9 节）+ A 股交易日历生成当日任务行，非交易日跳过；**日历依赖（v0.3 补）**：使用 spec-03 `calendar()` **全年交易日历（含节假日表）**，不依赖"每日刷新"的当日数据（凌晨刷新未落地时判定不受影响）；
- **生命周期事件触发（v0.4 补，联动 spec-05）**：Agent 生命周期状态迁移事件（总纲 §3.5）由调度器即时生成对应任务行——**归档/退休 → 生成"经验提取"任务（可延迟组，spec-05 §5）**；策略 checkpoint → 注册"验证评估"任务（验证窗口到期日触发，spec-05 §4.3）；子 Agent 创建进入试运行 → 生成"试运行回放"任务（spec-05 §6.2）；
- 用户/管理 Agent 动态新增、撤销、审批通过**即时生效**（总纲 §10.1），落审计；
- 时间线为模板、账户级可配（变更走任务审批 §4.4）：默认下午盘运行版（§10.2：12:01 选股 / 12:45 挂单 / 15:35 结算最早点 / 18:31 日报 / 20:00 总汇报 / 21:00 复盘可延迟）。

### 2.2 tick 循环（每分钟，固定北京时间 #22）

调度器每分钟 tick 的检查清单（P1 起实现）：

1. **到点任务投递**：`scheduled_ts ≤ now ∧ status=pending` 且资源闸门（§7.2）放行 → 投递执行（占闸门额度）；
2. **引擎结算触发**（联动 spec-01 §3.1）：到达某账户结算最早点（15:35）→ 检查数据就绪（spec-03 `is_ready`）→ 就绪即触发结算；未就绪进入重试窗口（默认 60 分钟，#43），超窗兜底 L2 近似结算并标注——**结算逻辑本体在 spec-01，调度器只判定"该不该触发"**；
3. **跟踪/信号到期**：卖出跟踪与信号 N 日结算到期（spec-01 §8 注册表）→ 触发引擎结算；
4. **盘中撮合周期**：INTRADAY 账户撮合周期到达 → 触发引擎盘中撮合（数据保鲜 #35 检查在引擎侧）；
5. **可延迟任务组窗口判定**（§2.5）；
6. **interrupt 超时扫描**（§3.3：`interrupt_entered_ts` 超过阈值 → resume 注入保守拒绝）与审批单过期扫描（§4.4）；
7. **管理 Agent 健康判定**（§9.1 降级判定）。

**tick 归属（v0.3 补，A7）**：core 内**调度器拥有唯一 tick 循环**；引擎/数据服务等模块以**"到期检查回调"注册**到调度器（§2.2 第 2-4 项即引擎注册的回调，总纲 §10.1"引擎侧 tick 检查"由此实现），或独立线程但全部操作幂等——禁止双循环各自判定同类状态产生竞态；回调注册机制细则 P1 蓝图定。

### 2.3 任务状态机与崩溃恢复（#19）

```
pending → running → done | failed | partial(收尾部分失败，v0.3)；
时效过期 → skipped；未决审批过期 → expired
```
- 崩溃恢复：进程重启后 `running` 且无心跳 → 判 failed 可重跑；Agent 会话从 LangGraph checkpoint 恢复（介质=业务库 SQLite，spec-02 §6.1）；
- 重跑幂等三层：引擎任务靠 settle_key UNIQUE/整事务（spec-01 §3.8）；记忆沉淀靠 dedup_key（spec-02 §3.1）；任务行本身靠 §2.1 幂等键；
- **重试纪律（#34/#37）**：重试仅限确定性错误（429 限流/认证失败）单任务 ≤2 次；失败重试指数退避+抖动+**重试预算默认 3 次/任务（v0.2 确认）**；未知错误一律不自动重试。

### 2.4 补跑与时效分级执行（#28）

- **无时效引擎任务**（结算/除权除息/跟踪进度/对账）：纯计算低成本，按交易日顺序**快进回放**补齐，保证账户连续正确（执行时机见 §2.6 启动序列）；
- **有时效决策任务**（启动时对照现实时间判定）：已过时点的选股 → `skipped` 不补；日报 → 降级**缺勤日报**（§5.3）；夜间复盘过期不补；**盘中首次启动且当日尚未设条件单 → 生成"当日补救启动"任务**（快速为持仓补保护性条件单与当日简况，防持仓裸奔）；
- **跨多个交易日未运行**：先快进回放确定性任务，LLM 任务只处理最近一个仍可执行窗口，其余补记缺勤。

### 2.5 可延迟无时效任务组（#50）

- **成员与优先级**（高→低）：①每日备份（spec-02 §10：**最高优先级，进程启动即补、不等空闲窗口；不受 30% 新任务线暂停（v0.3），仍受结算安全点约束**）②分层摘要补跑（谓词=spec-02 §4.1 日/周/月缺口检测）③卡片补卡（spec-02 §3.2 超时 24h）④夜间复盘（模板时点 21:00，错过空闲窗口补）⑤月度体检报告生成（§5.5）⑥L0 序列清理（spec-03 §5，保留 N>0 时）⑦**kb_stats 刷新（v0.4 补，spec-05 §3.3：日结算完成后确定性重算知识库统计，零 token——通常紧随结算即可完成，入组兜底）**⑧**归档经验提取（v0.4 补，spec-05 §5：归档事件触发生成，可延迟组执行）**；
- **空闲窗口判定（v0.2 确认）**：非交易时段 ∧ 无 running 任务 ∧ 本地可用资源 ≥30% 余量线（#37 联动）——满足即按优先级依次执行；
- **让路语义（v0.3 修正，B1）**：到点交易任务**优先投递**；running 的可延迟任务**不中断**（中断 LLM 调用浪费 token），仅**停止投递新的**可延迟任务，待其自然完成后剩余成员顺延——资源竞争交闸门（§7.2）约束。

### 2.6 启动序列（v0.3 新增，A4）

进程启动（首启/崩溃拉起/重启）后按序执行：

```
1. 单实例锁获取（§9.2）——失败即拒绝启动并告警
2. 系统时钟偏差自检（v0.6：网络时间对比，偏差 >60 秒告警不阻断——created_at 引擎盖章与回放时序可信度前提）+ 读全年交易日历（spec-03 calendar）→ 判定今日是否交易日
3. 生成/校验当日任务行（§2.1；历史缺失日按需补行）
4. 快进回放：无时效引擎任务按交易日顺序逐日补结算（生成补跑任务行，
   依赖 spec-01 settle_key 幂等，重复触发零重复）
5. 时效任务现实时间判定（§2.4：过期 skipped/缺勤日报/盘中补救启动）
6. 可延迟组按空闲窗口判定入队（§2.5，备份立即执行）
7. 进入 tick 循环（§2.2）
```

## 3. LangGraph 执行层运行时（§12.3 落地）

### 3.1 两层定界与自研胶水

- **业务层**（管理 Agent 治理：审批/大纲/知识库/汇总）= 本项目编排代码，不入图；
- **执行层** = LangGraph：每个任务一次图执行（选股图/日报图/复盘图/审批子图/能力建设链…），显式状态图+并行分支+节点可观测日志；
- **自研胶水四件**：调度触发（§2）、消息总线（§6.0）、工具注册绑定（spec-05 能力中心）、上下文装配（spec-02 §7）；
- checkpointer 用 SQLite saver（介质归 spec-02 §6.1），与任务表状态机衔接（图崩溃恢复↔任务重跑）；
- 运行时接口留薄抽象，LangGraph API 变动可局部替换（总纲护栏）。

### 3.2 任务收尾节点（沉淀调用点定义，spec-02 §6.2 联动）

每个任务图**固定以收尾节点结束**，依次：写 performance_records（§7.1）→ 任务状态落库 → **显式调用 `memory.sediment()`**（spec-02 §6.2 拍板的"任务收尾显式调用"，本节即调用点定义，P1 蓝图按此落）→ 消息上报（日报/事件，§6）。

- **sediment 失败语义（v0.3 补，A5）**：sediment（LLM 强模型调用）失败 → 任务标 **partial**（主体产出已落库、沉淀未完成）→ **自动重试**（指数退避，spec-02 dedup_key 保证重跑零重复沉淀）→ 连续失败默认 3 次 → 告警入管理 Agent 待办与日报；性能记录与任务状态落库先行，sediment 失败不回滚任务主体。
- **任务级执行预算（v0.6 新增，总纲 #60）**：每次图执行（任务）配预算——`max_llm_rounds`（默认 30）+ `max_tool_calls`（默认 60，均可配）；执行器节点计数（工具调用次数落 performance_records.tool_calls，spec-02 §11），超限 → **强制跳转收尾节点**（任务标 partial、日报/输出注明"预算截断"及已完成部分），防 LLM 循环失控烧 token；与 §7.2 资源闸门（并发/投递维度）、spec-02 §7.3 装配预算（上下文维度）三层互补。

### 3.3 interrupt 审批节点（#58；v0.3 修正 A3+A2）

- **超时机制（v0.3 改写）**：LangGraph `interrupt()` 挂起后该次图执行已返回、节点函数不再运行——**图内无活跃计时器**，唯一权威机制为：①进入 interrupt 时**节点包装器将 `interrupt_entered_ts` 写入任务行**（§2.1）②调度器 tick 扫描（§2.2 第 6 项）超时（默认 30 分钟，可配——v0.2 确认）→ **以 `Command(resume=保守拒绝)` 恢复图**（"审批超时未处理，维持原状"）：跳过该分支、其余子图继续；
- **迟到决定一致性（v0.3 拍板 A2）**：超时保守拒绝时**自动关闭对应审批单**（status=expired + 备注"图分支已超时拒绝"，§4.4）——防审批单仍 pending 与图分支状态不一致；**迟到的审批决定仅留痕、不生效**，UI 提示重新申请；
- 悬空期间同类申请由调度器**合并为一次提醒**，不重复计费；#34 重复申请短路（附上次结果）不受超时影响；待办超时本身经通道层推送提醒（§6.2 事件枚举）。

### 3.4 执行器节点封装（#33）

- 图内所有敏感操作（引擎/记忆/推送/外部网络）经**执行器节点**代理：注入 agent_id（LLM 不可见不可指定）、属主校验、审计盖章（audit_logs 唯一写入口，spec-02 §5.1）；
- Agent 不直接触网/触库；工具调用经统一注册（spec-05 绑定关系校验）。

### 3.5 结构化输出校验-重写（#45）

- 机器消费的输出（条件单 JSON/申请单/审批决定/能力注册元数据）必须过 schema 校验（字段完整/类型合法/数量为正/证券代码存在）；
- 非法 → 带错误信息回喂模型重写 ≤2 次；仍失败 → 跳过该产出并记"输出失败"入日报复盘，不无限重试。

## 4. 审批流（§4.4 统一模式落地）

### 4.1 审批单模型

```
approval_requests: id(ULID), type(task|granularity|capability|risk|launch), agent_id,
  payload JSON(期望值+理由(策略依据)+资源预估), content_hash(规范化去重哈希),
  status(pending|approved|rejected|expired|withdrawn), decided_by(manager),
  decided_ts, reason(驳回理由，可申诉), expires_ts, close_note(关闭备注，v0.3),
  result_ref(通过后生成的任务/配置/版本 id), created_ts
```

- **type 补充（v0.4，联动 spec-05 §6）**：`launch` = **上线确认单**——试运行验收三层标准通过后，管理 Agent 产出《验收报告》（spec-05 §6.1），以 launch 单提交**用户确认上线**（总纲 §4.1 [上线运行] 节点）；launch 单决定由用户作出（decided_by=user），同样走待办/超时/审计机制；
- **launch 通过后的账户复位（v0.6，总纲 #61；spec-01 §2.8）**：用户确认上线 → 执行**账户复位**（试运行归档 + 现金/份额/净值重置 10 万 + status trial→normal）→ 生成上线首日任务行（选股/挂单/结算）；复位失败告警并**阻断**上线首日任务生成（防带病起跑）；
- 流程：子 Agent 提交（执行器盖章）→ 管理 Agent 审核（必要性 / 与大纲一致性 / 资源可行性（§7 容量）/ 数据源能力（spec-03 健康度，如分钟行情可得性））→ **通过**：写账户/任务/大纲配置（大纲经 spec-05 API）+ 生成任务行（task_schedule.approval_id 回引）+ 生效通知；**驳回**：附理由，可申诉；
- 撮合粒度变更属账户配置变更：写入账户变更流水 + 大纲变更记录，全程留痕；管理 Agent 可**主动下调**（设备满载/数据源限流），理由留痕；
- **存储模型（v0.2 拍板 A）**：审批单**独立成表** + task_schedule.approval_id 引用——任务表统一管理"任务"（含审批来源），审批单承接五类申请；总纲 §10.1 措辞已同步 **v0.17.4**。

### 4.2 确定性短路（#34，零 LLM 成本）

- **重复申请哈希去重**：content_hash（type+agent_id+payload 规范化 JSON 哈希）命中已决审批单 → 直接附上次审批结果返回，**不进评审、零 LLM**；防"申请-驳回-再申请"振荡；
- **缺字段/格式非法**：直接退回（#45 校验前置）；
- **申请频率配额（v0.2 确认；v0.3 修正豁免规则）**：每 Agent 每类型 pending 未决 ≤3 件；被驳回同类 24h 冷却——冷却期内申请**直接退回**（附冷却剩余时间），**用户可在 UI 人工放行豁免**（v0.3 拍板 B2：替代原"payload 实质变化才受理"——后者不可确定性判定且与零 LLM 矛盾）；
- pending 满额退回时**提示"有待决审批单"并给出审批中心入口**（v0.3 补，B7；含 interrupt 悬空单占位场景）；
- 全部短路动作审计留痕。

### 4.3 需求澄清收敛（#34）

管理 Agent 需求澄清单策略 ≤5 轮，超出自动收敛为结构化清单转用户逐项确认，防对话无限膨胀。

### 4.4 审批单过期与关闭

- pending 单设 expires_ts（**默认 24h——v0.2 确认**）——过期标 expired（区别于 interrupt 超时的保守拒绝：expired=单失效需重新申请）；过期经通道提醒子 Agent；
- **interrupt 超时联动关闭（v0.3）**：图分支超时保守拒绝 → 对应审批单自动标 expired + close_note="图分支已超时拒绝"（§3.3）；此后到达的审批决定仅留痕（decided_by=迟到留痕）不生效，UI 提示重新申请。

## 5. 日报体系（§10.3/§10.5/#32 落地）

### 5.1 日报模板（大纲强制字段）

六段模板照总纲 §10.3：一、今日操作总结；二、持仓与盈亏；三、卖出跟踪摘要；四、市场观察；五、策略执行评估与明日计划；六、能力/任务/风险申报。字段完整性/格式校验归脚本零 token（§10.5）。

### 5.2 引擎数据段 + LLM 叙述段（#23 成本原则）

| 段 | 内容 | 生成方 |
|---|---|---|
| 数据段 | 一/二的明细表格、三的跟踪记录表、五的执行数据（未成交原因/偏差数据） | 引擎结算产物直接生成（spec-01），零 token |
| 叙述段 | 四（市场观察，标"主观"）、五的判断与明日方向、六的申报叙述 | LLM（子 Agent） |
| 合并 | data_section JSON + narrative → merged_markdown | 调度器/收尾节点拼装 |

```
daily_reports: id(ULID), agent_id, trade_date, version INT(递增，v0.3),
  data_section JSON, narrative TEXT, merged_markdown TEXT,
  status(normal|absent|resend), llm_perf_id(费用追溯), created_ts
  -- UNIQUE(agent_id, trade_date, version)——补发/修订新版本行留痕（v0.3 修正，A5）
```

### 5.3 日报生成与缺勤日报（#28）

- **正常**：18:31 任务触发——**前置依赖检查（v0.3 补，B3）：当日结算任务 done 才生成正常日报**；取结算产物（数据段）+ LLM 叙述段 → 合并落库 → 推送（§6）；
- **缺勤**：Agent 当日任务 failed/未运行 **或结算任务未完成/failed** → 引擎数据段照常补齐（可得部分）、叙述段=缺勤说明（未运行/未结算原因），`status=absent`，下一空闲窗口（§2.5）补生成——用户看到的日报完整度不变，叙述段如实说明；
- 修订/补发新版本行（version+1）留痕，不覆盖已推送版。

### 5.4 每日总汇报（20:00）

管理 Agent 汇总各子 Agent 日报+异常事件+待办 → 用户《每日总汇报》推送。管理 Agent 不可用（§9.1）→ LLM 点评段延迟批处理；连续 24h 未恢复 → 发**确定性拼接版**（纯数据汇总无叙述，调度器生成），不静默缺失。

### 5.5 月度《策略体检报告》（#32/§16 #12）

- **内容字段**：①各子 Agent 净值曲线与回撤（份额法口径，spec-01 §6.1）②概念验证进度（信号统计摘要：入信号频率、各环境桶预计可验证时间——spec-05 供给）③健康度（违规记录/缺勤天数/高耗任务占比）④费用月报（元/月/Agent/任务类型，spec-02 §11 消费）⑤下一步建议（LLM 撰写）；
- **生成**：月末最后交易日收盘后可延迟组（§2.5）生成——①-④确定性生成零 token，⑤ LLM；
- **推送时点（v0.2 确认）**：**次月首个交易日 20:00 与每日总汇报同批**推送（事件类型=月度体检报告，§6.2）。

## 6. 消息路由与汇报链路、推送通道（§10.4/§16 #36）

### 6.0 消息路由规则（v0.3 新增，C 类：消息总线成节）

| 消息流 | 路由处理 | 存储 | 推送 |
|---|---|---|---|
| 子 Agent → 用户（日报/异常/回执/提问回复） | **事件白名单校验**（spec-02 §6.2：非白名单→pending_review 转管理 Agent 审阅队列）→ 直达 | messages(sync_to_manager=1) | 按事件映射 §6.2 |
| 子 Agent → 管理 Agent（申请单） | 管理 Agent 待办队列 | approval_requests | 审批待办事件 |
| 管理 Agent → 子 Agent（审批决定/大纲下发） | 生效执行（写配置/生成任务行）+ 通知 | approval_requests + messages | 审批回执 |
| 系统 → 管理 Agent（健康检查/数据质量/降级事件） | 内部处理/待办 | messages / audit_logs | 异常提醒（严重时直达用户） |
| 系统 → 用户（总汇报/体检报告/预算提示/缺勤提醒） | 按事件映射 | messages | 按事件映射 §6.2 |

- 消息总线为自研胶水（§3.1）：统一按上表路由，全部落审计；白名单校验点是子 Agent 直达消息的唯一入口。

### 6.1 会话直达与后台同步（v0.16 链路）

- 子 Agent 日报与事件消息**直达用户**（独立会话，联系人式）**同时后台同步管理 Agent**——消息落 spec-02 messages（sync_to_manager=1）；管理 Agent 是监督者非中转；
- 子 Agent → 用户受**事件白名单**约束（日报/异常上报/审批与申请回执/提问回复；不可闲聊）——白名单强制校验=spec-02 §6.2 pending_review 拦截（§6.0 路由表）。

### 6.2 通道抽象与事件映射

- 通道统一接口：`send(type, event, content)`；实现：**站内通知（web：WebSocket 实时 + 浏览器通知 + 通知历史铃铛，默认）/ 邮件 SMTP / 微信 clawbot 通知 / 企微群机器人 webhook**（v0.5：个人微信通道由 Server酱 改为微信 clawbot 通知，总纲 v0.17.7；v0.7 Web 改写：默认通道桌面通知 → 站内通知 web，总纲 v0.20）；
- **clawbot 接入形态**：接入配置用户自配（服务地址/令牌等，入通道配置；P4 接入时按 clawbot 实际接口固化，实测项入 spec-06 §11 验证清单）；
- 事件类型枚举：每日总汇报 / 日报直达 / 异常提醒 / 审批待办 / 风控熔断 / **数据质量告警（v0.3 补：spec-03 §7 交叉抽检/除权跳变/日历校验的直达用户路径）** / 月度体检报告 / 缺勤提醒 / 待办超时提醒 / 预算提示 / **上线确认待办（v0.4 补，launch 单）**；
- 事件→通道映射可配（notify_rules 配置表：event, channel, enabled, format_level）——用户可配"只收摘要 / 只看异常 / 全部日报原文"（§10.4）。

- **试运行日报默认不推送（v0.6，总纲 #61）**：试运行历史回放的日报仅落库（验收可查、narrative 标注[试运行回放]），直达推送**默认 disabled**（notify_rules 可改）——防 5-20 个历史日日报轰炸用户会话；试运行结论随 launch 单《验收报告》向用户呈交。

### 6.3 限额感知与聚合（#36，P4 随外部通道实现）

- **限额配置化不写死**：**clawbot 限额按其服务条款接入时实测并固化（P4）**、企微群机器人 20 条/分钟（默认值入通道配置，可改）；
- 通道层按通道计数（日计数/分钟滑动窗口）；达阈值前可选**"合并为单条推送"**（事件打包：当日多条日报/事件聚合一则摘要）；
- **超限/失败的补偿路径 = 站内通知（web）兜底**（站内通道无配额）；
- 阶段说明：P1-P3 仅站内通知（web）单通道（无配额问题，本节逻辑不启用）；P4 上线邮件/clawbot/企微时实现。

### 6.4 推送可靠性

- 发送结果回写 messages.status（delivered/failed，spec-02 §6.2 状态机）；failed → 自动降级站内通知（web）兜底 + 计数告警；重试遵守通道限额（聚合后重发而非逐条重试）。

## 7. 容量评估与资源闸门（§4.5/#37 落地）

### 7.1 性能采集

- 执行器在任务收尾节点写 performance_records（schema 归 spec-02 §11：duration/mem_peak/llm_calls/tokens_in/cached_tokens/tokens_out/cost_yuan/data_fetch_bytes/high_cost_flag）；
- cached_tokens 由适配层透传（§8.1）；高耗任务标记与 spec-02 §7.3 装配预算联动。

### 7.2 资源闸门模型（#37，总纲 v0.17.5 口径）

- 任务声明资源类：`scan`（全市场扫描/大数据拉取）/ `llm-heavy`（多轮 LLM 任务）/ `light`（常规）；
- **闸门规则**：
  1. **同数据源大扫描互斥**：同源 scan 任务全局串行排队（其余任务等待并共享缓存）——源频率/并发/每日额度配额取 spec-03 §2.1 能力标签；
  2. **LLM 调用按性能评估动态投递（v0.2 拍板，总纲 v0.17.5 同步）**：不设固定并发数——投递前采样本地性能余量（1 秒均值：CPU 可用率与内存可用率），**两者均 ≥10%（可配）才投递下一个 LLM 调用，低于线进入等待队列（按优先级 FIFO）而非失败**，下个采样周期重判；实际并发由"本地余量 + 远端 provider 限流（429 走重试纪律 §8.3）"共同决定——LLM 调用以网络等待为主、本地占用低，10% 为硬底线；
  3. **本地资源预留 30% 余量线（新任务投递门槛，#37）**：可用资源低于 30% → 暂停投递新任务（running 不中断），回落即恢复——与第 2 条分层：30% 管新任务准入、10% 管任务内 LLM 调用准入；**例外（v0.3）：每日备份高优先级不受此线暂停（仍受结算安全点约束）**；
- **级联防护**：失败重试指数退避+抖动+重试预算（§2.3，默认 3 次/任务）；单任务连续失败不拖垮同源其他任务（源级隔离熔断）；一个数据源限流不影响其他源任务。

### 7.3 容量评估与应用

- 用户新建子 Agent / 勾选并行时，基于历史 performance_records 主动提示容量建议（"当前主机建议并行 ≤N 个 Agent"）；**最终由用户决定，管理 Agent 不强制拒绝**（#6）；
- 成本审查三维：**元/天 × Agent × 任务类型**（§4.5），月度汇总入《策略体检报告》（§5.5）。

## 8. LLM 路由与统一适配层（§12.4 落地）

### 8.1 统一适配层

- **OpenAI 兼容为主协议**（DeepSeek/通义/智谱/Kimi/OpenAI 兼容端点/Ollama）、**Anthropic Messages 为辅协议**；统一接口 `chat(messages, tools, model_group) / stream / tool-call`；文心/豆包等非兼容模型单独薄适配；
- 约束以**能力门槛**表达（强推理/工具调用/长上下文），路由按门槛选模型而非锁定协议；用户可固定任意 Agent 到任意 provider（管理 Agent 也可配 Claude）；
- **usage 透传**：`prompt_tokens_details.cached_tokens` → performance_records.cached_tokens（spec-02 §11 联动）；无该字段的 provider 记 0 并标注。

### 8.2 路由决策（默认自动；v0.2 确认排期）

- **P1 最小版**：单 Agent 手动跑通阶段不接路由——固定使用用户设置的单模型；
- **v1 启发式（P2）**：a) 任务类型映射（审批/策略评估/知识沉淀 → 强推理档；日报/格式化/常规查询 → 经济档）b) 成本与用量（预算参考值超出**仅提示**，#13）c) provider 健康检测（失败率/超时 → 剔除劣化者）；
- **v2 效果评分（P4）**：每类任务各模型质量评分（审批被打回率/日报字段完整率/重试率）→ 择优，结果回写评分；
- **手动覆盖优先于自动**：全局/某 Agent/某任务类型可固定 provider+model；开关默认"自动"。

### 8.3 重试纪律与预算（#34/#13）

- 重试仅限确定性错误（429/认证失败）单任务 ≤2 次；**跨供应商切换只作用于新任务**，不重放已发起任务（防重复扣费）；未知错误一律不自动重试；
- 预算参考值（全局/Agent 级可配）超出仅站内通知（web）提示"本月用量超预算"，不硬性降档。

## 9. 管理 Agent 降级与进程保障（#39/#41/#42）

### 9.1 安全自治模式（#39）

- **原则**：子 Agent 每日闭环由调度器驱动，**不依赖管理 Agent 可用性**；
- **降级判定（v0.3 补载体定义，B4）**：管理 Agent 非常驻进程，"心跳"=**周期健康检查任务**（health_check，默认 30 分钟一次：轻量 LLM ping + 进程内状态自检）——判定规则：**健康检查任务连续 N 次 failed（N 默认 3）∨ 审批/interrupt 恢复调用连续失败 N 次** → 进入安全自治；
- **降级行为**：**无新审批/新能力下发**（§4 审批流挂起、pending 单保持不丢）；既定大纲任务照常执行；风控照旧；确定性职责即时处理，LLM 职责（审批评估/汇总点评/知识沉淀）延迟批处理；
- **恢复**：健康检查连续成功 M 次（默认 2）→ 退出降级 → 积压 LLM 职责按可延迟组补跑；降级/恢复事件推送用户并审计；
- 管理 Agent 全局状态全落库（无进程内存依赖），损坏可由每日备份还原（spec-02 §10）。

### 9.2 单实例与守护（core 侧约定，Web 部署版）

- **单实例锁（#41，Web 改写）**：core **进程级单实例**——PID/锁文件 + SQLite 单实例标记（本规格定义 core 侧配合约定，systemd 单实例运行，替代 Tauri single-instance 插件）；双 core 双跑会产生双倍 token 与重复建单，锁失败即拒绝启动并告警；
- **守护与优雅退出（#42，Web 改写）**：core 由 **systemd 守护**（Restart=always + 看门狗/健康检查自动拉起，替代 Tauri 心跳监控，总纲 §12.1）；core 内所有外部网络调用强制超时（适配层/数据层统一实施）；**退出顺序 = 停 tick → 等结算事务落盘（spec-01 §3.8）→ 关 core**（systemd ExecStop 优雅停机钩子执行同一顺序）——与备份安全点（spec-02 §10）共用"结算中"标志。

## 10. 测试计划

- **调度**：到点触发/幂等键防重复生成/状态机迁移用例（含 partial）/崩溃恢复（模拟 kill → 重跑零重复、checkpoint 恢复）/交易日历跳过非交易日（全年日历）/中断回调注册（单 tick 无竞态）；
- **启动序列（v0.3）**：六步顺序/快进回放逐日编排（跨多日）/时效任务 skipped 判定/盘中补救启动触发条件/可延迟组入队与备份立即执行/**生命周期事件触发任务生成（归档→经验提取、checkpoint→验证评估、创建→试运行回放）**；
- **审批**：四类型流转用例/确定性短路命中与未命中/频率配额拦截与**冷却期 UI 人工放行**/**interrupt 超时 → 审批单自动关闭 + 迟到决定留痕不生效**/悬空合并提醒/审批单过期/**launch 上线确认单流转（decided_by=user）**；
- **收尾节点（v0.3）**：sediment 失败 → partial + 重试幂等（dedup_key 零重复）+ 连续失败告警；
- **执行预算（v0.6）**：超限强制收尾（partial + "预算截断"标注）/未超限不受影响/tool_calls 落表正确；
- **launch 复位联动（v0.6）**：确认 → 复位 → 上线首日任务行生成；复位失败阻断告警；
- **启动序列（v0.6）**：时钟偏差自检（模拟偏差 >60s 告警不阻断）；dedup_key 入键后同日多事件共存（同 type 双 checkpoint 各自成行）；
- **日报**：数据段+叙述段合并渲染/**前置依赖结算检查 → absent 降级**/字段完整性校验拦截/版本递增补发留痕/体检报告字段完整性（净值/验证进度/费用月报）；
- **推送**：消息路由表各流转/白名单拦截转审阅（联动 spec-02）/事件→通道映射/限额聚合触发（模拟达阈值）/站内通知（web）兜底/failed 回写/**数据质量告警直达路径**；
- **闸门**：同源 scan 互斥排队/LLM 调用 10% 余量动态投递与排队（模拟余量跌破/回升）/30% 新任务线暂停与恢复（与 10% 分层语义）/**备份豁免 30% 线**/退避+抖动+重试预算/源级隔离；
- **路由**：任务类型映射正确性/健康检测剔除/手动覆盖优先/预算仅提示（不降档）；
- **降级**：管理 Agent 健康检查连续失败 → 安全自治 → 恢复补跑全链路（确定性即时、LLM 延迟）。

## 11. §16 勾销表（本文件状态）

| # | 事项 | 状态 |
|---|---|---|
| 12 | 月度《策略体检报告》模板、内容字段与推送（净值/概念验证进度/健康度） | 已写入 §5.5（生成与推送归本规格；UI 展示归 spec-06；概念验证进度数据由 spec-05 供给） |
| 36 | 推送通道限额感知与聚合（**clawbot 限额按服务条款配置**/企微 20 条每分钟、合并单条、站内通知（web）兜底、限额配置化；v0.5 随总纲 v0.17.7 通道名同步；v0.7 随总纲 v0.20 兜底通道改站内 web） | 已写入 §6.3（P4 随外部通道实现；P1-P3 站内通知（web）单通道无配额问题） |

## 12. 评审确认记录

### 第一轮（2026-09-05 用户拍板，v0.2 落实）

1. **审批单存储模型：A**——审批单独立表 approval_requests + task_schedule.approval_id 引用；总纲 §10.1 措辞同步 **v0.17.4**（§4.1）；
2. **可延迟组空闲窗口判定**：非交易时段 ∧ 无 running 任务 ∧ 本地可用资源 ≥30% 余量线；备份例外开机即补（§2.5）；
3. **月度体检报告推送时点**：次月首个交易日 20:00 与总汇报同批（§5.5）；
4. **两个默认时长**：interrupt 审批超时默认 30 分钟（#58 建议值）+ 审批单 pending 默认 24h 过期（§3.3/§4.4）；
5. **LLM 路由排期**：P1 固定单模型（用户设置）→ 路由 v1 启发式 P2 → 效果评分 v2 P4（§8.2）；
6. **资源闸门与配额初值（含用户修订）**：**LLM provider 不设固定并发上限——按性能评估动态投递，保持本地剩余 ≥10% 性能余量（CPU/内存可用率双阈值，可配），低于线排队而非失败**（替代原"并发上限默认 2"）；任务重试预算默认 3 次/任务；申请频率配额（每类型 pending ≤3 件、驳回后 24h 冷却）（§7.2/§2.3/§4.2）。

### 第二轮（2026-09-05 用户拍板，v0.3 落实）

1. **总纲 v0.17.5 同步**：§4.5/决策表 #37 改"LLM 调用投递按本地性能余量（≥10%）动态评估排队而非失败，不设固定并发上限"（§7.2）；
2. **interrupt 迟到决定规则（拍板 A2）**：超时保守拒绝 → **自动关闭对应审批单**（expired + close_note"图分支已超时拒绝"）；**迟到决定仅留痕、不生效**，UI 提示重新申请（§3.3/§4.4）；
3. **启动序列（§2.6 新增）**：锁→全年日历→任务行→快进回放→时效判定→可延迟组→tick 六步；
4. **interrupt 扫描机制改写**：interrupt_entered_ts 记录 + 调度器扫描 + Command(resume) 注入保守拒绝，删"图内计时器"（§3.3/§2.1）；
5. **sediment 失败语义**：partial + 自动重试（dedup_key 幂等）+ 连续 3 次告警（§3.2）；
6. **tick 归属**：单 tick 循环 + 引擎/数据到期检查回调注册，禁双循环竞态（§2.2）；
7. **其余按建议落实**：daily_reports 改 UNIQUE(agent_id, trade_date, version)（§5.2）；可延迟组让路语义=不中断 running、仅停投新（§2.5）；冷却期直接退回 + **UI 人工放行豁免**（拍板 B2，替代"实质变化"判定，§4.2）；pending 满额提示待决单入口（§4.2）；日报前置依赖结算检查→absent 降级（§5.3）；管理 Agent 心跳载体=周期健康检查任务连续 N 次 failed（§9.1）；备份豁免 30% 线（§7.2）；任务生成用全年交易日历（§2.1）；消息路由表 §6.0 新增；事件枚举补数据质量告警（§6.2）；P 阶段实施对照（§13）。

### 第三轮（2026-09-06，夜间优化轮·用户追认后定稿，落实于 v0.6）

1. **任务级执行预算**（§3.2 新增，总纲 #60）：max_llm_rounds=30 / max_tool_calls=60 超限强制收尾、tool_calls 落表（spec-02 §11 联动）——补齐"输入预算（装配）+过程预算（轮次/工具）"双层防失控；
2. **启动序列补系统时钟偏差自检**（§2.6：网络时间对比 >60s 告警）——created_at 引擎盖章可信度的前置；
3. **launch→账户复位联动**（§4.1，总纲 #61 / spec-01 §2.8）：复位成功才生成上线首日任务行，失败阻断；
4. **任务表幂等键补 dedup_key**（§2.1）：事件触发任务同日多事件防碰撞；
5. **试运行日报默认不推送**（§6.2，notify_rules 默认值，总纲 #61）；
6. **用户追认（2026-09-06 上午）**：#60 任务级执行预算（30 轮/60 次默认值）与 #61（launch→复位联动、试运行日报不推送）拍板同意——v0.6 定稿。

**剩余待实测项（P1/P2 执行）**：闸门参数实测校准（LLM 投递 10% 阈值与 30% 新任务线的实际负载表现、性能采样开销）；**clawbot 接入实测（P4，入 spec-06 §11 验证清单联动）**；其余无数据依赖项。

**Web 部署版适配（v0.7，随总纲 v0.20，待用户终审）**：本文件"桌面通知/Tauri/sidecar/本地通道/每日开机"表述已全部改写为 **core 常驻 + 站内通知（web）+ systemd 守护**（§6 通知通道、§9.2 单实例与守护、§2.2 tick 归属）；无新增行为决策，业务规则与 v0.6 定稿一致。

## 13. P 阶段实施对照（v0.3 新增，对齐总纲 §14 里程碑）

| 模块（节） | P1 最小闭环 | P2 自动化 | P3 记忆与优化 | P4 产品化 |
|---|---|---|---|---|
| 调度器/任务表/启动序列（§2） | 最简版：tick+到点投递+启动序列+手动触发+备份调度（启动即补） | 完整：审批联动任务生成/补跑/快进回放/可延迟组全量/**验证评估与试运行回放** | 摘要谓词执行（spec-02 记忆系统 P3）/**经验提取与 kb_stats 刷新** | — |
| LangGraph 执行层（§3） | 收尾节点/执行器封装/校验重写/**任务级执行预算（#60）** | interrupt 审批子图/超时扫描/迟到决定关闭 | — | — |
| 审批流（§4） | — | 全量（统一模式/短路/配额/冷却豁免/**launch 上线确认**） | — | — |
| 日报（§5.1-5.4） | 手动日报（数据段+简单叙述） | 自动生成/缺勤/总汇报 | — | — |
| 体检报告（§5.5） | — | — | 最小版（①-④数据段） | 完整+次月首个交易日推送 |
| 消息路由与推送（§6） | 站内通知（web）单通道 | — | — | 邮件/微信 clawbot/企微+限额聚合 |
| 闸门/容量（§7） | 10%/30% 双线基础闸门 | scan 互斥/完整闸门 | 容量建议 | — |
| LLM 路由（§8） | 固定单模型 | v1 启发式（a+b+c） | — | v2 效果评分 |
| 降级与进程保障（§9） | 单实例锁/退出顺序/systemd 守护衔接 | 安全自治全量（健康检查任务） | — | — |

## 变更记录

| 版本 | 日期 | 状态 | 要点 |
|---|---|---|---|
| v0.1 | 2026-09-05 | 初稿待评审 | 调度器与任务表（tick 清单/状态机/崩溃恢复三层幂等/补跑时效分级/可延迟组+备份最高优先级）、LangGraph 执行层（收尾节点=sediment 调用点/interrupt 超时保守拒绝/执行器封装/校验重写）、审批流（统一模式/确定性短路/澄清收敛/过期）、日报体系（数据段+叙述段/缺勤日报/月度体检报告 §5.5）、推送通道（事件映射/限额聚合 P4/桌面兜底）、容量评估与资源闸门（#37 三闸门+30% 余量）、LLM 路由（适配层/路由 v1 P2/v2 P4/重试纪律）、管理 Agent 降级与进程保障；勾销 #12/#36；待评审项 6 条 |
| v0.2 | 2026-09-05 | 第一轮评审拍板落实 | ①审批单独立表（拍板 A，总纲 §10.1 同步 v0.17.4）②空闲窗口判定确认（非交易时段∧无 running∧30% 余量线）③体检报告次月首个交易日 20:00 推送④interrupt 30min+审批单 24h 过期确认⑤LLM 路由排期确认（P1 固定单模型→v1 P2→v2 P4）⑥**LLM 并发改按性能评估动态投递：剩余 ≥10% 性能余量（CPU/内存双阈值可配）才投递、低于线排队**，与 30% 新任务线分层（10%=LLM 调用线、30%=新任务线）；重试预算 3 次/频率配额确认；§12 改评审确认记录+剩余实测项（闸门参数校准） |
| v0.3 | 2026-09-05 | 第二轮评审拍板落实（ZCode 深度评审） | ①**总纲 §4.5/#37 同步 v0.17.5**（LLM 投递按性能余量动态评估，替代固定并发上限——上轮遗漏补齐）②interrupt 超时→**自动关闭审批单**+**迟到决定仅留痕不生效**（A2）③interrupt 扫描机制改写：interrupt_entered_ts+调度器扫描+Command(resume) 注入，删图内计时器（A3）④**§2.6 启动序列**六步（A4）⑤sediment 失败→partial+重试+告警（A5）⑥单 tick 归属+回调注册（A7）⑦daily_reports 版本化 UNIQUE 修正（A6）⑧冷却期直接退回+UI 人工放行（B2 拍板）⑨可延迟组让路语义修正（B1）⑩日报前置依赖结算检查（B3）⑪管理 Agent 心跳载体=周期健康检查任务（B4）⑫备份豁免 30% 线（B5）⑬任务生成用全年日历（B6）⑭pending 满额提示（B7）⑮消息路由表 §6.0+事件枚举补数据质量告警（C）⑯P 阶段实施对照 §13（C）⑰测试计划补 5 类 |
| v0.4 | 2026-09-05 | 联动修订（spec-05 v0.3 评审拍板承接；用户确认，无新增行为规则） | ①§2.1 task_type 枚举补**试运行回放/验证评估/经验提取/kb_stats 刷新**四类；②§2.1 任务生成补**生命周期事件触发**（归档→经验提取、checkpoint→验证评估、创建→试运行回放）；③§2.5 可延迟组补两员：**kb_stats 刷新**（日结算后确定性重算）与**归档经验提取**；④§4.1 审批 type 补 **launch（上线确认单）**——试运行验收通过后提交用户确认，decided_by=user；⑤§6.2 事件枚举补上线确认待办；⑥测试计划补生命周期触发与 launch 流转用例；§13 P 阶段对照同步 |
| v0.5 | 2026-09-05 | 联动修订（总纲 v0.17.7 推送通道名同步；用户拍板） | §6.2/§6.3/§11/§13 通道名同步：个人微信通道 **Server酱 → 微信 clawbot 通知**（接入配置用户自配；限额按其服务条款接入时实测固化、入通道配置不写死——原"每日 5 条"为 Server酱 专属数值作废）；企微 webhook 不变；无机制变更 |
| **v0.6** | 2026-09-06 | 夜间优化轮（**用户追认定稿**） | ①§3.2 **任务级执行预算**（总纲 #60：max_llm_rounds 30 + max_tool_calls 60，超限强制收尾 partial+"预算截断"；与装配预算/资源闸门三层互补）②§2.6 启动序列补**系统时钟偏差自检**（>60s 告警）③§4.1 **launch→账户复位联动**（总纲 #61，spec-01 §2.8；复位失败阻断上线首日任务）④§2.1 幂等键补 dedup_key（事件任务同日多事件防碰撞）⑤§6.2 试运行日报默认不推送（notify_rules 默认值）⑥§13 P 阶段对照/测试计划同步 |
| **v0.7** | 2026-09-06 | Web 部署版适配（**随总纲 v0.20 提交，待用户终审**；业务规则同 v0.6，无新增行为决策） | ①承载改写：sidecar → **core 常驻进程（systemd 守护 + 进程级单实例）**——§1 边界/§2.2 tick 归属/§2.6 启动序列（每日开机→重启）/§9.2 单实例与守护（#41 锁文件+SQLite 单实例标记、#42 Restart=always + 看门狗 + ExecStop 优雅停机）替代 Tauri single-instance 与心跳 ②**默认通知通道 = 站内通知（web）**，替代桌面通知——§6.2 通道实现/§6.3 兜底与 P 阶段说明/§6.4 failed 降级/§8.3 预算提示/§11 勾销/§13 P 阶段 全局替换（#8，总纲 v0.20）③依据与交叉引用版本同步（总纲 v0.20；spec-01 v0.7/spec-02 v0.5/spec-03 v0.6/spec-05 v0.6/spec-06 v0.5） |
