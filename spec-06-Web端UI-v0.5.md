# spec-06 Web 端 UI 规格 v0.5（Web 部署版适配：桌面端 UI → Web 端 UI）

> 版本：v0.5（2026-09-06，Web 部署版适配轮·待用户终审：承载形态由 Tauri 2 桌面壳重构为**浏览器前端 + Nginx 反代 + core 常驻（systemd）**——§1 边界/§2 栈/§3 通信安全/§7 部署承载/§8 部署工程细则/§9 P 阶段对照/§10 测试计划 全面改写；页面模块与设计规范（§4~§6）内容保留，仅替换通道与通知术语（桌面通知→站内通知 web）；v0.4 试运行横幅/复位提示/API Key 加密注保留）
> **状态：Web 部署版适配稿（随总纲 v0.20 一并以 Web 部署版 P1 基线提交，待用户终审定稿）——定稿后 v0.5 替代 v0.4 成为开发基线。**
> 依据：总纲 v0.20（Web 部署版）§11（界面模块/设置清单）、§12.1（部署形态：Nginx 反代 + core 常驻 + systemd）、§12.6（前端栈/进程通信）、§12.7（Web 访问安全）、§12.8 决策表；联动 spec-01 v0.7（引擎数据展示：结算产物/降级清单/quality/回放序列）、spec-02 v0.5（messages/审批引用/备份机制/装配预算展示）、spec-03 v0.6（健康度/L0 完整率/日历告警/采集实时状态展示）、spec-04 v0.7（审批中心数据/推送配置项/日报渲染/体检报告展示/闸门与预算）、spec-05 v0.6（知识库页数据/参考标注规范/概念归并确认/经验提取报告/上线确认单/验证版本展示）
> **受约束决策**：#8（推送渠道设置 UI，站内 web 默认，v0.20 口径） #13（预算仅提示——UI 进度条可视化） #15（卖出跟踪 P3 文字表格/P4 图表） #16（暖白默认/深色切换、数据保留默认永久可配清理） #22（蓝图级约定——预算展示） #33（身份通道绑定——UI 无旁路） #36（用户直控台） #39（降级状态展示） #41（单实例锁，Web 改写：core 进程级） #42（守护/优雅退出，Web 改写：systemd） #56（交易时段进程在线保障，Web 改写） #57（试运行精度差交接展示） #58（interrupt 待办提醒展示）
> **§16 勾销**：#12（UI 展示侧） #16（UI 侧） #22（Web 部署侧，自 spec-01 移交承接） #28（UI 侧） #33（UI 侧）（见文末勾销表）
> 与总纲冲突时以总纲为准并同步本文件。

---

## 1. 模块定位与边界

- **职责**：React 前端全部页面（§11.1 十三模块 + **登录页**）、**登录鉴权与会话 UI（§3）**、**全局设计规范（§5：A 股色彩语义/格式/确认分级/三态等）**、首启引导与空态体系、进程通信与访问安全（登录会话/CSRF/CSP/同源反代）、设置中心（§11.2 全清单）、**站内通知渲染（WebSocket 实时 + 浏览器通知）**、**§16 #22 部署工程细则（Web 部署侧：Nginx/systemd/证书）**；备份/导入导出/数据清理的 **UI 入口与交互**（机制归 spec-02）。
- **不负责**：一切业务逻辑与数据计算（core 侧 spec-01~05——UI 是纯消费方，仅渲染与交互转发）；推送通道实现（spec-04——UI 只提供配置界面、接入引导与事件映射矩阵编辑）；策略优化/知识库/审批的业务规则（spec-05/04——UI 展示与表单提交，规则校验在 core）。浏览器窗口/进程生命周期不由本前端负责（浏览器自带），服务生命周期归部署层 systemd（§7）。
- **消费方（数据契约）**：全部经 Nginx 反代访问 core 的 HTTP API + WebSocket（**前端无任何直连外部数据源/引擎/库的旁路**）；各页面数据源在各节标注。
- **信任边界**：#33——UI 不是信任边界内的 actor：所有敏感操作经 core API 由**执行器校验后落库盖章**；直控台操作不经 LLM 但**必经 core 直控接口**（审计盖章+通知子 Agent）；API Key 在 UI 仅掩码展示，明文只在 core 本地加密存储。

## 2. 技术栈与工程结构（§12.1/§12.6，Web 部署版）

- **承载形态（v0.20 Web 部署版）**：本文件=浏览器 SPA（构建产物由 Nginx 静态托管）；业务由 **core 常驻进程**（FastAPI + WS，127.0.0.1 环回）承载，systemd 保活；Nginx 统一反代 `/api`、`/ws`（同源、无 CORS 面）。**前端不做任何窗口/进程/系统级能力**。
- **前端：React 18 + TypeScript + Vite + Ant Design 5 + ECharts**（v0.2 第一轮拍板确认）；
- **目录结构**：`web/src/{pages,components,store,api}`（本前端）+ `deploy/nginx.conf`、`deploy/ai-agent-trading.service`（服务器侧，§8）；状态管理库 P1 蓝图定；**ECharts 按需引入**（控制包体）；
- **主题**：暖白默认 / 深色可切换 + 字体大小（#16）；观感模仿 ZCode；
- **路由与反代**：浏览器路由 `/login`（登录）与主布局各页；正式/预览部署经 Nginx TLS 反代（域名证书或平台预览 host 通配 `.monkeycode-ai.online` 等）；开发期 Vite dev server 以 `/api` 前缀反代 core。

## 3. 进程通信与访问安全（§12.6/§12.7，Web 部署版）

- **访问通道**：浏览器 → HTTPS/WSS → Nginx（TLS 终止 + 静态托管 + 反代 `/api`、`/ws`）→ core（FastAPI + WebSocket，**监听 127.0.0.1 固定端口**，环回不经 TLS/不经公网）；**同源部署，无 CORS 面**。
- **登录鉴权（Web 部署版）**：**单用户账号**——登录页（用户名+口令）→ core 签发会话 Cookie（**HttpOnly/Secure/SameSite**）+ **CSRF Token**（前端所有写请求携带）；登出/会话过期/设置页"强制下线"；登录失败速率限制与审计。未登录访问任何 `/api` 与页面路由 → 重定向登录页。
- **WS 推送订阅模型**：登录后按主题订阅（消息流/看板刷新/审批待办/闸门状态/健康度/任务状态/采集状态）——事件驱动推送，页面不轮询；
- **断线与一致性（v0.3 补）**：WS 重连成功后**当前页面强制 refetch 全量数据**（防增量事件丢失造成的状态错配）；WS 断开期间顶栏显示"连接中断/重连中"，重连后自动恢复；
- **CSP 与允许清单**：Nginx 下发安全响应头（CSP/HTTPS/HSTS）——禁止第三方远程脚本；前端不直接访问任何外部数据源；
- **core 生命周期（#42，部署层承接）**：core 由 systemd 管理（`Restart=always` + 健康检查端点 + 看门狗），崩溃自动拉起——UI 仅在 WS 重连成功时提示"服务已自动恢复"；退出顺序=**停 tick → 等待结算落盘 → 进程退出**（systemd stop，服务器侧执行，§7）；
- **单实例（#41，core 进程级）**：core 启动获取进程级单实例锁（PID/锁文件 + SQLite 实例标记）——调度与结算在 core 单进程内，浏览器多标签/多设备并发访问同一后端，**天然不会产生双调度器**；UI 无单开逻辑。

## 4. 页面路由与导航布局

- **登录后主布局 = 左侧导航栏 + 顶栏**（模仿 ZCode；v0.2 确认的单窗口形态在 Web 端=单一 SPA 主布局；无浏览器窗口管理/多窗口逻辑）；未登录重定向 `/login`；
- 导航清单（§11.1 十三模块）：对话面板 / Agent 看板 / 用户直控台 / 策略详情 / 交易中心 / 日报中心 / 知识库 / 能力市场 / 大纲管理 / 审批中心 / 性能监控 / 数据监控 / 设置；
- **全局元素（v0.3 扩充）**：顶栏（**连接态**/管理 Agent 降级横幅 #39/闸门状态微标/登录用户与会话管理入口）；**通知历史铃铛（v0.3 新增，B5）**——浏览器通知是瞬时的、错过即失：铃铛=统一通知历史收口（spec-02 messages 事件流），未读徽标、点击跳转对应页面与会话定位；未读计数（会话/审批待办/异常）；
- **导航级快捷键（v0.3）**：页面切换/搜索聚焦（C 类）；**危险操作一律不设快捷键**。

## 5. 全局设计规范（v0.3 新增——A 股习惯与体验基线，全页面强制）

### 5.1 色彩语义（拍板 A1：红涨绿跌）
- **红 = 上涨/盈利/正收益；绿 = 下跌/亏损/负收益**（A 股习惯，全局强制——数字、卡片、表格、ECharts 图表双主题一致；禁止任何页面出现国际惯例的绿涨红跌）；
- 中性语义色：信息蓝/警告黄/危险红（操作类，与涨跌红同色但以图标+文案区分语义）/成功绿（操作类同上区分）；
- **kb 状态徽标色**（§6.7）：valid 绿 / invalid 红 / observing 灰 / sealed 黄。

### 5.2 数字与时间格式（拍板 A6；总纲 §10.1 v0.17.9）
- **金额**：千分位分隔，保留 2 位（盈亏另带正负号与红绿色）；**百分比**：收益率 2 位、完整率/概率 1 位；
- **时间**：**交易数据时间戳（成交/结算/采集/条件单 created_at 等）统一按北京时间显示并带"京"角标**（数据语义不变，仅显示层；总纲 v0.17.9）；**通知到达等非交易类时间用用户本地时区**；会话内消息按日期分隔线分组；
- 数量：股数整数（零股 ≤2 位小数）；费率/比例按 context 位数。

### 5.3 危险操作确认分级（拍板 A2/B8，全局三级）
| 级别 | 适用 | 交互 |
|---|---|---|
| L3 输入确认 | **紧急清仓**、数据清理（删除类） | 二次确认对话框 + **要求键入关键词（"清仓"/"清理"）**才可执行 |
| L2 单次确认 | 冻结买入/冻结证券/解除冻结、删除知识库条目 | 确认对话框（展示影响范围） |
| L1 普通确认 | 设置保存/表单提交等 | 常规按钮反馈 |
- 全部危险操作：执行后**审计回执展示**（何时/何操作/影响范围）+ 相关 Agent 收到通知的提示。

### 5.4 三态规范（空态/加载/错误）
- **空态**：每页定义空态文案+行动指引（如看板空态→"创建第一个 Agent"引导按钮）；
- **加载**：骨架屏（列表/卡片）/图表 loading；**错误**：可重试错误显示重试按钮（联动 WS/HTTP 重试），不可恢复错误引导设置页自检；
- **首次启动引导流（拍板 A3，P1）**：欢迎页 → provider API Key 配置 → 数据源自检（日历/日线可达性，spec-03 calendar/daily）→ **引导创建第一个 Agent**（跳转对话面板需求澄清流）→ 进入看板；各页空态与引导流衔接（跳转目标明确）。

### 5.5 图表规范
- ECharts **暖白/深色双主题**（与全局主题联动，§5.1 语义色一致）；
- 资金曲线类：默认区间切换（近 1 月/3 月/全部）+ tooltip 十字准线；
- 长序列降采样（前端聚合，防数千点卡顿）。

### 5.6 性能与可用性
- **长列表虚拟滚动**（trades/messages 数千行场景）；
- **响应式与缩放适配**：主布局面向桌面浏览器（≥1280px 全量），窄屏/平板降级为可用布局（侧栏可折叠）；浏览器缩放 125%/150% 抽查无错位（Web 端以 CSS 响应式替代原桌面高 DPI 项）；
- 键盘：导航级快捷键（§4）；表单 Enter 提交/Esc 取消——危险操作除外（§5.3）。

## 6. 页面模块规格（§11.1 逐模块）

### 6.1 对话面板（联系人式多会话）
- 会话列表：管理 Agent + 每个子 Agent 独立会话（spec-02 conversations）；**未读计数+进入会话滚动锚定到第一条未读**；**站内/浏览器通知点击=跳转会话并定位该消息**；
- 消息渲染：气泡流（Markdown/表格/回执卡片）；**日报消息=摘要行+折叠**（默认显示标题/核心数字行，点击展开全文或"在日报中心打开"——防长内容淹没会话，spec-04 merged_markdown）；absent 日报显式标识；消息按日期分隔线分组、历史上翻分页加载；
- **任务态（v0.3 拍板 A4）**：Agent 任务进行中时，会话顶部显示**状态条**（"选股任务运行中…/结算中（重试窗口 32 分钟）/日报生成中"——绑定 task_schedule running/结算重试，WS 推送刷新）；
- **异步回执链（v0.3 拍板 A4）**：用户提问发出后显示状态标记流转：已发送 → 排队（闸门等待）→ 处理中 → 已回复（回复消息锚定）——防"发了没反应"的焦虑；
- 发送与白名单：子 Agent 回复受白名单（spec-02 §6.2）——被拦消息显示"已转管理 Agent 审阅"（pending_review）；
- 数据源：spec-02 messages/conversations + spec-04 task_schedule（状态条）+ WS。

### 6.2 Agent 看板
- 卡片墙：名称/生命周期状态标签（§3.5）/**今日状态（交易中/已结算/未结算——与未结算角标联动）**/今日盈亏/累计盈亏/持仓数/当日动作数；**冻结状态黄标**（冻结买入/冻结证券时显示，A2）；
- 排序默认：异常/待办优先 → 运行中 → 其余（可切换盈亏/名称）；
- 卡片点击 → 策略详情页；新建 Agent 入口；**空态=创建引导按钮（§5.4）**；
- 数据源：accounts/holdings 汇总（spec-01）+ WS 刷新。

### 6.3 用户直控台（#36；v0.3 拍板 A2 闭环）
- 操作：全局/单 Agent **紧急清仓**、**冻结买入**（保留卖出与风控）、**冻结证券**；**解除冻结/恢复（v0.3 补——冻结项列表+逐项解除入口）**；
- **确认分级**（§5.3）：清仓=L3 输入确认；冻结/解除=L2 单次确认（展示影响范围：涉及持仓/挂单）；
- 即时生效（直接操作引擎层，秒级、不经 LLM）；
- **清仓进度展示（v0.3 补）**：执行后显示生成的卖出条件单清单与成交进度（联动 6.5）；
- 反馈：审计回执展示；干预自动通知子 Agent 纳入复盘（消息出现在对话面板）；
- **状态可见性（v0.3 补）**：冻结生效后——Agent 看板卡片黄标（6.2）、策略详情页横幅标识、直控台冻结项列表常驻；
- 数据源：spec-01 直控接口。

### 6.4 策略详情页
- 区块：策略理念（锁定标识）与双层结构 / 能力包 / 模拟账户（**资金曲线 ECharts：区间切换+沪深300 基准对比线（v0.3 拍板 B4），份额法口径 spec-01 §6.1**）/ **指标摘要卡（v0.3 补 B4：累计收益率/最大回撤/信号胜率——份额法+信号注册表口径）** / 持仓明细（**"未结算"角标（#33）**；**持仓-条件单关联视图（v0.3 拍板 B7）：每行展开/悬停显示该票已挂保护单与今日成交——持仓安全感视图**）/ 条件单列表 / 交易流水 / 日报历史 / **策略演进（版本链：version_no/验证期标记/通过失败/failure_reason——验证子仓业绩独立展示，spec-05 §4.2）** / **卖出跟踪列表（P3 文字+表格，图表 P4，#15）** / 冻结状态横幅（若被冻结，6.3 联动）；
- 试运行态展示：试运行进度与《验收报告》入口（spec-05 §6）；
- **试运行横幅（v0.4，总纲 #61）**：账户 status=trial 时页面顶部显示"试运行中（历史回放，L2 近似撮合）"横幅（提示该阶段业绩为 L2 近似口径）；上线复位（spec-01 §2.8）后横幅消失；
- 数据源：spec-01 holdings/trades/condition_orders/settlement_log/signal_registry + spec-02 memory_entries(type=strategy) + spec-04 daily_reports + spec-03 index_quotes（基准线）。

### 6.5 交易中心
- 全局视图：订单/成交/条件单（跨 Agent 汇总+筛选）；结算日志（settlement_log：**granularity_used 按票档位清单、降级清单、quality 标记（按 spec-03 §7 字典渲染）**）；
- **回放可视化（v0.3 拍板 B1，P3 实施）**：条件单详情页展示**当日价格序列缩略图（该票实际档 L0/L1/L2）+ 触达点标记 + 成交价线**——"这单为什么在 10:32 成交"一眼可见，是 EOD-Replay"成交于真实触达时刻"（spec-01）的可信度证明；数据取 spec-03 get_replay_series（close_candidates 不入图）；
- 数据源：spec-01 §2.5 表 + spec-03 l0_coverage/get_replay_series。

### 6.6 日报中心
- 时间线（按 Agent/日期筛选）；absent/resend 版本切换查看；导出单篇；
- **"报告"标签页（v0.3 拍板 B6）**：月度《策略体检报告》全文阅读（spec-04 §5.5 生成产物，按月归档）——报告在对话面板只推摘要，全文在此；
- 数据源：spec-04 daily_reports + 体检报告。

### 6.7 知识库页
- 列表：**正向/反向类型筛选 + 状态筛选（observing/validating/valid/invalid/sealed）**；失效名单区；
- 详情：描述/computable_spec/统计快照（分环境桶表格：样本/胜率/期望值含费）/**避坑条目：被拦截/破例两列分开统计（#28）**/适用环境桶/**预计可验证时间**/来源与评审记录（双闸）/**状态机时间线（v0.3 补：何时观察/晋升/失效——证据变化可读）**；
- **概念归并确认队列（v0.3 拍板 A5，spec-05 §3.11 落点）**：管理 Agent 月度归并提议（自由标签→规范条目）在此展示，**用户/管理 Agent 确认后生效**（写 kb_tag_aliases）——含预览影响（将归并的信号样本数）；
- **经验提取报告（v0.3 拍板 A5，spec-05 §5 落点）**：归档 Agent 的终局归因报告在此查看（按 Agent 归档记录索引），报告结论回填的条目带"来源：经验提取"追溯链接；
- **"参考 KB-xxx（状态）"标注渲染规范（#16 UI 侧）**：徽标组件（KB-id+状态色 §5.1），点击跳转条目详情；
- 录入：用户手动录入（表单）→ 观察态+双闸状态跟踪展示；
- 数据源：spec-05 kb_entries/kb_stats/kb_tag_aliases + spec-04 体检报告②。

### 6.8 能力市场
- 列表：能力（类型/版本/**沙箱状态**/来源与 License/维护者）；上架四门槛状态展示；**沙箱报告查看**（sandbox_report_ref）；
- 详情：元数据/输入输出/绑定清单；绑定/解绑/回滚操作（走 core API 审计）；
- 触发入口：检索开源/生成 Skill（总纲 §4.3）；
- 数据源：spec-05 capabilities/capability_bindings。

### 6.9 大纲管理
- 查看（十节结构化渲染）/ **版本对比（diff 视图）** / 变更历史 / charter_version 当前值；**最新变更摘要展示**（spec-02 charter_summary）；
- 编辑入口（管理 Agent 生成/用户授权修改理念）；风控/任务清单变更提示"需审批生效"（spec-04 §4）；
- 数据源：spec-05 charter_versions。

### 6.10 审批中心
- 待办列表：type 筛选（task/granularity/capability/risk/**launch**）/状态筛选；申请详情（理由/资源预估/哈希去重提示）；
- **launch 上线确认单**：展示《验收报告》（三层验收逐项）→ 用户确认/拒绝（decided_by=user）；
- **上线复位提示（v0.4，总纲 #61）**：确认对话框明示"确认后账户将复位至 10 万初始资金，试运行数据归档留证"（L2 级单次确认，§5.3）；复位失败显示阻断告警（spec-04 §4.1）；
- 状态展示：interrupt 悬空合并提醒、**冷却期人工放行按钮**（spec-04 §4.2）、**迟到决定留痕标识**、过期提醒；推送内容提示"请回电脑确认"（无远程审批，限制写明）；
- 数据源：spec-04 approval_requests/task_schedule。

### 6.11 性能监控
- 图表：**费用三维（元/天 × Agent × 任务类型）**/耗时分布/token 与缓存命中率（cached_tokens 占比）；
- **预算进度条（v0.3 拍板 B2）**：本月累计费用 vs 预算参考值（spec-04 §8.3），超限黄色/红色渐近——与"仅提示"决策一致的**可视化**；
- 容量建议卡/高耗任务列表/闸门状态（**10%/30% 线当前余量实时值**）；
- 数据源：spec-02 performance_records + spec-04 闸门与预算接口。

### 6.12 数据监控
- **今日采集状态卡（v0.3 拍板 B3，首屏）**：采集器当前状态（采集中/中断/已收盘）/今日实时覆盖率（在采时滚动刷新）/停牌剔除数/延续采样状态——盘中一眼确认"L0 活着没"；
- 源健康度（A/B/C、成功率、延迟 P50/P95）/当前 source_binding/故障切换历史/L0 完整率（按票）/quality 事件流/交易日历校验告警/采集清单与观察集合；
- 数据源：spec-03 §9/§3/l0_coverage/source_binding/audit。

### 6.13 设置（§11.2 全清单）
| 组 | 项 | 生效语义（v0.3 标注） |
|---|---|---|
| 外观 | 主题（暖白默认/深色）、字体大小 | 即时 |
| 模型 | 自动/手动开关；自动路由规则（映射/预算参考值/健康周期）；手动覆盖；API Key 管理（掩码+core 加密——实现选型=本机密钥文件 0600 + Fernet/AES-GCM 或系统 keyring，Linux，P1 蓝图定，v0.4/v0.5） | 即时（进行中任务不中断） |
| Agent 通用 | 新建默认参数（资金/风控模板/任务时点/并发提醒） | 仅作用于新建 |
| 数据源 | 免费源顺序/开关、收费源配置、缓存目录 | **次日生效**（采集与判定换源，spec-03 单源同日） |
| 交易 | 默认撮合粒度、费率、涨跌停/ST 拦截、流动性比例、卖出跟踪天数 | 粒度变更需审批；其余次日 |
| **推送** | **站内通知（默认：浏览器实时推送 + 浏览器系统通知）**；**微信双通道**：**微信 ClawBot**（OpenClaw 插件：**接入配置=OpenClaw 网关地址+令牌**；**接入引导卡**——①在用户本机执行安装命令 `npx -y @tencent-weixin/openclaw-weixin-cli@latest install`（复制按钮）②微信扫码绑定步骤 ③回填网关地址/令牌 ④**测试推送按钮**；**通道状态**：未安装/未绑定/就绪）+ 企微群机器人 webhook；邮件 SMTP；**事件→通道映射矩阵**（spec-04 notify_rules）；"只收摘要/只看异常/全部原文" | 即时（映射变更次条消息生效） |
| Skill/MCP | 能力包管理入口；记忆设置（向量开关、保留策略） | 即时/按说明 |
| **备份与恢复** | 手动导出/导入；自动备份计划（每日/每周、保留 N 份）；备份内容清单；导出加密口令（可选）——机制归 spec-02，本页仅 UI | 即时（备份计划次窗口生效） |
| **数据保留与清理** | 默认永久保留；可配置清理（日志天数/旧流水/市场观察归档）——**清理=§5.3 L3 输入确认**；市场观察滚动删除保留期（spec-02 §4.3） | 次日 |
| **账户与安全（Web 部署版，v0.5）** | 修改登录口令、会话管理（当前登录设备/强制下线）；HTTPS 与证书状态展示（spec-04/spec-06 §8） | 修改口令即时生效，其余会话 |
| 系统 | 数据目录、日志级别 | 部分需重启（标注）。**开机自启/崩溃自拉起=部署层 systemd，非 UI 设置（§7/§8）** |

- **微信 ClawBot 通道边界（v0.2 写死）**：**仅承载出站推送**——微信侧入站消息不属于本系统；发送失败自动降级**站内通知（web）兜底**（spec-04 §6.3）。

## 7. 部署承载与进程可靠（服务器侧，#41/#42/#56，Web 部署版）

> Web 部署版把原桌面壳的系统级能力上收至**部署层（core + systemd + Nginx）**，本前端只消费其结果并做状态呈现：

- **单实例锁（#41）**：core 进程级单实例锁（PID/锁文件 + SQLite 实例标记；systemd 单实例拉起）——双进程=双调度器风险由部署层杜绝（spec-04 §9.2），浏览器侧无单开逻辑；
- **core 守护（#42）**：systemd 管理 core 生命周期（`Restart=always` + 健康检查端点 + 看门狗）崩溃自动拉起（#39 全落库，重启安全）；外部网络调用强制超时在 core 侧执行（spec-04 §9.2）；UI 连接态展示与"服务已自动恢复"提示见 §3；
- **交易时段进程在线保障（#56，Web 改写）**：Linux 服务器常驻、无电源休眠——systemd 保活 + 健康检查 + 异常自拉起 + 停机/重启自动恢复补跑——**P1 验收：交易时段闲置 40 分钟不掉采集（进程持续在线实测口径，联动 spec-03）**；
- **站内通知渲染（默认通道 web）**：经 WS 实时推送渲染；浏览器 Notification API（用户授权后）系统级通知=标题 Agent 名/事件类型、正文=摘要、**点击跳转对应页面并定位**（会话消息/审批单/日报）；通知历史铃铛收口（§4）；格式模板入 spec-04 §6.2 web 通道；
- **优雅退出（服务器侧）**：systemd stop 按序退出"停 tick → 等待结算落盘 → 进程退出"——运维操作，UI 经 WS 感知连接断开并提示"系统维护中/已安全退出"。

## 8. §16 #22 部署工程细则（Web 部署侧承接，自 spec-01 移交；v0.20 Web 部署版改写原"Windows 工程细则"）

- **全程 UTF-8**：含中文路径/文件名（服务器数据目录如 `/opt/ai-agent-trading` 下中文子目录亦可）——core 以 `PYTHONUTF8=1` 启动、文件 I/O 显式 encoding、SQLite/日志/导出全 UTF-8；部署脚本与 systemd 单元注释全 UTF-8；
- **systemd 单元**（`deploy/ai-agent-trading.service`）：core 以 venv 运行（或 PyInstaller 单文件可执行形态）；`Restart=always` + 健康检查端点轮询 + 看门狗；`ExecStop` 优雅停机（停 tick→结算落盘→退出）；`Environment=PYTHONUTF8=1`；日志经 journald 并接 logrotate；
- **Nginx 反代**（`deploy/nginx.conf`）：`/` 托管 web 构建产物；`/api/*`、`/ws` → `127.0.0.1:<core_port>`；TLS 终止（Let's Encrypt 或用户自备证书）+ HSTS/安全响应头/CSP；客户端上传（导入备份/图片）请求体大小与超时限制；限速与登录防爆破联动（core 侧速率限制）；
- **目录与运行账号**：core 以非 root 专用账号运行，数据目录权限收敛（含 API Key 密钥文件 0600）；备份/日志目录仅该账号与运维可写；
- **可移植性**：前端/服务/部署三件独立版本化；升级=停服务→替换→迁移 schema 前自动快照→启动自检（spec-02 备份一致性）。

## 9. P 阶段实施对照（对齐总纲 §14；P1 列经 v0.2 拍板确认）

| 模块（节） | P1 最小闭环 | P2 自动化 | P3 记忆与优化 | P4 产品化 |
|---|---|---|---|---|
| **全局设计规范（§5）** | **P1 起强制执行**（红涨绿跌/确认分级/三态/格式/引导流） | — | — | — |
| 承载与访问（§2/§3/§7/§8） | Web 前端 + 登录鉴权 + **站内通知（web）基础** + core 生命周期(systemd) + 进程在线保障(#56) + 优雅退出；**#22 部署细则（Nginx/systemd）P1 起** | 多标签/多设备会话管理完善、通知历史全量 | — | — |
| 对话面板（§6.1） | 管理 Agent 对话+单子 Agent 会话+手动日报渲染+**回执链与任务态** | 全量（白名单状态/折叠） | — | — |
| Agent 看板（§6.2） | 基础卡片+空态引导 | 全量（状态标签/冻结黄标） | — | — |
| 策略详情（§6.4） | 持仓/流水/条件单+未结算角标+**试运行横幅（v0.4）** | 演进/验证版本+**指标卡与基准线** | 卖出跟踪列表 | 图表（#15） |
| 交易中心（§6.5） | 基础版 | 完整（quality 字典渲染） | **回放可视化** | — |
| 日报中心（§6.6） | 基础版 | 完整 | — | **"报告"标签页（体检报告全文）** |
| 知识库（§6.7） | — | — | **全量（标注渲染 #16/避坑统计 #28/归并确认队列/经验提取报告/状态机时间线）** | — |
| 能力市场/大纲（§6.8/§6.9） | — | 全量 | — | — |
| 审批中心（§6.10） | — | 全量（launch/冷却放行） | — | — |
| 性能监控（§6.11） | 基础闸门状态 | 完整+**预算进度条** | 容量建议 | — |
| 数据监控（§6.12） | — | 完整+**采集状态卡** | — | — |
| 设置（§6.13） | 最小（模型/外观/**账户与安全+登录**/系统） | 交易/数据源 | 推送（邮件/通道配置完善；站内默认已在 P1） | **全量（ClawBot/企微/邮件+备份迁移 UI+清理）** |
| 直控台（§6.3） | **P1 即做**（含解除恢复与确认分级） | — | — | — |
| 首启引导（§5.4） | **P1 即做** | — | — | — |
| #22 部署细则（§8） | P1 起（承载前置） | — | — | — |

## 10. 测试计划

- **设计规范**：**红涨绿跌全局快照测试**（数字/卡片/图表双主题）/时间"京"标注/确认分级（清仓 L3 输入确认、冻结 L2、解除流转）/三态（空态引导跳转/错误重试）；
- **通信与访问**：登录/会话 Cookie+CSRF 校验/未登录重定向/WS 断线重连/**重连后当前页 refetch**/systemd 崩溃自拉起后 UI 恢复提示/优雅退出顺序（部署侧）；
- **直控台**：清仓输入确认拦截误触/操作秒级生效/审计回执/**解除冻结恢复与状态黄标联动**/清仓进度展示/子 Agent 收到干预通知；
- **试运行态（v0.4）**：trial 横幅显示与消失（联动 spec-01 §2.8 复位）/launch 确认对话框复位提示文案/复位失败阻断告警展示；
- **对话**：**回执链四态流转**/**任务状态条（结算重试倒计时）**/日报折叠展开/通知点击定位消息/未读锚定/日期分组分页；
- **渲染**：日报合并渲染/未结算角标时点/quality 字典渲染/degraded 不传染；
- **知识库**：参考标注徽标快照（四态色）/避坑两列统计/**归并确认队列流转（确认→kb_tag_aliases 生效→统计重算）**/经验提取报告查看/状态机时间线/预计可验证时间；
- **审批**：launch 验收报告确认流转/冷却期放行/迟到留痕标识；
- **图表**：资金曲线区间切换/**基准对比线计算正确性**/指标摘要卡口径（份额法+信号统计）/**回放可视化（触达点标记与成交价线）**；
- **监控**：**预算进度条（超限渐近色）**/**采集状态卡（模拟中断/恢复）**；
- **设置**：API Key 掩码加密/口令修改与强制下线/事件→通道映射生效/**ClawBot 接入引导三态+测试推送**/清理 L3 确认/生效语义标注；
- **主题**：暖白/深色持久化+对比度抽查；
- **#22 部署细则**：中文路径全链路 UTF-8/反代路由与 HTTPS 证书/上传导入大小限制/非 root 账号与密钥文件权限/升级迁移演练；
- **进程在线保障（联动 spec-03）**：交易时段闲置 40 分钟不掉采集（#56，Web 部署版：systemd 保活 + 采集在线实测口径）。

## 11. §16 勾销表（本文件状态）

| # | 事项 | 状态 |
|---|---|---|
| 12 | 月度《策略体检报告》（spec-04/06） | UI 展示侧已写入 §6.11/§6.4/§6.6"报告"标签页（生成与推送归 spec-04 v0.7 §5.5）——**两侧齐备，本项关闭** |
| 16 | 知识库"参考非指令"落地（spec-05/06） | UI 侧已写入 §6.7（"参考 KB-xxx（状态）"徽标渲染规范）——spec-05 §3.5+本节，**两侧齐备，本项关闭** |
| 22 | 部署工程细则（spec-01+Web 部署侧，v0.20 改写原 Windows 细则） | Web 部署侧已改写 §8（自 spec-01 移交承接：UTF-8/中文路径/systemd+Nginx/证书/上传边界/权限收敛）——**本项关闭** |
| 28 | 反向避坑库细则（spec-05/06） | UI 侧已写入 §6.7（被拦截/破例分开统计展示）——spec-05 §3.6+本节，**两侧齐备，本项关闭** |
| 33 | 结算窗口与精度差标注（UI/验收） | UI 侧已写入 §6.4（"未结算"角标）；验收侧 spec-05 §6.3 已写——**两侧齐备，本项关闭** |

## 12. 验证任务清单（P4 及接入期执行）

| 任务 | 归口期 | 结果固化位置 |
|---|---|---|
| **微信 ClawBot（OpenClaw 插件）接入实测**：OpenClaw 网关消息发送 API（鉴权/目标会话指定/消息格式与长度）/扫码绑定流程与引导可达性/推送到达率/限额条款——固化通道适配器与限额配置 | P4 首批 | spec-04 §6.2/§6.3 |
| 部署与访问实测：Nginx 反代路由与 HTTPS 证书/登录速率限制与爆破防护/多标签与多设备并发会话 | P1-P2 | §3/§8 |
| 升级与迁移演练：停服升级→迁移 schema 前自动快照→启动自检；备份包导入恢复全流程 | P2-P4 | §8（spec-02 备份一致性） |
| 主题对比度（暖白/深色全页面抽查，WCAG AA 参考） | P4 | §5 |
| 交易时段进程在线 40 分钟验收（systemd 保活 + 采集在线口径，联动 spec-03 P1 验收） | P1 | §7 |

## 13. 评审确认记录

### 第一轮（2026-09-06 用户拍板，v0.2 落实）

1. **组件库与图表库**：**Ant Design 5 + ECharts** 确认（§2）；
2. **窗口形态**：**单窗口+左侧导航**（模仿 ZCode），不做多窗口/浮窗（§4）；
3. **P1 最小 UI 范围**：桌面壳+对话面板（管理 Agent+单子 Agent）+Agent 看板+策略详情（持仓/流水/角标）+日报中心+**直控台**+设置最小+桌面通知+防睡眠——对齐总纲 §14 P1 交付（§9 P 阶段对照 P1 列为准）；
4. **clawbot 接入形态（用户提供官方接入说明后写死，总纲 §10.4 同步 v0.17.8）**：**微信 ClawBot = OpenClaw 微信插件**（腾讯微信官方 `@tencent-weixin/openclaw-weixin-cli`）——前置：设备执行 `npx -y @tencent-weixin/openclaw-weixin-cli@latest install` 安装插件，**微信扫描展示的二维码**完成绑定；系统侧经 **OpenClaw 本地网关**推送（设置页配置：网关地址+令牌）；**仅承载出站推送**（微信侧入站不在本系统范围）；UI=接入引导卡（安装命令复制+扫码步骤+配置回填+测试推送按钮）+通道三态（未安装/未绑定/就绪）；OpenClaw 网关消息发送 API/到达率/限额条款 P4 实测固化（§12 验证清单）。

### 第二轮（2026-09-06 用户拍板，v0.3 落实——UX 轮）

1. **红涨绿跌全局色彩语义（A1）**：红=涨/盈利、绿=跌/亏损，全局强制（数字/卡片/表格/图表双主题）（§5.1）；
2. **直控台功能闭环（A2）**：补解除冻结/恢复入口+冻结状态黄标（看板/详情横幅）+清仓进度展示；**确认分级三级**（清仓=输入确认 L3/冻结=单次确认 L2/普通 L1，§5.3）；
3. **首启引导流+空态规范进 P1（A3）**：欢迎→API Key→数据源自检→引导建 Agent→看板；各页空态行动指引（§5.4）；
4. **对话面板过程感（A4）**：异步回执链（已发送→排队→处理中→已回复）+任务状态条（绑定 task_schedule）+日报折叠（摘要行+展开/跳转）+通知点击定位+未读锚定（§6.1）；
5. **知识库页补两个 spec-05 落点（A5）**：概念归并确认队列（§3.11 提议在此确认）+经验提取报告查看（含来源追溯）+状态机时间线（§6.7）；
6. **时间显示规则（A6，总纲 §10.1 同步 v0.17.9）**：交易数据时间戳统一北京时间显示+“京”标注，通知到达等非交易时间用本地时区（§5.2）；
7. **B 类排期**：回放可视化 P3（B1，条件单详情=当日序列+触达点标记+成交价线）；预算进度条（B2，性能监控）；今日采集状态卡（B3，数据监控首屏）；资金曲线区间+基准对比+指标摘要卡（B4）；通知历史铃铛收口（B5）；体检报告"报告"标签页（B6）；持仓-条件单关联视图（B7）；危险操作确认分级全局规范（B8，并入 §5.3）；
8. **C 类随 P1 蓝图带过**：三态规范/ECharts 双主题/虚拟滚动/WS 重连 refetch/高 DPI/数字格式/生效语义标注/导航快捷键（§4-§6 分散落点）。

### 第三轮（2026-09-06，夜间优化轮·用户追认后定稿，落实于 v0.4）

1. **策略详情页试运行横幅**（trial 态呈现 + 复位后消失，联动 spec-01 §2.8）；
2. **launch 上线确认的账户复位提示**（确认对话框明示复位至 10 万与试运行归档，L2 级确认）；
3. **API Key 加密实现注**（Windows DPAPI 或本机密钥文件，P1 蓝图定稿）；
4. P 阶段对照与测试计划同步；
5. **用户追认（2026-09-06 上午）**：随总纲 v0.18 四项拍板一并定稿（v0.4 为开发基线）。

### 第四轮（2026-09-06，Web 部署版适配，落实于 v0.5）

1. **承载形态重构**：Tauri 2 桌面壳 → **浏览器 SPA + Nginx 反代 + core 常驻（systemd）**——§1/§2/§3/§7/§8 全面改写（登录鉴权/同源反代/进程级单实例/部署工程细则）；
2. **默认通知通道**：桌面通知 → **站内通知（web：WebSocket 实时 + 浏览器通知 + 通知历史铃铛）**，全局替换术语（§1/§4/§6.1/§6.13/§7/§9/§10）；
3. **新增账户与安全设置组**（改口令/会话管理/强制下线，§6.13）；#22 改写为部署工程细则（UTF-8/systemd/Nginx/证书/权限收敛，§8）；
4. **#56 Web 改写**：交易时段进程在线保障（systemd 保活）替代防睡眠，验收口径不变（§7/§10/§12）；
5. 页面模块与设计规范（§4~§6）内容规则不变，仅替换通道/通知术语；P 阶段对照与测试计划同步。

**剩余待实测项**：微信 ClawBot 网关 API 与限额（P4）；部署与访问实测/升级迁移演练/交易时段进程在线 40 分钟（§12 各归口期）；主题对比度（P4）；其余无数据依赖项。

## 变更记录

| 版本 | 日期 | 状态 | 要点 |
|---|---|---|---|
| v0.1 | 2026-09-05 | 初稿待评审 | Tauri 2 壳+React 13 页面逐模块+进程通信与本地安全+#22 Windows 细则+P 阶段对照+验证清单；勾销 #12/#16/#22/#28/#33（两侧齐备关闭）；待评审项 4 条 |
| v0.2 | 2026-09-06 | 第一轮评审拍板落实 | ①组件库确认 Ant Design 5+ECharts ②窗口形态确认单窗口+左侧导航 ③P1 最小 UI 范围确认 ④clawbot 接入形态写死：微信 ClawBot=OpenClaw 微信插件（本机 npx 安装+微信扫码绑定），经 OpenClaw 本地网关出站推送（仅出站）；设置页接入引导卡（安装命令/扫码步骤/三态通道状态/测试推送）；OpenClaw 网关 API 与限额 P4 实测——总纲 §10.4 同步 v0.17.8；§12 改评审确认记录 |
| v0.3 | 2026-09-06 | 第二轮 UX 评审拍板落实（ZCode 深度评审） | ①**新增 §5 全局设计规范**：红涨绿跌色彩语义（A1）/数字与时间格式（A6——交易时间戳北京时间+“京”标注，总纲 §10.1 同步 v0.17.9）/危险操作确认分级三级（A2/B8）/三态与首启引导流（A3，P1）/图表双主题与降采样/虚拟滚动与高 DPI（C 类）②直控台闭环：解除冻结/恢复+黄标联动+清仓进度（A2）③对话面板：异步回执链+任务状态条+日报折叠+通知定位未读锚定（A4）④知识库：概念归并确认队列（spec-05 §3.11 落点）+经验提取报告+状态机时间线（A5）⑤交易中心：回放可视化 P3（B1）⑥性能监控：预算进度条（B2）⑦数据监控：今日采集状态卡（B3）⑧策略详情：资金曲线区间+沪深300 基准线+指标摘要卡+持仓-条件单关联（B4/B7）⑨通知历史铃铛（B5）⑩日报中心"报告"标签（B6）⑪设置生效语义标注（C）；页面节重编号（原 §5→§6 等）；测试计划大幅扩充 |
| **v0.4** | 2026-09-06 | 夜间优化轮（**用户追认定稿**） | ①§6.4 策略详情页**试运行横幅**（trial 态 + L2 近似口径提示 + 复位后消失，联动 spec-01 §2.8）②§6.10 launch 确认对话框**账户复位提示**（重置 10 万 + 归档留证，L2 级确认）③§6.13 API Key 加密实现注（DPAPI/密钥文件，P1 蓝图定）④P 阶段对照与测试计划同步；无新增页面，纯状态呈现与确认文案补全 |
| **v0.5** | 2026-09-06 | Web 部署版适配（**随总纲 v0.20 提交，待用户终审**） | ①**形态重构**：桌面壳（Tauri2/sidecar）→ **Web 前端 + Nginx 反代 + core 常驻（systemd）+ 单用户登录鉴权**——§1 边界/§2 栈/§3 通信与访问安全/§7 部署承载/§8 #22 部署工程细则 改写 ②**默认通知通道=站内通知（web）**，全局替换桌面通知术语（§4/§6.1/§6.13/§7/§10）③新增**账户与安全设置组**（改口令/会话/强制下线，§6.13）④#41/#42/#56 改写为 core 进程级单实例/systemd 守护/交易时段进程在线保障（§3/§7/§9/§10）⑤§12 验证清单替换为部署与访问实测/升级迁移演练/进程在线 40 分钟 ⑥标题与文件名改 Web 端 UI；页面模块规则不变 |
