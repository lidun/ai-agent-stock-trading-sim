# spec-01 模拟交易引擎规格 v0.7（Web 部署版适配：进程保障与引用同步；业务规则承 v0.6）

> 版本：v0.7（2026-09-06，Web 部署版适配轮·随总纲 v0.20 提交，待用户终审：引擎业务规则与 schema 完全沿用 v0.6（满仓 #62 / 独立 trial 账户 #63 不变），仅做承载表述同步——§3.8 备份安全点引用由"spec-02/桌面规格"改为 spec-02 §10；§16 勾销 #22 由"Windows 细则（转桌面）"改写为"部署工程细则（Web 部署侧承接）"；引擎进程保障/守护/退出顺序归 core+systemd（spec-04 §9.2、总纲 §12））
> **状态：⏳ Web 部署版适配稿（随总纲 v0.20 一并以 Web 部署版 P1 基线提交，待用户终审定稿）——定稿后 v0.7 替代 v0.6 成为 Web 部署版开发基线（业务规则同 v0.6）。**
> 依据：总纲 v0.20（Web 部署版，业务决策承 v0.19：#62 满仓策略、#63 验证与试运行独立账户）§7 模拟交易引擎、§8.5 数据正确性守恒、§10.2 默认时间线、§12.1（core 常驻 systemd）、§12.8 决策表；联动 spec-05 v0.6（§4.2 独立验证账户）、spec-03 v0.6（官方收盘价独立就绪窗口、backfill）、spec-06 v0.5（持仓/流水/回放序列 UI 展示）
> **受约束决策**：#2 #29 #30 #31 #35 #38 #43 #44 #46 #47 #48 #49 #52 #53 #54 #56 #57 #58 #59 #61（引擎进程级守护 #41/#42 归 core+systemd、备份 #51 归存储规格 spec-02——Web 改写）
> **§16 勾销**：#1 #2 #3 #4 #5 #23 #26 #31 #32（完整归口）；#6 #22 #24 #29（引擎侧部分）——见文末勾销表
> 与总纲冲突时以总纲为准并同步本文件；v0.3/v0.4 已总纲同步（v0.17.2/v0.17.6），v0.5 对应总纲 v0.18（夜间优化轮，含 #61 试运行账户复位），v0.6 对应总纲 v0.19（满仓/独立账户 #62/#63），v0.7 对应总纲 v0.20（Web 部署版适配，业务规则同 v0.6）。

---

## 1. 模块定位与边界

- **职责**：模拟账户管理、条件单受理与撮合（默认 EOD-Replay 盘后回放）、资金与持仓核算、费用与公司行动处理、风控硬红线执行、卖出跟踪与信号注册（登记与 N 日结算）、**策略版本记账（v0.4，§2.7）**、结算产物产出（结算结果、日报数据段、对账报告）。
- **不负责**：盘中 3 秒行情采集器实现与数据落地（spec-03，引擎仅消费其产物与定义判定接口）；LLM 决策（子 Agent 行为规格）；知识库统计口径（spec-05）；分仓验证的窗口判定与晋升/回滚流程（spec-05 §4——引擎只提供版本记账与回滚结算支持）；UI 展示（spec-06）。
- **唯一交易通道**：子 Agent 的一切买卖意图通过条件单表达；引擎是唯一执行者（信任边界内，见 #33——执行器属主校验由编排层执行器封装，引擎自身按调用方注入的 agent_id 做属主校验）。

## 0. 数值精度约定（v0.3 新增，全局适用）

- **价格/金额/费用/数量一律 Decimal 运算**，禁止 float 进入撮合与对账路径；浮点比较（涨跌停价、触发价）一律 Decimal 精确比较（`=Decimal('10.00')` 级别），禁止 epsilon 容差比较随实现自定。
- SQLite 存储格式：价格/金额以 **TEXT 存 Decimal 字符串**（如 `'10.35'`，保留 4 位小数 `DECIMAL(12,4)` 语义），数量存 **TEXT**（允许零股卖出，小数位 ≤2）；禁止 REAL 列。
- **涨跌停价取整规则**：涨停价 = `round_half_up(昨收 × (1+限幅), 2)`，跌停价 = `round_half_up(昨收 × (1−限幅), 2)`（银行家舍入一律不用，统一四舍五入到分）；与交易所规则一致，逐票计算并缓存当日值。
- 完整率/偏差率等比率比较用 Decimal，阈值比较严格大于/小于（≥90.0% 判达标不含 89.99…% 因精度噪声——单测固定 89.9%/90.0% 边界用例）。
- **时间戳存储约定（v0.5）**：交易类时间戳（created_at/trade_time/settle 时刻/采集 ts 等）一律按**北京时间 naive 存储**（A 股无夏令时，与交易日历同基准，规避时区换算歧义）；显示层"京"标注归 spec-06 §5.2（数据语义不变）。
- **结算可复算性（v0.5）**：回放结算实现为纯函数管道（输入=条件单集合+价格序列+参考数据快照，输出=成交与账务变更）——同一输入重跑产出一致结果，支撑崩溃重跑、审计复算与 §10 回归金标准集。

## 2. 领域模型与数据表（SQLite schema 草案）

### 2.1 accounts（模拟账户）
```
id TEXT PK            -- 账户ID（= 子Agent ID，1:1）
initial_capital DECIMAL  -- 初始资金（默认 100000）
cash DECIMAL          -- 当前现金
nav DECIMAL           -- 份额净值（#48 份额法）
shares DECIMAL        -- 用户份额（外部现金流=申购/赎回份额，不计盈亏）
total_pnl DECIMAL     -- 累计盈亏（份额法口径下的策略盈亏）
today_pnl DECIMAL     -- 今日盈亏
granularity TEXT      -- 当前撮合粒度: eod_replay | intraday_5m | intraday_1m
granularity_history JSON  -- 粒度变更流水（审批单引用）
settle_key TEXT       -- 最后已结算的交易日+账户唯一键，格式 `YYYY-MM-DD:account_id`（幂等锚点）
status TEXT           -- trial | normal | paused_buy | halted | archived（#3.5 生命周期映射；trial=试运行回放期，§2.8）
active_version_no TEXT  -- 当前 active 策略版本（spec-02 §9，v0.4 联动）
created_at / updated_at
```
注：v0.3 删除 `frozen_cash` 字段——卖出资金当日可用（§6.2）后不存在"冻结资金"状态；未结算占用以"已成交待日终入账"？否——回放结算为一次性事务（§3.8），事务内直接落账，无冻结窗口。

### 2.2 holdings（持仓）与 lots（T+1 买入批次）——lots 为唯一事实源
```
holdings: id PK, account_id, symbol, quantity, avg_cost(真实价含费,只读派生展示),
          updated_at
lots: id PK, account_id, holding_id, buy_trade_id, buy_date, buy_price,
      quantity, remaining,                       -- remaining=未核销股数（T+1 可卖唯一事实源）
      strategy_version_no TEXT,                  -- v0.4：批次所属策略版本（继承 buy_trade 的版本）
      corp_action_flags TEXT                     -- v0.5：送转/除权重写标记（公司行动重写留痕，§6.6）
-- T+1 规则：卖出可卖数量 = Σ lots(其 buy_date < 今日) 的 remaining；
-- 当日买入入 lot（remaining 全额），T+1 起可卖
```
- **v0.3：删除 holdings.frozen_qty 双轨字段**。可卖数量、冻结口径一律由 lots 派生；avg_cost 为派生只读值（= Σ(lot.buy_price×remaining)/Σremaining + 摊入费用规则），每日结算事务中以 lots 重建校验，不一致即对账报警（不静默修复）。
- **公司行动日存量 lot 同步重写（v0.3 新增）**：送转比 s：重写该 holding 下所有存量 lot——`buy_price ÷ (1+s)`、`quantity × (1+s)`、`remaining × (1+s)`（数量 Decimal 取整到股，向下取整，尾差计入 audit_log）；holdings.avg_cost 同步重算；audit_log 记录重写事件与前后值。保证 FIFO 核销键一致性。

### 2.3 condition_orders（条件单——交易意图唯一通道）
```
id TEXT PK                -- 单调可排序 ID（ULID），字典序=时间序（供 priority 平局裁决，见 §3.4）
account_id TEXT FK        -- 属主（引擎按此做属主校验）
order_type TEXT           -- buy | sell_take_profit | sell_stop | sell_trail |
                              sell_open_board | buy_seal_confirm | basket | time | combo
direction TEXT            -- buy | sell
scope TEXT                -- single(symbol) | basket(symbols[]) | holding
symbol TEXT / symbols JSON
trigger JSON              -- 结构化触发条件（见 2.4），schema 校验（#45 由执行器前置）
basis TEXT                -- 判断基准档: replay_l0 | approx_l2 | intraday（默认按类型表 4.2）
price_ref TEXT            -- absolute | pct_change(昨收) | vs_cost（触发维度 §7.3）
qty DECIMAL / amount DECIMAL / budget DECIMAL(篮子)
price_type TEXT           -- market | limit(price)
validity TEXT             -- today | until(date) | long
priority INT              -- Agent 声明优先级（同日同刻冲突时用）
status TEXT               -- active | partial | filled | cancelled | expired | invalid
insufficient_events INT   -- v0.3：资金不足事件计数（≥1 时日报说明；不改变单据状态）
invalid_reason TEXT       -- v0.3：置 invalid 时记录原因（如 basis 与当日档不兼容）
strategy_version_no TEXT  -- v0.4：下单时点策略版本快照（默认=account.active_version_no；
                              验证期任务下创建的单=验证版本号，spec-05 §4.2）
created_at DATETIME       -- 引擎盖章（可信时间源，防 Agent 声称）
creator TEXT              -- agent_id（执行器注入）
reason TEXT               -- 创建理由（Agent 填写，入日报）
settled_on DATE           -- 已参与结算的交易日
```
- **v0.3（D2=A）**：`funds_insufficient` 从终态列表移除，改为**事件**（insufficient_events 计数 + audit 记录）——触发时资金不足的单**保持 active** 继续参与后续回放（资金回笼后复判，可能成交）；只在日终结算事务收尾时，若仍 active 且当日窗口结束，按 validity 规则转 expired（today）或保留（long）。日终快照可标注"当日曾资金不足"，但状态字段不因资金不足单独变值。

### 2.4 trigger 条件 JSON（示例）
```json
// 价格类（按该票当日实际判定档执行，见 4.2 矩阵）
{"kind":"price_le","price":10.0}                       // 价 ≤ 10
{"kind":"price_ge","price":11.5}
{"kind":"pct_chg","op":"le","pct":-5,"not_limit":true} // 当日相对昨收跌幅 ≥5% 且未跌停
{"kind":"vs_cost","op":"ge","pct":8}                   // 相对持仓成本 +8%
{"kind":"trail","drop_pct":3}                          // 回撤基准=持仓期间最高价（前复权口径，取值窗含买入当日）
{"kind":"open_board"}                                  // 触及涨停后首次开板（需持仓且当日曾封板）
{"kind":"seal_confirm","minutes":30}                   // 封板持续 ≥30 分钟（需盘口或 L0 判定，见 4.2）
{"kind":"volume","op":"ge","turnover_yi":2}             // 当日累计成交额 ≥2 亿
{"kind":"time","at":"14:50"}
{"kind":"and","parts":[...]} / {"kind":"or","parts":[...]}   // 组合
```

### 2.5 trades / settlement_log / signal_registry / audit_log
```
trades: id, account_id, order_id, symbol, side, qty, price(真实价), amount,
        fee_total, commission, stamp_tax, transfer_fee, trade_time(采样点时刻),
        basis_requested(单上 basis), basis_used(该票当日实际档 l0/l1/l2), quality, settle_date, reason,
        strategy_version_no TEXT  -- v0.4：继承 order 的策略版本
settlement_log: id, settle_key(UNIQUE `trade_date:account_id`), granularity_used(JSON 按票档清单),
        status(done), created_at        -- 幂等：settle_key UNIQUE；granularity_used 为 {symbol: 档位} 清单
signal_registry: id, account_id, sig_type(candidate|buy|sell|pitfall_intercept),
        symbol, reg_date, concept_tag, env_bucket, exception(0|1), pitfall_id,
        fwd_return_pct(填于 N 日后), fwd_end_date, quality,
        strategy_version_no TEXT,  -- v0.4：产生该信号的策略版本（验证评估与现役同期比较的分组键，spec-05 §4.2）
        trial_flag INT DEFAULT 0   -- v0.4：试运行历史回放期登记=1（spec-05 §3.11 统计隔离：默认排除于知识库状态机证据）
audit_log: id, ts, actor(agent_id|user|engine), action, target, payload_json, result
```

### 2.6 复权因子与公司行动
```
adjust_factors: symbol, ex_date, factor, kind(split|dividend|rights|delist), cash_per_share
suspensions: symbol, start_date, end_date, status        -- 停牌（含退市整理期标记）
delistings: symbol, delist_date, type(退市整理|直接退市), announced_at
```

### 2.7 策略版本记账（v0.4 新增；**v0.6 起废弃——被总纲 #63 独立验证账户方案替代**）

> **废弃说明（v0.6）**：strategy_version_no 字段链路**保留**（记录每笔交易归属的策略版本，跨版本历史可追溯）；但"验证子仓=账户内版本分组核算 / FIFO 版本内执行 / 窗口收尾强制平仓并入账户现金 / 30% 验证仓位上限"等分仓机制**全部废弃**——验证改跑独立账户（同参数、独立资金、满仓、独立结算），通过后主账户切换版本、验证账户归档（spec-05 §4.2 v0.5）。

- **版本标记链路**：`condition_orders.strategy_version_no → trades → lots → signal_registry` 全链路继承；下单时点快照——普通任务下单=账户当时 active 版本；**验证期任务**（spec-05 优化流程经执行器声明 trial）下单=验证版本号；
- **验证子仓 = 版本分组核算**：验证版本的条件单/成交/持仓批次独立分组核算盈亏（验证期期望值、"≥20 笔验证版本信号"计数、与现役同窗口现役版本信号统计比较——均按 version_no 分组取数）；现役版本余仓照常运行；
- **账户级共享不变量**：资金池共享（现金同一池）、风控硬红线/单票上限/T+1/对账守恒（§6.5）**一律按账户级整体核算，不因版本分组拆分**——验证期仓位 ≤30% 的约束由 spec-05 §4.2 流程在提交时声明，引擎只执行账户级硬红线；
- **窗口收尾结算（v0.4 写死）**：验证窗口结束（通过晋升或失败回滚）时，验证子仓若有未平持仓——**按当日官方收盘价强制平仓并入账户现金，版本盈亏定格**（计入验证版本业绩，供判定与演进记录）；此后该票持仓归现役版本管理（新 lots 标 active 版本）；
- **窗口收尾平仓的 T+1 约束（v0.5 补）**：强制平仓同样受 T+1——窗口结束日买入的 lot 当日不可平，留待次一交易日由"验证评估"任务照会引擎按**次日官方收盘价**补平仓，版本盈亏随实际平仓日定格（防"当日买当日强平"违反 T+1）；
- **回滚语义**：rollback（spec-02 §9 API）只改 active_version_no 指针与失败标记，**不重算历史**；验证子仓收尾结算如上；
- 试运行（历史回放）期登记的信号标 `trial_flag=1`（与版本标记正交：试运行期所有信号均标 trial，无论版本）。

### 2.8 试运行回放结算与上线（v0.6 改写：独立 trial 账户，总纲 #63；原 #61 复位机制废弃）

- **落点（v0.6）**：试运行历史回放跑**独立 trial 账户**（创建子 Agent 时一并生成：主账户 normal + trial 账户 status=trial，同初始参数）——回放日逐日结算正常走 §3 流程（历史日数据已在库，不经 15:35 就绪窗口）；试运行期信号标 trial_flag=1（§2.5）；
- **上线（launch 单通过）**：trial 账户**整体归档留证**（trial_archive 快照，作《验收报告》证据）——主账户天然干净（10 万未动），**无需任何复位**；生产结算自上线交易日起算于主账户；
- **语义**：L2 近似业绩留在 trial 账户归档中（#57 精度差一致性，不进正式净值曲线）；正式业绩自主账户上线交易日起算（#48 份额法口径）；
- accounts 表新增 `role`（main|trial|validation）与 `parent_agent_id` 字段（v0.6 迁移 005）——主账户 role=main、parent=自身；trial/validation 账户 parent 指向主 Agent。

## 3. 撮合执行：EOD-Replay（默认档，P1 必做）

### 3.1 档位选择（数据就绪驱动 #43/#53；v0.17.2 票级选档）
1. 结算触发：**最早 15:35（可配）起算**，重试窗口至多 60 分钟（15:35+60m 为兜底时刻，超窗对序列缺失票强制 L2）；引擎每 tick 检查该账户数据就绪性；**本窗口仅适用当日实盘结算——历史日补跑（快进回放/试运行回放）直接按可得档结算（§2.8），不经本窗口**；
2. **档位判定按票级进行（v0.17.2，D4 采纳票级口径）**——账户内每只证券独立取档，同一账户不同票可以不同档；同一证券整日内不混档（段级/分钟级混合仍禁止）：
   - 该票 **L0**：本地 3 秒序列存在且该票**完整率 ≥90%**（分母=当日实际交易秒数，午休剔除、15:00 后延续采样段不计入，#56）→ 该票用 L0；
   - 该票 **L1**：该票 L0 不达标/当日离线 → 免费源当日 1 分钟线（或 ftai 分时，若验证通过），且已落地；
   - 该票 **L2**：两者皆无（老交易日补跑等）→ 日线区间近似，该票结算标"近似结算"；
   - **任一票降级不拖累其他票**；结算日志 granularity_used 记录 {symbol: 档位}，日报给出降级清单（哪些票哪档、原因）；
3. 未就绪 → 重试窗口（默认 60 分钟，起算点=15:35）；超窗兜底：仅缺数据的票以 L2 结算，已就绪票按其实际档结算（不再等待）；
4. **官方收盘价就绪独立窗口（v0.5，#59 配套）**：回放序列就绪 ≠ 官方收盘价就绪——L2 结算与收盘档成交（§3.2 步骤 3）均依赖官方日线源收盘价，其就绪**独立检查**、重试至当日 20:00（可配）；仍未得 → 该账户结算任务保持 pending 顺延并告警，**禁止以未经验证的替代价虚构收盘价**（宁可晚结、不可错结；可得时点实测归 spec-03 §4.1/§11）；
5. 同一账户同日只结算一次：写 settlement_log 前检查 settle_key UNIQUE；崩溃重跑不重复（#49，靠 §3.8 整事务原子性 + UNIQUE 双保险）。

### 3.2 回放算法（L0/L1，逐票独立执行）
```
输入：当日有效条件单集合 O（按 created_at 升序）、各票价格序列 S（票级档），
      官方日线源当日 OHLC/收盘价 C、持仓与 lots、板块/停牌/除权参考
1) 过滤：剔除停牌股（#54 前置过滤，采集范围即不含）；剔除收盘后提交的 validity=today 单；
   开盘前单（created_at<9:30）起点=9:30 后首个有效采样点（#31）；
   basis 与该票当日档不兼容的单 → 置 invalid + invalid_reason（见 4.2 矩阵）；
2) 逐票推进该票序列 S（L0：每采样点 t 有 price；L1：按分钟 OHLC 成交价口径见 3.3）：
   for t in S from max(min_created_at, 9:30) to 15:00:
     对每张涉及该票、在 t 前已创建且仍 active（v0.3 含"曾资金不足"）的条件单判定 trigger：
       · 价格类：以 t 的采样价（L0）或分钟区间（L1）判触达；
       · 事件类：维护该证券日内状态机（曾封板？开盘价=涨停价？开板时刻 t_open）；
       · 时间类/量能类：按累计值判定；
       · 盘前单起点 9:30；**回放起点=created_at 之后的采样点才参与（#38 天然防前视）**；
     触发 → 风控校验（§7）→ 资金校验（§3.4）→ 通过则撮合（3.3/3.4）；不足则记 insufficient 事件继续；
3) 收盘价档（#59）：该票当日所有成交价最终对齐官方日线源收盘价 C——
   若某单按"收盘档"成交则价=C；L0 序列末值与 C 偏差 >0.3%（与 §8.5 共用阈值）→ 该票当日
   quality=degraded，标注入结算与日报（degraded 只影响该票，不传染其他票，v0.17.2 票级口径）；
4) 结束后处理（§3.8 单事务）：撤销过期的 validity=today 单；篮子满额余单撤销；
   更新 signal_registry 进度；写 settlement_log(done)；触发对账自检（6.5）；生成结算结果消息。
```

### 3.3 成交价口径
- **L0 档**：价格类条件在触达采样点成交，成交价 = 该采样点价（3 秒采样级，非逐笔；滑点默认 0，可配）；
- **L1 档（v0.2 起，v0.3 补收盘分钟歧义）**：相邻分钟数据确认触达、按条件单价格成交——
  - 触达判定不依赖单分钟收盘价近似，用相邻分钟确认序列是否穿越条件价 X：
    · 分钟数据含 high/low（标准 1 分钟线）：X 被某分钟 [low, high] 覆盖且价格自上一分钟进入该区间 → 判定触达于该分钟；**触达分钟 = X 首次被覆盖的那个分钟**（v0.3 写死，不用"上一分钟"）；
    · 分钟数据仅 close（无 OHLC）：close[t-1] 与 close[t] 跨越 X → 判定触达于第 t 分钟（插值穿越点为推断价）；
  - **成交价 = 条件单价格**（触发价/限价 X，模拟挂单价成交）；market 或未带价时用推断市场价（OHLC 情形：买取触达分钟 low、卖取 high；仅 close 情形：取插值穿越点价）；
  - 价格优先不吃亏：推断市场价更优（买：市场价 < X；卖：市场价 > X）时按更优的市场价成交；
  - **v0.3 写死收盘分钟口径（消除 §3.2 步骤3 与主规则歧义）**：触达判定发生在 15:00 收盘分钟时，按收盘档处理——成交价 = min(条件价 X, 官方收盘价 C) 中对委托方**更差**者（保守口径：收盘分钟流动性按集合竞价计，不给占便宜价），即买入取 max(X, C)、卖出取 min(X, C)；quality 标注 `close_minute_fill`；理由：#59 规定收盘成交以官方源为准，同时避免把"按条件价成交"条款外延到集合竞价分钟（那里价格的形成机制不同）；
  - 该口径 P1 用真实分钟数据实测（§12 实测项 1）；
- **L2 档**：区间触达 + 当日官方收盘价成交；**移动止盈 L2 判定口径（v0.5 写死）**：触达 = 当日 low ≤ 参考高点×(1−drop_pct)（参考高点 = 买入以来日线 high 累计最大与当日 high 取大），成交价 = 官方收盘价，quality 标 degraded；
- 费用按成交价计算（6.4）。

### 3.4 撮合与资金占用（按回放时间顺序，天然解决 #46）
```
买入：校验（现金充足/单票上限 50%（按下单时点市值+当日成交价）/数量合法性（§3.7 D6）/
     新股与 ST 拦截/流动性 4.3）→ 现金扣减、持仓新增 lot（当日买入，T+1 起可卖；
     lot.strategy_version_no 继承 trade，v0.4）；
卖出：可卖数量 = Σ lots(buy_date < 今日) 的 remaining（v0.3：唯一事实源，无独立冻结字段；
     v0.4：验证子仓卖出仅核销同版本 lots——FIFO 在版本内执行）；
     成交后按 lot FIFO 减核、现金入账——卖出资金当日可用再买入（A 股规则，v0.2 确认）；
篮子：预算+选入规则（等权/最多 N 只/全部，按权重 P2）逐票触发占用；预算不足的后续票
     跳过并记 insufficient 事件（不置状态，见 2.3 v0.3）；
同刻冲突（同一采样点多单触发且资金不足）：按 Agent 声明 priority，平级按 order_id（ULID，字典序=时间序）；
资金不足：记 insufficient 事件、单保持 active 继续参与后续采样（v0.3 D2=A，资金回笼后可复判成交）；
```

### 3.5 封板与事件例外（#2/#5 撮合细则）
- 涨停封死：当日某时刻起 price == 涨停价（Decimal 精确比较，§0）且持续 → 该段买入单**不成交**（模拟"排队未买到"）；盘中开板后恢复可成交；一字板（开盘=涨停且全天未开）→ 全天买入不成交，日报"涨停未买入"；
- 跌停封死 → 卖出单不成交（除非此前已触发部分成交）；
- 开板卖出单：持仓股当日曾封板（price 触及涨停价）后首次出现 price < 涨停价 → 触发卖出，成交价 = 开板采样点价；"曾封板"以该票当日档 L0 序列判定——**开板/封板事件类单仅在 L0 有效**：该票当日为 L1/L2 时，开板类单自动置 invalid + invalid_reason=`basis_requires_l0`（不静默跳过；日报列出）；
- 量能/盘口依赖类（seal_confirm 需封单数据）：默认不启用，待盘口源验证（#26）。

### 3.6 板块与规则（判定表，撮合时实时计算）
| 板块/状态 | 涨跌幅限制 | 备注 |
|---|---|---|
| 主板 | ±10% | |
| 创业板/科创板 | ±20% | ST 仍 ±20% |
| 北交所 | ±30% | |
| 风险警示 ST（主板） | ±5% | |
| 注册制新股 | 上市前 5 日无限制 | 之后按板块 |
- 涨跌停价计算：§0（四舍五入到分，Decimal）；
- 停牌股：条件单挂起（active 保留），复牌日恢复判定（复牌日按该票当日档判定）；
- 新股/ST 买入默认拦截（账户可配置豁免，需审批）；已持仓遇 ST/退市不自动卖（除退市整理规则 6.6）。

### 3.7 申报数量规则（v0.3 写死，D6）
- 主板/创业板：买入 100 股整数倍；卖出可零股（不足 100 的尾股可一次性卖出）；
- **科创板（D6）**：买入 ≥200 股起，之后**按 1 股递增**（201、202…任意 ≥200 的整数）；卖出无下限（零股可卖）；
- 北交所：申报 100 股起、1 股递增（买卖同）；零股卖出同上；
- 校验失败 → 拒单（invalid + invalid_reason=`qty_rule`），schema 前置校验（#45）+ 撮合时二次校验双保险。

### 3.8 结算事务原子性（v0.3 新增，P0 修复）
- **一次日终结算 = 单个 SQLite 事务**（`BEGIN IMMEDIATE … COMMIT`）：trades 逐笔插入、holdings/lots/accounts 更新、signal_registry 推进、settlement_log(done) 写入、audit_log——全部同事务；任一步失败整体 ROLLBACK；
- 崩溃恢复：见无 done 日志 → 判定上次结算未完成 → **从头上跑**（幂等靠整事务原子性 + settle_key UNIQUE 双保险）；禁止"续跑半截事务"；
- 连接约定：`busy_timeout`（默认 5 秒）+ WAL 模式；写锁竞争在 §3.1 重试窗口内自然吸收（每 tick 重试），不让调用方感知锁；
- 备份安全点（联动 #51 总纲）：备份任务仅在"无进行中结算事务"时点执行（应用层安全点标志，见 spec-02 §10；Web 部署版下 core 常驻运行、结算与备份共用"结算中"标志的语义不变，退出顺序见 spec-04 §9.2）。

## 4. 判断基准：basis 与当日实际档（v0.3 全矩阵，D3）

### 4.1 basis 与票级档的 9×3 全矩阵
说明：行=条件单类型（按其 basis 声明归入），列=该票当日实际档；矩阵值：
- ✅ = 按该档正常判定与成交；
- ❌ = 该单置 invalid + invalid_reason（进日报，不静默）；
- ⚠️ = **退化判定并成交**，quality 标 degraded + 退化说明（写 trades.quality 与日报）。

| 条件单类型（默认 basis） | L0 | L1 | L2 |
|---|---|---|---|
| 买入/止盈/止损（价格类、price_le/ge） | ✅ | ✅（相邻分钟确认+条件价成交，§3.3）| ✅（区间触达+收盘价成交） |
| 涨跌幅触发（pct_chg，昨收基准） | ✅ | ✅ | ✅（区间触达近似） |
| 相对成本（vs_cost） | ✅ | ✅ | ✅（近似） |
| 移动止盈（trail） | ✅ | ⚠️ 退化=取该票当日分钟序列的"区间高点序列"做回撤基准（L1 OHLC 用每分钟 high 的累计最大值）；仅 close 时用分钟 close 累计最大值 | ⚠️ 退化=以当日最高价（官方日线 high）为唯一回撤参考点（触达=当日 high 相对回撤 drop_pct 即触发一次判定） |
| 开板/封板事件（open_board/seal_confirm） | ✅ | ❌ invalid `basis_requires_l0` | ❌ invalid `basis_requires_l0` |
| 量能（volume，日累计成交额） | ✅（采样累计） | ✅（分钟成交额累计，若分钟线含成交额字段；字段缺失时 ⚠️ 退化=按 close×volume 估算并标 degraded） | ⚠️ 退化=用日线成交额总量一次性判定（无日内累计演化，标 degraded） |
| 时间类（time） | ✅（时钟，与档无关） | ✅ | ✅ |
| 篮子（basket，内部票各自按上表行规则） | 内部各票按其类型行 | 内部各票按其类型行 | 内部各票按其类型行 |
| 组合（combo，AND/OR） | 递归按子条件行 | 子条件任一 ❌ → 整单 ❌ invalid（保守）；⚠️ 子条件退化则整单 ⚠️ | 同左 |

- basis=intraday（盘中档声明）的单在 EOD-Replay 日（账户未启用盘中档）提交时：引擎拒绝创建（§4.2 档位不符即拒绝，规则不变）；
- **移动止盈 L1/L2 退化判定为允许（D3 拍板 A 方案）**——退化实现必须整日统一（不因退化中途切回），quality 标 degraded，日报注明退化类型。

### 4.2 除权日核验（#44）
- 价格类条件单触发基准一律用**前复权口径即时换算**：库内真实价+复权因子，判定前按当日复权因子把触发价换算到同口径；除权日不因跳空误触发/漏触发；成交与成本仍用真实价。

### 4.3 流动性约束时点口径（§16 #4/#32 落地）
- 单笔 ≤ 当日成交额 1%：成交额取**判定时点已发生的累计成交额**（L0 按触达采样点累计、L1 按该分钟前累计、INTRADAY 按周期快照；分时快照无累计成交额字段时采集器须携带——spec-03 数据项）；
- 不足 1% 档位时**按可成交上限缩量成交**（部分成交），不整单拒绝；缩量后的余数若仍不足 1 手则该笔不成交并记 insufficient 事件；
- 篮子单内各票独立做该检查。

## 5. 盘中档（INTRADAY_5M/1M）——P2 实施，本规格给出规则

- 仅账户级经审批启用；撮合源 = 同一 3 秒采集流（每 5/1 分钟取快照判定，**数据保鲜 #35**：快照超过 5 分钟未更新 → 该轮暂停撮合不用 stale 价；连续 3 轮无数据 → 当日自动降级"仅挂单不撮合"或转 EOD，通知管理 Agent）；
- 触发即成交（快照价±滑点，滑点可配）；T+1、费用、上限等规则与回放档一致；
- **v0.3 写死"当日末次结算并入"（v0.2 一句话展开）**：INTRADAY 账户盘中已产生的成交记录，15:35 结算时只做**成交清点幂等合并**——已存在的 trades 不重复写、不重跑撮合、不重新判定；结算事务仅补充：settlement_log、signal_registry 推进、对账自检与日报数据段；**严禁重新执行撮合循环**（§3.8 事务内原子完成）。

## 6. 账户与资金规则

### 6.1 份额法净值（#48）
- 外部注资/撤资 = 份额申购/赎回：`份额变动 = 现金流 / 当日净值`；当日无净值（未结算）时挂起至下一结算日；
- 净值 = (现金 + 持仓市值(真实价) + 停牌估值) / 份额；策略盈亏 = 净值 × 份额 的增量——现金流永远不进盈亏；
- 净值曲线与 #30 信号统计输入均用此口径。

### 6.2 T+1 与批次（#46/#23）
- 当日买入 qty 入 lot；次一交易日起可卖；卖出按 lot FIFO 核销（§2.2 唯一事实源）；
- **卖出资金当日可用、不冻结**（v0.2 确认，无"冻结至次日"配置项）。

### 6.3 资金竞争（#46/#23）
- 按 3.4 回放时间序；同刻按 priority，平级 order_id（ULID 字典序=时间序）。

### 6.4 费用模型
```
佣金：rate(默认 0.00025) × 成交额，最低 5 元（买卖双向）
印花税：0.0005 × 卖出成交额（单边卖出）
过户费：0.00001 × 成交额（买卖双向，沪深均收）
红利税：默认不计（#52）；启用时按简化档——持有期 ≤1 个月 20% / 1 个月~1 年 10% / 超 1 年免；
        持有期 = 买入 lot 的 buy_date 至分红 ex_date（按自然月整月数取档）；
        分红入账时按各 lot 持股比例分摊计税扣减（v0.3 口径维持，v0.5 文字修正）
```

### 6.5 对账守恒与自检（#49 + §8.5；v0.3 D1=A 重写为双守恒式）
- **v0.3：两条守恒式并列独立校验，互不混写（D1=A）**：
  - **式一（金额守恒）**：`cash = Σ买入流出 − Σ卖出流入 + Σ分红 − Σ费用 + 注资 − 撤资`（全 Decimal，按账户级流水逐笔累计）；
  - **式二（NAV 守恒）**：`NAV = (cash + Σ持仓真实价市值 + 停牌估值) / shares`，且 `NAV × shares_end == cash + 持仓市值总额`（份额法恒等一致性，Decimal 校验）；
  - 二者每笔钱都从同一份 trades/流水表推导，**任何一条不平 → 告警 + 暂停该账户撮合（带病不运行）**；
  - 删除 v0.2 单式写法（原式混合了费用、NAV、净流入口径，两处规则无法对齐——评审发现的对账恒等式内部矛盾）；
- 成交价核验（§8.5）：随机抽 N 笔成交，用官方日线源当日 OHLC 核验成交价 ∈ [当日最低, 当日最高]（L2 收盘成交价 == 官方收盘价，Decimal 精确比较）；越界 = 对账不平处理；
- **版本分组核算与账户级守恒的关系（v0.4）**：§2.7 验证子仓分组核算**不改变**本节账户级守恒——分组核算仅为业绩归因视图（spec-05 消费），资金流水仍账户级整体校验。
- 纯函数（撮合/费用/除权/涨跌停判定）单测 fixture——见 §10 测试计划。

### 6.6 公司行动（#47，实现规则）
| 事件 | 处理 |
|---|---|
| 送转 | ex 日：存量 lot 与 holdings 同步重写（§2.2 v0.3 规则）；不动现金 |
| 现金分红 | ex 日：现金 += 每股股利 × 持股（扣红利税如启用，§6.4 起算口径）；成本不变 |
| 配股 | 默认放弃并审计记录（可配"参与"：按配股比例×持股扣现金缴款、股数增加）；**现金不足以缴款时自动放弃并审计（v0.3 写死，禁负现金红线一致性）** |
| 退市整理期 | 进入整理期首日**按当日官方收盘价自动清仓**（#59 口径，v0.2 确认）；整理期最后一交易日仍无法成交（跌停封死）→ 记零值核销并审计 |
| 直接退市 | 按停牌末价核销（stale-price 结清，quality 标注） |
| 长期停牌 | 持仓按最后成交价估值、净值标注"停牌估值"；停牌 >30 交易日通知管理 Agent 评估 |
- **v0.3 补退市整理"首日"判定路径**：退市整理期标记由 spec-03 每日**开盘前**刷新 `delistings` 表（delist_date+announced_at）取得；引擎在结算前对当日持仓做**二次校验**——若"持仓证券当日新增整理期标记"即触发首日清仓规则；数据缺失（当日表无记录但实际已进入整理期）时：按停牌处理并在日报**显著告警**（管理员可人工触发补跑）；不做 T+1 重跑前一日结算（复杂度不值）。

## 7. 风控（两层，7.7/#46）

- **硬红线（引擎强制，不可绕过）**：禁杠杆（现金为负即拒单）；T+1 冻结；异常单拦截（价格偏离、数量非法（§3.7）、金额>现金等，schema+规则双层）；**单票上限默认 100%（满仓策略，总纲 #62——参数保留仅作事故性防护，账户可配）**；篮子与单票共用同一上限检查——**以上均按账户级执行（v0.6：每个账户独立核算，主/验证/试运行账户互不合并）**。
- **软风控（策略级，引擎执行来自大纲的参数；v0.6 满仓范式下调整）**：**"现金下限/仓位分散"类参数废止（#62——风险规避靠择时择股）**；保留 止损线、回撤熔断%（个股/组合级别仍有意义）——按"大纲当前版本参数"执行（参数由子 Agent 动态设定、管理 Agent 审批，#3）；熔断触发 → 冻结新买入（仅处理减仓），复盘后经管理 Agent 同意恢复。
- 引擎不自行决定软风控数值；参数缺失时按账户默认模板执行并告警。
- **v0.3 新增（D5 用户确认）：熔断日的挂单与结算口径**——熔断触发时**已有挂单不撤单**；触发后新触发的**买入类**单不成交并标 `circuit_break` 事件（audit+日报，单保持 active 不推进），**卖出类**单照常判定与成交；熔断当日结算照常执行（不是不结算，是"买入侧冻结"）；解冻后（管理 Agent 同意恢复）挂单恢复正常参与。

## 8. 卖出跟踪与信号注册（登记与结算在引擎，统计在 spec-05）

- **登记点**（引擎自动）：候选入选（candidate，由子 Agent 选股产出触发登记调用）、买入成交（buy）、卖出成交（sell）、避坑拦截（pitfall_intercept + pitfall_id + exception 标记，#54）；
- 登记字段：signal_type/symbol/reg_date/concept_tag/env_bucket(客观标签由 spec-03 环境规则提供)/quality/**strategy_version_no（v0.4）**/**trial_flag（v0.4：试运行历史回放期=1，spec-05 §3.11 统计隔离消费）**；concept_tag 登记时**优先引用现有知识库条目名（受控词表，spec-05 §3.11）**，未覆盖新概念用自由标签；
- **N 日结算**（默认 N=10，账户可配）：调度在 reg_date 起第 N 个交易日收盘后执行——取该日官方收盘价（或最近可得价，停牌/退市 → stale-price 结清并标注，#34 引擎侧执行、统计归 spec-05），计算前瞻收益写入 fwd_return_pct；结算日志留痕；
- 结算产物推送：到期报告给子 Agent（复盘用）并供管理 Agent 汇总。

### 8.1 exit_trackings 卖出跟踪落库与报告判定（v0.5 新增，总纲 §7.6 落地）

```
exit_trackings: id, account_id, sell_trade_id, signal_id(sell 信号关联),
  symbol, sell_date, sell_price, qty, sell_reason(止盈|止损|调仓|主动|强平|清仓),
  status(tracking|done), track_end_date,
  fwd_return_pct(卖出后累计涨跌幅), bench_return_pct(同期沪深300), excess_pct,
  period_high, period_low, conclusion(卖对|卖平|卖早), is_loss_case(0|1),
  quality, created_ts, done_ts
```

- **登记**：卖出成交时引擎自动登记（1:1 关联 sell 信号与卖出 trade）；跟踪期每日收盘拉取该票与基准指数行情（调度器触发，spec-04 §2.2 第 3 项）；
- **第 10 交易日结算（确定性、零 token）**：计算 fwd_return / excess / period_high·low 与结论判定——**卖对**：fwd_return ≤ −3% 或 excess ≤ −3%（卖出后确实走弱）；**卖早**：fwd_return ≥ +5% 或 excess ≥ +5%（明显继续上涨）；其间=**卖平**（阈值 config 可调，P1 校准）；期间该票涨幅 > 卖出后持有至今收益 → 标"卖出损失案例"（少赚/多亏量化入报告）；
- 报告推送子 Agent 复盘（结论写入交易决策记忆与策略演进记录）；管理 Agent 健康度消费"卖出质量趋势"（spec-05 §7.3）；按卖出理由类别的统计归 spec-05 消费；
- 试运行期卖出同样登记；账户复位（§2.8）后随试运行数据归档、不再跟踪（trial 标记）。

## 9. 引擎"可执行性 checklist"（§16 #5，P1 验收逐项核；v0.3 补三项；v0.4 补一项）
- [ ] 买入数量规则（主板/创业板 100 整数倍、科创板 200 起后 1 股递增、北交所 100 起后 1 股递增；卖出可零股）
- [ ] 封板/一字板买入不成交、跌停卖出不成交、开板恢复
- [ ] 停牌挂起/复牌恢复；停牌股不进判定序列（#54）
- [ ] 佣金最低 5 元、印花税卖出单边、过户费
- [ ] 板块差异化涨跌停实时计算（含 ST、新股前 5 日；§0 四舍五入+Decimal）
- [ ] L1 相邻分钟触达判定（OHLC 覆盖 / close 插值）+ 按条件价成交 + 收盘分钟保守口径（§3.3）
- [ ] **票级档位判定与降级清单（v0.17.2）：任一票降级不拖累他票、日报降级清单**
- [ ] basis × 档位全矩阵（§4.1 表逐格实现：✅ ❌(invalid_reason) ⚠️(degraded)）
- [ ] **资金不足→回笼→复判→成交（v0.3 D2，insufficient 事件语义）**
- [ ] **对账双守恒式（金额+NAV 各自独立校验，D1=A）**
- [ ] **结算事务原子性（§3.8：单事务、超时回滚、崩溃从头上跑）**
- [ ] 除权日复权换算、送转/分红/配股/退市整理自动处理（含存量 lot 同步重写）
- [ ] T+1 批次（lots 唯一事实源）、卖出资金当日可用
- [ ] 收盘价口径 #59（0.3% 偏差 degraded 标注、**票级不传染**）
- [ ] 幂等：同账户同交易日重跑结算不重复成交（settle_key UNIQUE + 整事务双保险）
- [ ] 对账双守恒 + 成交价抽检核验
- [ ] **Decimal 全链路（价格/金额/数量无 float 进入撮合与对账路径）**
- [ ] 策略版本标记（v0.6：strategy_version_no 作为记录字段保留；分组核算/版本内 FIFO **废弃**——独立账户方案 #63）
- [ ] **试运行独立 trial 账户（v0.6 §2.8：主/trial 双账户创建、trial 归档留证、主账户零污染）**
- [ ] **满仓买入（v0.6 §7：单票上限 1.00 默认——全现金买入成交用例）**
- [ ] **官方收盘价独立就绪窗口（§3.1 第 4 项：序列 60 分钟 + 收盘价至 20:00，不虚构替代价）**
- [ ] **卖出跟踪 exit_trackings（§8.1：登记/第 10 日结算/结论判定阈值/卖出损失案例）**
- [ ] **结算可复算性（§0：同输入重跑同输出）**

## 10. 测试计划（#49 fixture 单测清单；v0.3 扩充；v0.4 补）
- 撮合纯函数：触发判定（价格/涨跌幅/移动止盈/开板事件）单测含边界（等于触发价、一字板、9:30 起判）；
- 移动止盈三档退化：L0 基准 / L1 分钟 high 累计最大值 / L2 日线 high 唯一点；
- basis×档位矩阵：9 行 × 3 档逐格用例（✅ 成交、❌ invalid_reason、⚠️ degraded 退化说明）；
- 回放时间序：资金竞争、同刻 priority、order_id（ULID 字典序）平局裁决；
- **资金不足→回笼→复判→成交**（D2 语义验收用例）与"日终仍不足但 active（long 单）继续跨日"场景；
- 费用：佣金最低 5 元边界、印花税单边、科创板费率、红利税持有期整月取档（§6.4）；
- 除权除息：送转**存量 lot 重写**（buy_price÷(1+s)、quantities 同步、FIFO 键一致性）、分红入账、除权日条件单不误触发；
- 事务原子性：结算中途崩溃（模拟 kill）→ 恢复从头上跑 → 零重复成交、零半截数据；
- 对账：金额守恒与 NAV 守恒用例（含"现金回笼"与"分红+费用"混合场景）；成交价越界检测；**Decimal 精度用例：float 陷阱值（0.1+0.2 类）不产生分差**；
- 票级降级：同账户票 A(L0) + 票 B(L1) 并存结算、降级清单正确性、degraded 不传染；
- 涨跌停价：四舍五入到分 vs 交易所真实值抽样（各板块 20 只，构造数据对照）；
- 熔断日口径（D5）：买入标 circuit_break 不推进、卖出照常、解冻恢复；
- 幂等：同键重跑零重复；INTRADAY 当日成交清点合并（不重跑撮合）；
- 科创板数量规则（D6）：200 起买、1 股递增、201/199 拒单边界；
- **策略版本记账（v0.4）：现役+验证并行下单版本标记正确/验证子仓按版本分组核算（期望值与现役同期比较取数）/验证版本卖出仅核销同版本 lots/窗口收尾未平仓按官方收盘价并入且版本盈亏定格/回滚不改历史/account 级风控与单票上限跨版本合计计算/trial_flag 登记与正交性**；
- **试运行复位（v0.5）**：复位单事务原子性/归档快照完整性/重置后对账平（双守恒）/signal_registry 保留与 trial_flag 隔离/复位后条件单全作废；
- **收盘价就绪分离（v0.5）**：收盘价不可得时结算 pending 顺延（不虚构价）、恢复后自动续结、20:00 超窗告警；
- **exit_trackings（v0.5）**：第 10 日判定阈值边界（−3%/+5%）/卖出损失案例标注/试运行卖出复位后停止跟踪；
- **回归金标准集（v0.5）**：固定历史日输入（条件单+序列+参考数据）→ 固定期望成交与账务输出，纳入 CI 防回归。

## 11. §16 勾销表（本文件状态）

| # | 事项 | 状态 |
|---|---|---|
| 1 | 撮合细则（整手/起买/最低佣金） | 已写入 §3.7/§6.4/§9（科创板 1 股递增 v0.3 定稿） |
| 2 | 封板例外规则 | 已写入 §3.5 |
| 3 | 判断基准默认档表 | 已写入 §4.1（v0.3 升级为 9×3 全矩阵） |
| 4 | 流动性时点口径 | 已写入 §4.3 |
| 5 | 可执行性 checklist | 已写入 §9（v0.3 补 3 项：复判成交/双守恒/事务原子性；v0.4 补版本记账） |
| 6 | 信号注册表 schema（引擎侧） | 已写入 §2.5/§8（v0.4 补 strategy_version_no/trial_flag） |
| 22 | 部署工程细则（引擎进程相关部分；v0.20 Web 改写原"Windows 细则"） | Web 部署侧承接：spec-06 §8（总纲 §16 #22）+ 引擎进程保障/守护/退出顺序归 core+systemd（spec-04 §9.2）——**引擎侧本项关闭** |
| 23 | 资金竞争与 T+1 批次 | 已写入 §3.4/§6.2/§6.3（lots 唯一事实源 v0.3） |
| 24 | EOD-Replay 回放执行细则（引擎侧） | 已写入 §3（v0.3：票级选档 + 事务原子性）；#30 完整率按票归 spec-03 |
| 26 | 条件单类型扩展细则 | 已写入 §2.4/§4.1/§3.5 |
| 29 | 收盘价偏差阈值（引擎侧） | 已写入 §3.2/§3.3（0.3%，票级不传染） |
| 31 | 盘前单起判 | 已写入 §3.2 步骤 1 |
| 32 | 流动性约束时点口径 | 已写入 §4.3 |

## 12. 评审确认与剩余实测项

**第一轮评审拍板（2026-09-04，落实于 v0.2）**：
1. L1 成交口径改相邻分钟确认 + 按条件价成交（§3.3）；
2. 收盘价偏差阈值 0.3%（总纲 v0.17.1 同步）；
3. 卖出资金当日可用不冻结（§6.2）；
4. 退市整理清仓按官方收盘价（§6.6）；
5. 篮子三档 + 按权重 P2。

**第二轮评审拍板（2026-09-05，落实于 v0.3）**：
1. **D1=A**：对账守恒式重写为**双式并列**（金额守恒 + NAV 守恒，§6.5），删除 v0.2 单式；
2. **D2=A**：funds_insufficient 从终态改为**事件计数**，单保持 active 可复判成交（§2.3/§3.4）；
3. **D3=A（含矩阵）**：basis × 档位 9×3 全矩阵（§4.1），移动止盈 L1/L2 允许退化判定并标 degraded；
4. **D4=票级选档**（总纲 v0.17.2 同步修订）：档位与完整率按票判，任一票降级不拖累他票；降级清单入日报；
5. **D5 确认**：熔断日挂单不撤、买入标 circuit_break 不推进、卖出照常、解冻恢复（§7）；
6. **D6=1 股递增**：科创板 200 起买后 1 股递增（§3.7），北交所同步写死；
7. 其余评审建议全部采纳：收盘分钟保守口径、涨跌停价四舍五入、Decimal 全链路、存量 lot 重写、退市整理二次校验+缺失告警、settle_key 格式统一、order_id 用 ULID、frozen_qty 删除、配股现金不足自动放弃、红利税整月取档、INTADAY 幂等合并写死、费率与涨跌停实测入单测、§4.3 归位第四章。

**第三轮评审拍板（2026-09-05，联动 spec-05 v0.3，落实于 v0.4；总纲 v0.17.6 同步）**：
1. **分仓验证记账=方案 A 账户内版本标记**（用户于 spec-05 v0.3 评审确认）：condition_orders/trades/lots/signal_registry 加 strategy_version_no、signal_registry 加 trial_flag（§2.7）；
2. 配套口径：验证子仓版本分组核算（风控仍账户级、FIFO 版本内执行、窗口收尾未平仓按官方收盘价并入且版本盈亏定格、回滚不改历史）；
3. concept_tag 登记受控词表优先（spec-05 §3.11 联动）。

**第五轮（2026-09-06，满仓策略+独立账户，用户决策直接落地，v0.6）**：
1. §7 硬红线单票上限默认 100%（总纲 #62 满仓策略；参数保留作事故防护）；
2. §2.7 策略版本记账废弃（字段链路保留作记录；分组核算/版本内 FIFO 删）；
3. §2.8 改写：试运行跑独立 trial 账户（主/试双账户），上线=trial 归档留证、主账户零污染——#61 复位机制废弃（总纲 #63）；
4. accounts 迁移 005：role（main|trial|validation）+ parent_agent_id。

**第四轮（2026-09-06，夜间优化轮·用户追认后定稿，落实于 v0.5）**：
1. 文本损坏修正（§2.2 holdings/lots 字段块、§3.7 "起buy"错字、§6.4 红利税表述）——非语义变更；
2. **exit_trackings 落库与结论判定阈值**（§8.1 新增，总纲 §7.6 机制落地补全——总纲有流程无落库，此前为缺口）；
3. **官方收盘价就绪独立窗口**（§3.1 第 4 项：序列窗口 60 分钟与收盘价窗口（至 20:00）分离，禁止虚构替代价）——修复"L2 超窗兜底自身也依赖官方收盘价"的隐含死锁；
4. **试运行账户复位**（§2.8 新增，总纲 #61 联动）+ **验证子仓收尾 T+1 约束**（§2.7）——修复"试运行回放结算落点未定义"与"窗口收尾当日强平违反 T+1"两个缺口；
5. L2 移动止盈判定口径写死（§3.3）、时间戳存储约定与结算可复算性（§0）、checklist/测试计划同步扩充（含回归金标准集）；
6. **用户追认（2026-09-06 上午）**：四项待决全部同意——#60 任务级执行预算（30 轮/60 次默认值）、#61 试运行账户复位、L0 采集载体修正、exit_trackings 判定阈值初值（−3%/+5% 可配）；v0.5 定稿为开发基线。

**剩余实测项（P1 数据接入后执行，结果固化回本文件）**：
1. L1 相邻分钟触达/按条件价成交 + **收盘分钟保守口径** vs 真实分钟数据的偏差实测；
2. 收盘价偏差阈值 0.3% 实际分布实测；
3. 涨跌停价计算 vs 交易所真实值抽样（各板块 ≥20 只）——已入 §10 单测，无需真实数据接入，P1 编码期执行；
4. 费率边界（佣金 5 元/印花税单边/过户费）5×5 构造场景——已入 §10 单测，P1 编码期执行。

## 变更记录
| 版本 | 日期 | 状态 | 要点 |
|---|---|---|---|
| v0.1 | 2026-09-04 | 草稿 | 初稿：覆盖 §16 引擎侧全部待决项，schema 草案与回放算法、撮合细则、测试计划 |
| v0.2 | 2026-09-04 | 第一轮评审修订 | ①L1 档改相邻分钟确认+按条件价成交 ②阈值 0.5%→0.3% ③卖出资金当日可用 ④退市清仓按官方收盘价 ⑤篮子三档+按权重 P2 |
| v0.3 | 2026-09-05 | 第二轮评审修订（ZCode 深度评审） | ①D1=A 对账双守恒式（金额+NAV，删 v0.2 矛盾单式）②D2=A funds_insufficient 改事件，单保持 active 可复判 ③D3 basis×档 9×3 全矩阵（移动止盈允许退化+degraded）④D4 票级选档（总纲 v0.17.2 同步：档位/完整率按票判，降级不拖累、不传染）⑤D5 熔断日口径（circuit_break 事件）⑥D6 科创板 200 起后 1 股递增⑦新增 §0 数值精度（Decimal 全链路、涨跌停价四舍五入）⑧§3.8 结算事务原子性 ⑨存量 lot 公司行动重写、退市整理二次校验、配股不足自动放弃、红利税整月取档 ⑩settle_key 统一 `date:account_id`、order_id 用 ULID、删 frozen_cash/frozen_qty ⑪§4.3 归位第四章、INTRADAY 幂等合并写死、checklist/测试计划扩三项+Decimal 用例 |
| **v0.3 定稿** | 2026-09-05 | **用户确认定稿** | 状态冻结为开发基线；下一步 P1 蓝图可直接引用 |
| v0.4 | 2026-09-05 | 联动修订（spec-05 v0.3 分仓记账拍板承接；总纲 v0.17.6 同步） | ①**§2.7 策略版本记账新节**：condition_orders/trades/lots/signal_registry 加 strategy_version_no、signal_registry 加 trial_flag、accounts 加 active_version_no ②验证子仓=版本分组核算（期望值/20 笔计数/现役同期比较的取数键），FIFO 版本内执行 ③窗口收尾：未平仓按官方收盘价并入、版本盈亏定格、回滚不改历史 ④风控/单票上限/对账守恒仍账户级（跨版本合计）⑤§8 登记字段补齐+concept_tag 受控词表优先 ⑥checklist/测试计划补版本记账项；无引擎行为规则变更，纯 schema 扩展与记账口径 |
| **v0.5** | 2026-09-06 | 夜间优化轮（用户追认定稿） |
| **v0.6** | 2026-09-06 | 满仓策略+独立账户（用户决策落地） | ①§7 硬红线单票上限默认 **100%（满仓 #62）**、软风控废"现金下限/仓位分散"类 ②§2.7 策略版本记账**废弃**（字段保留记录）③§2.8 试运行改**独立 trial 账户**（#63，复位机制废弃；主账户零污染）④迁移 005：accounts.role+parent_agent_id ⑤checklist 同步 | ①文本损坏修正（§2.2 字段块/§3.7 错字/§6.4 表述）②**§8.1 exit_trackings**：卖出跟踪落库 schema+第 10 日结论判定阈值（卖对 −3%/卖早 +5%，可调）+卖出损失案例（总纲 §7.6 落地补全）③**§3.1**：官方收盘价就绪独立窗口（至 20:00，不虚构替代价）+历史日补跑不经就绪窗口④**§2.8 试运行回放结算与上线复位**（总纲 #61：trial 态/复位归档/重置 10 万/信号保留）⑤§2.7 窗口收尾 T+1 约束⑥§3.3 L2 移动止盈判定口径⑦§0 时间戳存储约定+结算可复算性⑧checklist +4 项、测试计划 +5 类（含回归金标准集）
| **v0.7** | 2026-09-06 | Web 部署版适配（**随总纲 v0.20 提交，待用户终审**；业务规则同 v0.6，无新增行为决策） | ①§3.8 备份安全点引用改写：spec-02/桌面规格 → **spec-02 §10**（Web 部署版 core 常驻语义不变，退出顺序归 spec-04 §9.2）②§16 勾销 #22 改写：Windows 细则（转桌面）→ **部署工程细则（Web 部署侧承接：spec-06 §8/总纲 §16 #22；引擎进程保障归 core+systemd）** ③依据/决策注与交叉引用版本同步（总纲 v0.20；spec-03 v0.6/spec-05 v0.6/spec-06 v0.5；"归桌面"改 core+systemd/存储规格）④引擎 schema/撮合/结算/满仓/独立账户业务规则零变更 |
